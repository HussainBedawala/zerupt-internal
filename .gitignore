# macOS
.DS_Store

# Code repo (separate git remote)
erp/
.gstack/

# ZATCA work tree — a SEPARATE nested git repo/worktree; never commit into this repo
erp-zatca/

# Scratch / working notes (orchestration plans, ad-hoc artifacts)
tmp/

# Ephemeral scheduler lock (machine-local, not source)
.claude/scheduled_tasks.lock
.neon

# graphify codebase-graph outputs (regenerable; keep GRAPH_REPORT.md tracked)
study/ops/graphify/graph.json
study/ops/graphify/*.html
study/ops/graphify/obsidian/
study/ops/graphify/.graphify_*
study/ops/graphify/2026-*/
study/ops/graphify/cache/
