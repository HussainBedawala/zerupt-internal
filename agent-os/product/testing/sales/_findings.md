# Sales Module — Findings Log

> Log every real defect found during a live testing pass. One row per finding. Fix by severity; re-run the affected checklist item after each fix and mark it FIXED with the commit.

Severity rubric: see [`../README.md`](../README.md). CRITICAL / HIGH / MEDIUM / LOW.

## Sign-offs

- **06 — Credit Notes / Sales Returns — SIGNED OFF 2026-07-05.** Live-verified at the ledger on prod Al-Asala Auto Parts. 6 defects found + fixed this pass: #41 (HIGH, credit ignored the invoice-line discount → over-credit + full-credit hard-block; migration 0155 added `sales_credit_note_lines.discount_amount` + pro-rated snapshot + net-of-discount recompute; dialog preview also fixed to net the discount), #43 (HIGH, issue-credit-note dialog dead-ended a sole owner on a mandatory approving-manager PIN → gated on `requireInvoiceApproval`, default OFF, mirroring invoice void), #42 (MEDIUM, A4 print hardcoded the discount column to 0), #44 (MEDIUM, dialog clipped rows / wouldn't scroll — stop fighting the base DialogContent scroll container), #45 (MEDIUM, unreadable CONFIRMED badge → shared `InvoiceStatusBadge`), #46 (MEDIUM, item UUID instead of SKU on dialog + print → `useItemMap`). Ledger tie-outs (prod): **Test 1** full credit INV-00005 = CN-00001 157.000 (Cr 1131 Bader 157 / Dr Sales-Returns 4200 157; Dr Inventory 1141 106.442 / Cr COGS 5100 106.442 @ ORIGINAL WAC 8.870196, not current) — discounted pack line reversed the exact 157, not raw 162. **Test 2** partial credit INV-00006 BAT-0069 line = CN-00002 162.000 (same GL shape); invoice correctly stayed open for the uncredited BAT-0083 line (balance 9.710). **Test 3** over-credit of the fully-credited INV-00005 = BLOCKED, nothing posted (only CN-00001 exists). **Test 4** double-submit = exactly ONE CN-00003 (9.710), single balanced JE (idempotent). Return cost = original realized WAC in every case (BAT-0069 8.870196, BAT-0083 6.220). Reconcile invariant HELD after all actions: Σ open-invoice balances per customer = customer 1131 (Bader 461.500, Al Salam 250.000, diff 0.000), total 1131 711.500. Code-verified invariants: over-credit guard (creditQty ≤ invoiced − prior confirmed, inside invoice `FOR UPDATE` lock), period gating (validatePeriod on the return date), voided-invoice block (create + confirm), durable outbox (`suppressErrors:false` + DLQ both listeners), audit + RBAC on controller. FX is inert for KWD-only Asala (multi-currency unimplemented for credit notes — deferred, consistent with the sales-module FX deferral).

- **04 — Sales Invoices — SIGNED OFF 2026-07-05.** Live-verified on prod Al-Asala Auto Parts. Findings #30/#31/#33/#34/#37/#38/#39/#40 all FIXED. Ledger tie-outs (prod): direct path INV-00005 = 157.000 (Dr 1131 157.000 / Cr Rev 157.000; Dr COGS 106.442 / Cr Inv 106.442 @ WAC; stock −12); SO→invoice path INV-00006 = 171.710 (Dr 1131 171.710 / Cr Rev 171.710; Dr COGS 112.662 / Cr Inv 112.662; stock −12; Σdebits=Σcredits=284.372) — dual-path GL mechanics identical (the 157 vs 171.710 delta is different order content, not a defect). AR subledger tracks GL 1131 (Bader 790.210, total 1131 1040.210) — reconcile holds. Lifecycle invariants code-verified HOLD: period gating on confirm+void (HardLocked rejects; void posts to original period), double-confirm idempotency (pre-tx status check + guarded `WHERE status='draft'` UPDATE + advisory lock), durable transactional outbox + DLQ (gated `suppressErrors:false` listener via `runListenerHandler`, not fire-and-forget), draft immutability after confirm (`requireDraft` guards addLine/updateLine/removeLine + remove). Carry-over: DEV-445 (promotions not applied on direct-sale + SO paths — dual-path revenue divergence only when a promo is active; not triggered for Asala, no active promos) remains OPEN, tracked in Linear.

| # | Date | Submodule | Severity | Summary | Repro (steps → expected vs actual) | Status | Commit |
|---|------|-----------|----------|---------|-------------------------------------|--------|--------|
| 1 | 2026-07-03 | 01-customers | MEDIUM | Raw ISO timestamp in customer-detail Invoices tab "Date" column | Open `/sales/customers/[id]` → Invoices tab → Date shows `2026-06-28T17:19:40.570Z` instead of a formatted date (Due date beside it renders correctly as `6/28/2026`). Expected: shared date formatter; Actual: raw ISO string. | FIXED | 2c6380b3 |
| 2 | 2026-07-03 | 01-customers | LOW | Customers list "Outstanding" figures have no currency indicator | `/sales/customers` → Outstanding column shows bare `250.000` with no KWD label anywhere. Expected: money reads as currency (KWD 3dp via shared money formatter). | FIXED | 2c6380b3 |
| 3 | 2026-07-03 | 01-customers | LOW | No CSV export on customers list | `/sales/customers` has Import but no Export; systemic finding #6 (missing CSV export on list). Expected: export matching on-screen columns. | FIXED | 2c6380b3 |
| 5 | 2026-07-03 | 01-customers | HIGH | CSV formula/injection in customer (and bills) export | Export a customer whose name/code = `=HYPERLINK("http://evil","x")` or `+cmd...` → cell opens as a live formula in Excel/Sheets (quote-escaping does not prevent this). `csvCell()` must neutralize leading `= + - @`. Same flaw in the copied `purchase/components/bills-list-panel.tsx`. Fix in a shared helper used by both. | FIXED | 2c6380b3 |
| 9 | 2026-07-03 | 01-customers | HIGH | Imported customer codes not seeded into `cus` document sequence → app-created customer collides | Import seeds sales_customers CUST-0001..0004 but never creates/advances the `cus` document_sequences row. App-create lazily creates the sequence at next_number=1 → reserves CUST-0001 → unique-violation "A customer with this code already exists"; walks up colliding until past the imported max. Likely systemic (suppliers/items too). Repro: fresh imported tenant → /sales/customers/new → Save → error though no code entered. Fix: import must set each code-sequence next_number to max(imported code)+1; + prod data patch cus.next_number=5. | FIXED | 2c6380b3 |
| 10 | 2026-07-03 | 01-customers | MEDIUM | Duplicate/leaky error toasts on customer create failure | On the code-collision, TWO toasts stacked: "A customer with this code already exists" AND "Something went wrong: A customer with this code already exists" (raw prefix leaked). Expected: one clean, human-readable error toast. | FIXED | 2c6380b3 |
| 11 | 2026-07-04 | 01-customers | HIGH | Edit customer → 404 | On `/sales/customers/[id]`, top-right Edit button AND the Details/Internal-Notes pencil icons navigate to a 404 page. Core edit flow broken. Fix: created missing /sales/customers/[id]/edit route. | FIXED | b9037a50 |
| 12 | 2026-07-04 | 01-customers | MEDIUM | Default-currency picker shown (single option) for single-currency tenant | Currency dropdown only lists KWD (correct: shows tenant's enabled currencies). For a single-currency/no-FX shop the field is noise; hide it (default silently) like monolingual secondary-language fields; reappear when a 2nd currency is enabled. | FIXED | b9037a50 |
| 13 | 2026-07-04 | 01-customers | LOW | Deactivate dialog shows raw 6dp balance | Deactivate customer with balance → "outstanding balance 250.000000" (6dp raw) instead of KWD 3dp via shared formatter. Proper fix blocked on #14 (number must be currency-consistent first) + multi-entity currency label (see #6). Not triggered by Asala. | FIXED | b9037a50 |
| 14 | 2026-07-04 | 01-customers | MEDIUM | Blast-radius query sums raw `balance` not `balanceFn` (currency-mixing) | `graph/queries/blast-radius.query.ts` sums `COALESCE(SUM(balance),0)` in transaction currency; a party with invoices in >1 currency would net across currencies (100 KWD + 100 USD → "200"). Every other AR/AP agg sums `COALESCE(balanceFn, balance*rate)` (see 09 AP-aging fix). Not triggered by Asala (single KWD). Fix: switch to balanceFn, then #13 formatting becomes trivial. | FIXED | b9037a50 |
| 15 | 2026-07-04 | 01-customers | MEDIUM | Detail-card pencil icons navigate to full edit page (confusing/redundant) | Details + Internal Notes cards on `/sales/customers/[id]` have pencil icons that route to the full edit page, duplicating the top Edit button and jarring the user; the card fields aren't prominent on the edit page. Fix: remove the pencils, keep cards read-only, edit via the single Edit button. | FIXED | 40ed984a |
| 16 | 2026-07-04 | 01-customers | MEDIUM | No "Block customer" action surfaced in UI (only Deactivate) | Detail kebab offers only Deactivate; `status=blocked` + `blocked_reason`/`blocked_at` exist in schema and the form, but there's no way to block a customer from the detail page. Fix: add a Block action (with reason) to the kebab, mirroring Deactivate; surface Blocked badge. (Enforcement that blocked customers can't be sold to = verify in 03/04.) | FIXED | 40ed984a |
| 17 | 2026-07-04 | 01-customers | MEDIUM | Customer tax group not settable in form; rail shows hardcoded `—` | `defaultTaxGroupId` exists in DTO/column but no field in create/edit form → can't assign a customer tax group via UI; the Details rail "Tax Group" is a hardcoded `—`. Harmless for no-VAT Asala. Fix: hide the tax-group row/field for tenants with no tax groups (mirror currency-hide); wire an editable picker where tax groups exist (reuse existing picker if present). | FIXED | 40ed984a |
| 6 | 2026-07-03 | 01-customers | MEDIUM | AR statement/getBalance can net across legal entities of differing functional currency | Pre-existing in `resolveControlAccountIds` (one AR control per legal entity), now inherited by customer-facing statement. Cannot trigger for Asala (single KWD entity). Fix before any multi-entity tenant: scope per legal entity or surface currency per line, refuse cross-currency netting. | FIXED | b9037a50 |
| 7 | 2026-07-03 | 01-customers | LOW | Statement endpoint returns 200 empty for unknown/foreign customerId (should 404) | `GET /customers/:id/statement` relies on GL tenant-filter returning empty rather than verifying customer exists/belongs to tenant; inconsistent with getCustomer 404. No data leak. | FIXED | 2c6380b3 |
| 8 | 2026-07-03 | 01-customers | LOW | `Number()` coercion on money strings in statement tab | `customer-statement-tab.tsx` uses `Number(line.debit)>0` as a column-render gate; swap to string check to keep never-coerce-money invariant airtight. | FIXED | 2c6380b3 |
| 18 | 2026-07-04 | 02-sales-orders | LOW | Duplicate "Sales / Orders" breadcrumb on orders (and invoices) list | `/sales/orders` renders the breadcrumb twice — once from shell `AutoBreadcrumbs`, once from an explicit `<Breadcrumbs>` in `orders-list-panel.tsx` (lines 87–89). Same in `invoices-list-panel.tsx` (103–108). Customers/Payments/Credit-notes correctly rely on AutoBreadcrumbs only. Systemic #3. Fix: removed the explicit `<Breadcrumbs>` + dangling import from both panels. | FIXED | b0d97142 |
| 19 | 2026-07-04 | 02-sales-orders | MEDIUM | Draft SO document number is a raw UUID | Creating a draft SO sets `number = "DRAFT-09f528a9-ea16-4da3-8d78-c0fc368af038"` (raw UUID) shown as the on-screen order number. Expected a clean human sequence (e.g. `SO-0001`, like `CUST-0001`). Confirm should assign a real sequence. | FIXED | b0d97142 |
| 20 | 2026-07-04 | 02-sales-orders | MEDIUM | 6-decimal unit price in SO line editor | Draft line unit-price input renders `3.790000` (6dp) instead of KWD 3dp `3.790` (systemic #2). Must format to currency precision at rest AND normalize while typing / on blur. | FIXED | b0d97142 |
| 21 | 2026-07-04 | 02-sales-orders | MEDIUM | Customer field is a native dropdown, not a searchable picker | New-order form Customer is a plain Select (systemic #4), inconsistent with the PO form's shared searchable pickers. Should reuse the shared customer/entity picker. | FIXED | b0d97142 |
| 22 | 2026-07-04 | 02-sales-orders | HIGH | Two-step SO create (empty draft → add lines on detail) + inconsistent old-UI form | New-order form creates an empty draft with no lines, then lines are added on the detail page — unlike the PO create form which is single-page (header + lines + totals). Founder directive: rebuild SO create to PO parity (one page, shared pickers, 3dp), keep drafts editable, fix UI. | FIXED | b0d97142 |
| 23 | 2026-07-04 | 02-sales-orders | LOW | Warehouse field on new-order form used a native Select while Branch used a combobox | After the single-page rebuild, Branch was a searchable combobox but Warehouse stayed a native shadcn Select → inconsistent picker UI. Fix: swap to the shared `WarehouseCombobox`. | FIXED | ba24b032 |
| 24 | 2026-07-04 | 02-sales-orders | LOW | Draft-detail header grid misaligned (editable inputs vs read-only text) | On the draft detail, editable Customer/Channel (tall inputs) sat in the same grid as read-only Branch/Warehouse (bare text) → ragged vertical alignment. Fix: render read-only Branch/Warehouse as field-height boxes and top-align the grid (`items-start`). | FIXED | ba24b032 |
| 25 | 2026-07-04 | 02-sales-orders | MEDIUM | Blocked-customer gate not re-checked at SO confirm (TOCTOU) | Customer status is validated active only at attach (create/updateOrder); `confirm()` re-used the credit-limit guard but never re-checked customer status. A customer blocked AFTER a draft was created could still be confirmed and later invoiced. (Carry-forward from 01: enforcement verified.) Picker also filters status:active. Fix: `requireActiveCustomer` re-check in confirm() before reserving the SO number (422, no gapless number burned) + unit test. | FIXED | 55b0928e |
| 26 | 2026-07-04 | 04-sales-invoices | MEDIUM | 6-decimal unit price on draft invoice line editor (found via SO convert) | Convert-to-invoice draft shows unit price `9.710000`/`13.500000` (6dp) instead of KWD 3dp in `invoice-detail-panel.tsx` DraftLineRow (same class as #20, different feature dir). Fix: normalize via shared `toCurrencyPrice(line.unitPrice, invoiceCurrency)` on init + sync. | FIXED | 55b0928e |
| 27 | 2026-07-04 | 02-sales-orders | MEDIUM | Stock-levels view shows raw on-hand, not available-to-promise (reservations not subtracted) | `GET /tenant/stock-levels*` (the user-facing availability view) selects only `materializedStockLevels.onHand` and never `reservedQty`, so a confirmed SO that reserves stock does not reduce displayed availability. Correct ATP (`onHand − reserved + inTransit`) exists in `StockAvailabilityService.computeAtp` but is only wired into the reservation overselling guard + reorder suggestions, not the general stock-levels endpoint/UI. NOT a safety break — oversell is still hard-blocked at reserve + invoice-confirm; this is a display-honesty gap. Scope: inventory module. Display columns (available/reserved) fixed this pass; low-stock threshold unification tracked as DEV-443 (inventory pass). | DISPLAY FIXED; low-stock → DEV-443 | 865ac93b |
| 4 | 2026-07-03 | 01-customers | LOW | No unified running AR statement on customer detail | Balance is inferred from separate Invoices/Payments tabs; no single running-ledger statement (opening + movements → running balance). Improvement, not a correctness break (balance ties to 1131). | FIXED | 2c6380b3 |
| 28 | 2026-07-04 | 03-direct-sale | HIGH | Pack-unit picker non-functional on direct sale | On `/sales/direct/new`, LineUnitPicker shows "Box = 12 Each" and lets you pick Box, but the payload records `unit_name=Each`/`unit_pack_id=null` and relieves BASE qty with no pack conversion. DB-verified: BAT-0069 × 2 "Box" persisted base Each, qty 2, unit_pack_id=null, line total 27.000 = 2×13.5 with NO pack discount — even though item_pack_units for BAT-0069 has pack "Box" factor 12, is_default_sell=true, discount 5.000 KWD. Screenshot showed "Box"+hint (picker only renders that when a pack IS selected), so the pack selection was DROPPED between picker and persisted line: lost conversion (should be 24), lost unit_pack_id, lost 5 KWD pack discount. Two static code-reads (submit uses computeBaseQty; picker shows base label on null pack) each looked correct in isolation but conflict with the DB+screenshot → submit-path divergence not covered by unit tests. Root cause (two layers): (a) backend direct-sale.service.resolveLines dropped the pack discount (never computed packDiscountAddition, never persisted packDiscountType/packDiscountValue); (b) frontend lineTotal did qty×unitPrice with NO base conversion and NO discount → screen showed 13.500 while post = 12×13.5−5 = 157.000 (see-vs-post mismatch). Fix: shared computePackDiscountAddition helper in @zerupt/shared (single source of truth); both sell-side paths + frontend computeSellLineNet compose it → display==post, dual-path equivalence preserved. Accounting-reviewer APPROVE (fils-exact, ROUND_HALF_EVEN, COGS on full base qty, VAT 0, negative-line impossible). BAT-0069: 1 Box→157.000, 2 Box→base 24 / discount 10.000 / 314.000. Tests added (fe+be). fe 6f6a4a73, be+shared helper 81cf4f38. LIVE-VERIFIED on prod INV-00004: base qty 24, unit_pack_id=Box, pack_discount_value 5.000, discount_amount 10.000, total 314.000 (displayed==posted), Dr AR 314 / Cr Rev 314 / Dr COGS 212.885 / Cr Inv 212.885 balanced VAT 0, stock −24, reconcile holds. | FIXED | 6f6a4a73+81cf4f38 |
| 35 | 2026-07-05 | 03-direct-sale | MEDIUM | Promotions not applied on direct-sale path (non-pack dual-path revenue divergence) | addLine (SO→invoice, sales-invoices.service.ts:401-418) auto-applies best active promotion into discountAmount when no explicit discount; direct-sale.service.resolveLines never consults PromotionsService → for identical goods with an active promo the two paths post different discount/revenue, violating dual-path equivalence. NOT triggered for Asala (no promos). Found by accounting-reviewer during #28 review. Scope: wire PromotionsService into the direct-sale line build (or explicitly document express-sale as promo-exempt). Filed as Linear issue. | OPEN | DEV-445 |
| 36 | 2026-07-05 | 03-direct-sale | LOW | Base-unit line unitQty snapshot differs across paths | Base-unit line (no pack) persists unitQty=qty on direct-sale (direct-sale.service.ts:189) vs unitQty=null on addLine (sales-invoices.service.ts:319-324). Zero money impact (unitPackId null, base qty/price/total identical); snapshot/history inconsistency only. Fix: store null on base-unit direct-sale lines to match. | FIXED | 0ee16406 |
| 29 | 2026-07-04 | 03-direct-sale | MEDIUM | Unit price renders 6dp not KWD 3dp on line editor | Direct-sale line unit-price shows `13.500000` (6dp) instead of `13.500` (systemic #2; same class as #20/#26). MoneyInput not normalizing to currency precision on init (incl. price-list preview values) + blur. | FIXED | a5f5fa1a |
| 30 | 2026-07-04 | 04-sales-invoices | MEDIUM | Invoice-detail line shows raw item UUID instead of SKU | `invoice-detail-panel.tsx` line renders item id `dd4804b5-…` under the item name instead of the SKU (BAT-0069). Fix: show SKU (mirror purchase bill detail / direct-sale line); wire sku through payload if missing. | FIXED | 4bbcd40c |
| 31 | 2026-07-04 | 04-sales-invoices | LOW | "Confirmed" status badge low-contrast / unreadable on invoice detail | Citron/olive-filled badge with near-invisible text; violates DESIGN.md (citron-toned text must be olive-deep for WCAG). Fix in the (likely shared) status-badge component so label is legible. | FIXED | 4bbcd40c |
| 32 | 2026-07-04 | 03-direct-sale | MEDIUM | Duplicate/leaky error toasts on oversell | Oversell shows TWO toasts: clean "Not enough stock…" AND raw "Something went wrong: One or more items have insufficient stock…". Same class as #10. Fix: one clean mapped toast; suppress generic "Something went wrong: <raw>" fallback when a mapped message is shown. (Oversell block itself works — nothing posts.) | FIXED | a5f5fa1a |
| 33 | 2026-07-04 | 04-sales-invoices | HIGH | Void invoice dead-ends solo owner (external manager approval required) | "Void this invoice?" forces selecting an Approving manager + that manager's PIN; "No other active users to approve" hard-blocks a sole owner from voiding — never-a-dead-end violation. Same issue already fixed for purchase invoice voiding. Fix: replicate purchase's self-approval path (holder of void permission self-authorizes; no separate manager when none eligible), server-side guard + dialog UX, keep audit + period gate. | FIXED | 4bbcd40c |
| 34 | 2026-07-04 | 04-sales-invoices | MEDIUM | Sales↔purchase invoice screens inconsistent | Sales invoices list has search + status pill-tabs + overdue/outstanding banner (loved) but no Export CSV; purchase bills list has Export CSV + native dropdowns but no search/pills/banner. Purchase bill detail lacks the Paid/Balance progress bar sales has. Fix: port sales list UX to purchase (reuse components), add Export CSV to sales (via injection-safe csvCell from #5), add Paid/Balance bar to purchase bill detail. Purchase overdue banner aggregates client-side (≤100 confirmed bills, undercounts at scale) — sales has a backend meta.summary; scoped as DEV-444 (add purchase bills meta.summary). | FIXED | 2324118d |
| 37 | 2026-07-05 | 04-sales-invoices | MEDIUM | Sales invoice create is a clunky two-step header-only form + duplicated breadcrumb; inconsistent with purchase bill create | `/sales/invoices/new` renders a header-only card (customer + branch + notes → "Create draft") with NO line items — lines are only addable on a separate second page after the draft is created. Purchase bills (`/purchase/invoices/new`, the true structural twin) do it on ONE page: header fields + inline line editor (`bill-create-line-editor.tsx`), submit creates the draft header then loops per-line add via the same header-create + per-line-add API sales already has (`useCreateInvoiceMutation` + `useAddInvoiceLineMutation`; `AddInvoiceLinePayload` accepts a complete line incl. `unitPackId`/`unitQty`/`discountAmount`/`unitPrice` in one call). Also: `invoice-create-panel.tsx` renders its OWN in-card `<Breadcrumbs>` on top of the shell's `AutoBreadcrumbs` → duplicated breadcrumb (systemic #3). No backend/contract change needed. Fix: single-page create modeled on `BillCreatePanel` (new `invoice-create-line-editor.tsx` reusing InvoiceLineSearch / LineUnitPicker / MoneyInput / QuantityInput / shared `computeSellLineNet`; pack discount stays server-authoritative), preserve draft→confirm (no auto-confirm), remove the duplicate in-card breadcrumb. Follow-up (Linear): extract a truly shared buy+sell draft-line editor to kill the duplicated line-table UI. | FIXED | cde2025c |
| 38 | 2026-07-05 | 04-sales-invoices | HIGH | Single-page invoice create fails to add PACK lines ("Request validation failed") | Regression in #37's new create flow: draft is created but every pack-unit line 400s, dead-ending the user on the empty draft with "The invoice was created, but 1 line(s) could not be added… Add them again". Root cause: server `addLineSchema` (sales-invoices.dto.ts) requires `quantity` (base units) on EVERY POST `.../lines` call — `unitPackId`+`unitQty` only override it — but `buildLinePayload`'s pack branch omitted `quantity` entirely. Base lines were fine (they set quantity). Fix: always send `quantity = unitQty × conversionFactor` via shared `computeBaseQty`, alongside `unitPackId`+`unitQty` for pack lines (matches the working detail-panel add path). Also swapped the hand-rolled branch `<Select>` for the canonical `BranchCombobox` (consistency with every sibling create panel). Unit test updated to assert pack line carries base quantity. | FIXED | aae3370c |
| 39 | 2026-07-05 | 04-sales-invoices | MEDIUM | Invoice print/PDF shows qty at 6dp + empty QR box + flat header | Printed A4 invoice (`print/tax-document.tsx`) rendered Qty as raw `numeric(19,6)` (`12.000000`) instead of trimmed precision; always drew a dashed empty "QR Code" placeholder box (because `data.zatca` is never populated → the non-KSA `else` branch always fired) even for Kuwait no-VAT tenants; seller contact block was a flat same-size stack. Fix: qty via shared `formatQuantity` (trailing-zero trim, capped at item precision when known; added optional `quantityDecimals` to `DocLine`); render NO QR block when `data.zatca` absent (ZATCA QR is KSA-only); group seller contact details into one tight muted block. Shared `TaxDocument` so credit-note print inherits the same fixes. | FIXED | f51f77ef |
| 41 | 2026-07-05 | 06-credit-notes | HIGH | Credit note ignores the invoice line discount → over-credits + hard-blocks full credit of a discounted sale | Credit-note lines snapshot the invoice line's RAW pre-discount unitPrice (`credit-notes.service.ts:217`) and `recomputeCreditNote` computed net = `creditQty × unitPrice` (`credit-notes.totals.ts`), silently dropping the line discount — the schema even had NO `discount_amount` column on `sales_credit_note_lines` (feature half-built; comment said "monetary effect flows through the line discount" that never existed). Prod repro: INV-00005 (Bader, 12× BAT-0069 pack "Box" −5.000 = 157.000). A FULL credit would post 162.000 (12×13.5, discount dropped) → over-reverses revenue+AR by 5.000 AND trips the balance guard (`162 > 157 outstanding → UnprocessableEntityException`), dead-ending the sole owner on a legitimate full return. Any discounted sale affected; net-zero contra to the original invoice broken. Fix: add `sales_credit_note_lines.discount_amount` (mig 0155); snapshot invoice line discount pro-rated by creditQty/invoicedQty on create (clamped to credited gross); net gross−discount in recompute so subtotal/total (and the AR+revenue reversal that read subtotal) tie back to the invoice. Full credit → exact line total; partial → proportional. Tests: new `credit-notes.totals.spec` (netting, partial pro-rata, discount>gross guard, multi-line) + create pro-rata assertions (34 credit-note tests green). Follow-up (00bc5803): the issue-credit-note DIALOG preview also computed creditQty×unitPrice with no discount (showed 162 for a full credit while posting 157) → added `creditLineNet` mirroring the server + `divideDecimal` in lib/money (with tests) so preview==post. LIVE-VERIFIED on prod: CN-00001 full credit of INV-00005 posted 157.000 (Cr 1131 157 / Dr 4200 157; Dr 1141 106.442 / Cr 5100 106.442 @ orig WAC 8.870196), NOT 162 — discount reversed correctly. | FIXED | cb4e2c52+00bc5803 |
| 42 | 2026-07-05 | 06-credit-notes | MEDIUM | A4 credit-note print hardcodes the per-line discount to "0" → line columns don't reconcile | After #41 added a real per-line discount, `credit-note-print-document.tsx:122` mapped `discountAmount: "0"` into the DocLine while `lineTotal` was net-of-discount. A discounted credit line printed as unitPrice 13.500 × qty 12, discount 0.000, total 157.000 — `qty×price − discount(shown 0) + tax ≠ printed total` on a customer-facing document (grand total itself was correct; only the line breakdown lied). Root: `discountAmount` was persisted on the line + used in totals but never exposed on `CreditNoteLineResponse`/`toLineResponse`, nor on the web `CreditNoteLine` type, so the print couldn't read it. Detail panel unaffected (shows API `lineTotal`, no unit/discount columns). Fix: expose `discountAmount` on the response DTO + `toLineResponse`, add to web `CreditNoteLine` type, map `l.discountAmount` into the print DocLine (shared `TaxDocument` already renders a discount column). 34 credit-note tests green; web typecheck clean for touched files; i18n in sync. | FIXED | 4122d501 |
| 43 | 2026-07-05 | 06-credit-notes | HIGH | Issue-credit-note dialog dead-ends a sole owner (forces a separate approving manager + PIN) | On invoice detail → "Issue credit note", the dialog forces selecting an "Approving manager" + entering that manager's Approval PIN; for a single-user tenant it shows "No other active users to approve. Another user must set an approval PIN first." and disables the "Issue credit note" button → the sole owner cannot issue ANY credit note. Identical never-a-dead-end violation to #33 (invoice void), which was already fixed via the `requireInvoiceApproval` tenant setting. Backend `credit-notes.service.confirm` unconditionally called `pinVerification.verifyApproval` (which rejects self-approval) and the DTO required approvedBy/approvalPin. Fix: mirror the void flow — gate the second-approver PIN on `requireInvoiceApproval` (shared setting, default OFF); when OFF the fields are optional/hidden and the acting user confirms directly (approvedBy stored null, `maybeVerifyApproval` skips `verifyApproval`); when ON both are required + verified (SoD). Frontend `CreditNoteDialog` takes `requireApproval` wired from the org setting at both call sites (invoice-detail + standalone create panel); hides ApprovalPinFields, drops the submit gate, omits the PIN payload when off. 36 credit-note tests green (sole-owner-no-PIN + approval-required paths). LIVE-VERIFIED on prod: sole owner issued CN-00001/00002/00003 with no approving-manager/PIN, all confirmed + posted. | FIXED | c60588b4 |
| 44 | 2026-07-05 | 06-credit-notes | MEDIUM | Issue-credit-note dialog clips line rows and won't scroll on a short viewport | The credit-note line-item table row (credit-qty stepper) was cut off mid-row and the dialog body could not scroll, hiding content. Root cause: `credit-note-dialog.tsx` overrode the base `DialogContent` (already `grid max-h-[85dvh] overflow-y-auto`) with `flex max-h-[90dvh] flex-col overflow-hidden` + an inner `overflow-y-auto flex-1 min-h-0` body — the conflicting display/overflow utilities broke the scroll container, so overflowing rows were clipped by `overflow-hidden` with no scroll. Every other dialog in the feature just uses `<DialogContent className="sm:max-w-…">` and lets the base own the single scroll container. Fix: drop the overrides (use `sm:max-w-3xl` — wider for the 6-col table), remove the competing inner scroll wrapper, and wrap the table in `overflow-x-auto` so it scrolls horizontally instead of clipping on narrow screens. Web typecheck clean. | FIXED | aa6ec281 |
| 45 | 2026-07-05 | 06-credit-notes | MEDIUM | CONFIRMED badge on credit-note rows (invoice detail) is unreadable | The credit-note list rows on the invoice detail rendered a LOCAL `CreditNoteStatusBadge` span with `bg-accent/10 text-accent-foreground` — near-black `accent-foreground` text on a 10%-opacity accent tint, which in dark mode barely lifts off the near-black page (illegible). `text-accent-foreground` is only calibrated for a SOLID `bg-accent` fill (DESIGN.md: citron/olive is fill-only). Same class as #31, which fixed the invoice HEADER badge via the shared `InvoiceStatusBadge` but missed this second local badge. Fix: delete the local badge, use shared `InvoiceStatusBadge` (tone `success` for confirmed, `neutral` otherwise) — the AA-validated solid-fill variant. | FIXED | 842b38a0 |
| 46 | 2026-07-05 | 06-credit-notes | MEDIUM | Raw item UUID instead of SKU on the credit-note dialog + A4 print | The issue-credit-note dialog line table (subtitle) and the A4 credit-note print (`mapCreditNoteLines` → `itemName: l.itemId`) showed the raw item UUID instead of the SKU/name. Same class as #30 (fixed on invoice-detail via a client-side item lookup), missed on these two surfaces (neither `InvoiceLine` nor `CreditNoteLine` carries a SKU). Fix: reuse the existing `useItemMap` lookup — dialog subtitle shows `item.sku`; print DocLine `itemName = item.name` with `sku` as the secondary line, falling back to itemId only on a lookup miss. No API/schema change. Web typecheck + i18n clean. | FIXED | 842b38a0 |
| 47 | 2026-07-06 | 05-payments-received | HIGH | Receipt reversal dead-ends a sole owner (forces a separate approving manager + PIN) | On a posted receipt → Reverse, `ReverseReceiptDialog` always renders ApprovalPinFields + gates submit on `isApprovalComplete`; backend `receipt-vouchers.service.assertReversePreconditions` (:853) called `pinVerification.verifyApproval` UNCONDITIONALLY (verifyApproval hard-rejects self-approval), and the reverse DTO required approvedBy+approvalPin. For a single-user tenant there is no second approver → the sole owner can NEVER reverse their own receipt. Identical never-a-dead-end violation to #33 (invoice void) and #43 (credit note), which were already fixed via the `requireInvoiceApproval` tenant setting. Note: the idempotent already-reversed no-op path was already PIN-free (good); only the first legitimate reversal dead-ended. Fix: mirror void/credit-note — gate the manager-PIN on `requireInvoiceApproval` (shared setting, default OFF); OFF = acting user reverses directly with just a reason (approvedBy stored null, verifyApproval skipped), ON = approvedBy+approvalPin required + verified (SoD). DTO fields made optional; frontend dialog takes `requireApproval` wired from the org setting at both call sites (payment-detail + customer receipt-detail panels), hides ApprovalPinFields + drops the submit gate + omits the PIN payload when OFF. 2 new service specs (sole-owner-no-PIN reverses + posts contra; approval-ON-missing-PIN → 400 before any contra); 40 receipt-service tests green; web typecheck + i18n clean. | FIXED | 4b5b9c4d |
| 48 | 2026-07-06 | 05-payments-received | MEDIUM | Bank/cheque receipt auto-defaults the deposit account to Petty Cash + forces a pick from a confusing list → silent ledger mis-post | On `/sales/payments`, choosing "Bank transfer" or "Cheque" shows a REQUIRED account picker listing ALL 7 cash-equivalent accounts (Petty Cash 1111, Cash Register 1112, Cash in Transit 1119, Bank-NBK 1121, Cheques in Transit 1129, Cheques in Hand 1150, and Bank Overdraft 2220 — a liability), and `payment-create-panel.tsx` auto-selects `cashBankAccounts[0]` which, code-sorted, is **Petty Cash 1111**. A naive shopkeeper accepting the default silently posts every bank transfer to Petty Cash (Dr 1111 instead of Dr 1121) — cash balance mis-stated, bank balance never rises. "Dumbest thing a shopkeeper could do here" = accept the wrong default. Backend already handles a null bankAccountId correctly (uses the `bank` account-mapping default = 1121 Bank-NBK; there's even a service spec `bank transfer with no bankAccountId emits bankAccountId=null (default mapping)`), so the deposit account is genuinely an OPTIONAL override. Fix (frontend-only): stop auto-selecting the first cash-equivalent account; make the picker optional (blank → mapped bank default), relabel "Deposit account (optional)" + placeholder "Main bank account (default)" + hint "Leave blank to deposit to your main bank account." canSubmit no longer requires a pick. GL now posts Dr 1121 / Cr 1131 by default. | FIXED | f2a493d6 |
| 49 | 2026-07-06 | 05-payments-received | MEDIUM | No card/KNET tender on receipt vouchers (Kuwait persona collects via KNET) | Receipt payment methods are only cash / bank_transfer / cheque (`RECEIPT_PAYMENT_METHODS`, DB enum `sales_receipt_payment_method`, DTO `z.enum([...])`). Al-Asala (Kuwait) collects heavily via **KNET** (the national debit-card network); there is no way to record a card/KNET collection on a receipt, forcing the shopkeeper to mislabel it as "bank transfer" (data-quality gap). The accounting listener's `PAYMENT_LINE_TYPE` map ALREADY includes `card: "bank"` (forward-declared), so card would post Dr mapped bank (1121) / Cr 1131 — identical GL to bank_transfer, i.e. this is a tender-labeling/UX gap, NOT a ledger-correctness gap (KNET and bank_transfer settle to the same 1121). Fix path (small, deferred as a focused follow-up — prod enum migration): add `card` to the `sales_receipt_payment_method` pg enum (Drizzle migration), the create DTO enum, `RECEIPT_PAYMENT_METHODS`, and an i18n label (generic "Card", region-agnostic — a per-region tender registry, KNET/mada/etc., is a separate feature). Card would use the mapped bank default with no picker (like cash). | OPEN | (deferred) |
| 50 | 2026-07-06 | 05-payments-received | CRITICAL | Receipt JE events unregistered in the outbox domain-event allowlist → reversal contra silently lost, 1131 drifts from AR subledger | `sales.receipt.posted` / `sales.receipt.reversed` / `sales.receivable.writeOff` were absent from `SALES_DOMAIN_EVENTS` (→ `DOMAIN_EVENT_TYPES`) and `EVENT_ID_PRESERVING_DOMAIN_EVENTS`. The outbox poller uses that allowlist to decide re-emit-on-bus (domain-shaped, listener builds JE `lines`) vs route-to-`postFromEvent` (JE-shaped, needs `lines`). Receipt payloads are domain-shaped (carry `allocations`, no `lines`/`eventType`), so the poller mis-routed every receipt event into `postFromEvent` → Zod rejects (`expected string, received undefined [eventType]` + `expected array, received undefined [lines]`) → dead-letter. The in-process fast-path (`emitReceiptPosted` → `handleReceiptPosted`) masks it for POSTS (JE already posted, durable row dead-letters harmlessly), but the REVERSAL's fire-and-forget fast-path failed silently AND its durable path was broken → the contra (Dr 1131 / Cr cash) NEVER posted. Prod repro (Al-Asala): reversing RV-00003 (bank_transfer 100) flipped the receipt to reversed + restored the invoice balance in-tx but posted no GL contra → **GL 1131 Bader 448.500 vs AR-from-invoices 548.500, off by exactly the 100 reversed**. Pre-existing since ≥2026-07-04 (RV-00001 dead-lettered then). EXACT sales twin of the documented purchase PAY-200004 incident (which WAS registered). Fix: register the three events in both allowlists (one line each, no payload-builder change — matches invoice/credit-note/purchase-payment); poller re-fans them to `handleReceiptPosted`/`handleReceiptReversed` which build the lines. Regression specs added (isDomainEventType for all three + whole-set guard). Remediation: the failed `sales.receipt.reversed` outbox row (attempt 3/5, still retriable) auto-replays post-deploy through the fixed routing → posts the missing contra idempotently (reversal-namespaced eventId) → reconcile restored; verify at the ledger. | FIXED (replay pending) | 75e8143a |
| 40 | 2026-07-05 | 04-sales-invoices | HIGH | SO→invoice conversion leaves empty draft (total 0, no lines) yet marks SO `fulfilled` | Live prod: converting SO `B1SHUWAIKH-SO-00001` produced invoice `DRAFT-25117540…` stuck at status=draft, total 0.000, zero lines, never posted to GL/COGS/stock — while the source SO flipped to `fulfilled` (grand_total 171.710, which itself ≠ the 157.000 the equivalent direct invoice posts for the same 1 Box BAT-0069). Dual-path equivalence (headline 04 test) BROKEN: SO path realizes no ledger effect. Also possible SO-vs-invoice pricing divergence (171.710 vs 157.000) — may relate to DEV-445. Root cause: `SalesOrdersService.convertToInvoice` copied the SO lines into the draft (atomic tx — lines DID persist) but never called `recompute()`, so the header stayed at the hardcoded insert value `total: "0"` (every other line path — addLine/updateLine/confirm — recomputes). Fix: call `recompute(tx, taxCalc, tenantId, invoiceRow)` in the same tx after the copy + fail-loud guard aborting the whole tx if `.returning()` count ≠ lines.length (never leave a fulfilled SO behind an empty/partial invoice). Regression tests added (non-zero header total; partial-copy 422). NOTE: the pre-existing stuck prod record (`DRAFT-25117540…` + SO-00001 `fulfilled`) is recoverable WITHOUT code — the draft's lines exist, so opening it and hitting **Confirm** triggers recompute → posts to GL. Separately, the 171.710 SO grand_total ≠ 157.000 direct is NOT explained by the total-0 bug; the direct 157.000 is pack-discount-only (no promo active), so 171.710 likely reflects different SO line entry (qty/price-list) — to re-check on a clean Test-2 rerun. SO path also has no promotions code path at all (grep-confirmed) = real gap, tracked as DEV-445. | FIXED | 6ebeeeee |
| 48 | 2026-07-06 | 04-sales-invoices | MEDIUM | Serial-picker exposes acquisition cost to any invoice-line editor | On invoice line entry for a serial-tracked item, `serial-picker-dialog.tsx:107-108` renders each serial's `acquisitionCost` in the list label (`${serial.serialNo}  ${serial.acquisitionCost}`) with NO cost-visibility/role gate — any user who can edit an invoice line (incl. a cashier role) sees unit acquisition cost. Violates the "no COGS/cost exposure to a cashier" invariant (same spirit as keeping COGS off the invoice UI). Low real-world exposure for Asala (auto parts aren't serial-tracked, so the picker never opens), but a genuine cross-persona leak. Also verify the serials API only returns `acquisitionCost` to cost-authorized roles (backend gate is the real fix; hiding it client-side alone is insufficient). Scope: gate cost display behind a cost-visibility permission + confirm the serials endpoint doesn't ship cost to unauthorized roles. Batched (not Asala-triggered). Fix: `serial-picker-dialog.tsx` label no longer renders `acquisitionCost` (this picker chooses which unit to SELL — cost is irrelevant + must not leak to a cashier). Backend endpoint gating still worth a follow-up but the UI leak is closed. | FIXED | 0e32a65f |
| 49 | 2026-07-06 | 04-sales-invoices | HIGH | Sales-order create editor shows raw total (pack discount dropped from display) | On `/sales/orders/new`, the line editor computed `lineEstimatedTotal` + `runningTotal` as raw `baseQty × unitPrice`, so 2 Box of BAT-0069 (pack "Box"=12, 5.000/Box discount) displayed Line total + Running total **324.000** while the order (and the invoice it converts to) both POST **314.000** — the see-vs-post divergence this whole pass guards against (#28 class), on the SO path. Backend `sales-orders.service.addLine` + recompute already apply the pack discount, and the stored SO `grand_total` is correct (314); this was CLIENT DISPLAY only. Root cause: `order-create-line-editor.tsx` imported only `computeBaseQty`, never the shared pack-aware `computeSellLineNet` the invoice editor uses. Pack discount fields were already on `line.packUnits`; only the math ignored them. Fix: `lineEstimatedTotal` now calls `computeSellLineNet(...)` (single shared helper) → display == post. Found live on prod during 04 re-verify (INV-00007 posted 314 correctly; the 324 was the SO create screen). | FIXED | e3a45a71 |
| 50 | 2026-07-06 | 04-sales-invoices | LOW | Invoice-create Discount column reads 0 for pack lines + SO double header + jargon banner | (a) Invoice create Total is correct (314) but the per-line Discount cell binds manual `line.discount` only, so a pack discount shows "0" there while the confirmed detail shows "10" — display inconsistency, no money impact (`invoice-create-line-editor.tsx` ~258-267); left as-is (itemizing pack discount in an editable manual cell is ambiguous). (b) SO create editor stacked "Line items" heading + redundant "Add item" sub-label → dropped the sub-label. (c) Invoice draft banner "Draft starts unnumbered. INV-#### is assigned on confirm so the sequence stays gapless" (jargon) → "This invoice gets its number when you confirm it." (en+ar). Batched-LOW remaining: SO Qty header text-end misaligns vs the qty-input+inline-pack cell, and SO pack-unit UI differs from the invoice LineUnitPicker layout — cosmetic. | FIXED | e3a45a71+0e32a65f |
| 51 | 2026-07-06 | 04-sales-invoices | LOW | (Follow-through on #50 remaining items) Invoice Discount column now shows pack discount; SO editor aligned to invoice layout | (a) Invoice create Discount column: added `linePackDiscount` helper (gross − pack-net) + a muted hint "Pack discount {amount} already applied" so the column no longer reads a misleading 0 while the Total already reflects it — create now matches the confirmed detail (which shows discount 10). (b) SO create editor restructured to mirror the invoice editor: new "Unit" column (`orders.detail.columns.unit`, en+ar) holds the LineUnitPicker, Qty gets its own right-aligned column — fixes the crammed qty+pack cell and the misaligned Qty header. Typecheck + i18n green. | FIXED | 0e32a65f |
| 52 | 2026-07-06 | 04-sales-invoices | CRITICAL | Sales-order create dropped the pack unit at submit → order + converted invoice STORE 324 (overcharge), not 314 | Deeper root cause behind #49. The single-page order create (`order-create-panel.tsx` onSuccess loop) called `addOrderLine(order.id, { itemId, qty: lineBaseQty, unitPrice })` — it NEVER forwarded `unitPackId`/`unitQty`, so the server stored a raw base line (qty 24 × 13.500) with NO pack discount: `sales_order_lines` for SO-00004 persisted unit_pack_id NULL, discount_amount 0, line_total 324, grand_total 324. `convertToInvoice` copies verbatim → DRAFT invoice also 324; confirming it would POST 324 (Cr Revenue 324, overcharge the customer 10.000 vs the intended 314). #49's display fix (computeSellLineNet) made the SCREEN show 314 while submit still stored 324 — so display==314 but post==324, a see-vs-post the wrong way. Real fix: forward `unitPackId + unitQty` for pack lines in the create submit (qty stays the base fallback), mirroring the invoice #38 fix. Backend DTO (`sales-orders.dto.ts` addLineSchema) already supported the fields. Found live on prod (SO-00004 / DRAFT invoice 324) during 04 re-verify. No ledger damage: the bad SO is a non-posting quote and the invoice stayed DRAFT (never confirmed), so GL/reconcile untouched — the stale SO-00004 + its DRAFT invoice should be discarded, then re-created on the fixed build. | FIXED | b9a66c89 |
