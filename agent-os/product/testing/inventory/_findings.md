# Inventory — Testing Findings Log

> Running log of issues found during live testing. One row per finding. Update status as fixed.
> Severity rubric: see [`../README.md`](../README.md). Fix CRITICAL/HIGH immediately; batch MEDIUM/LOW for review.

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| - | 2026-06-29 | (baseline) | PASS | Ledger-first invariant holds: on_hand = Σ stock_ledger_entries per item/location (149/149 pairs, 0 mismatch) | DB recon | on_hand ties to ledger | VERIFIED PASS |
| - | 2026-06-29 | (baseline) | PASS | Valuation ties: Σ total_value = Σ(on_hand×average_cost) = inventory control acct 1141 GL balance (all KWD, 3dp, no negatives) | DB recon | valuation = GL control | VERIFIED PASS |
| 1 | 2026-06-29 | 01 Items/Catalog | MEDIUM | Items list has NO page header/title; screen jumps straight to the toolbar | Open `/inventory/items` | A page header ("Items" + subtitle) like other modules | FIXED (committed 2e8b0e53/88b803e4) — header added to list + form; re-verify live per playbook 01 |
| 2 | 2026-06-29 | 01 Items/Catalog | MEDIUM | Cost/Selling Price render at 2dp ("8.53") not the tenant currency precision (KWD = 3dp "8.530") | Items list | Dynamic precision via shared currency-precision util | FIXED (committed) — items-table reuses getCurrencyDecimals + useTenantCurrency |
| 3 | 2026-06-29 | 01 Items/Catalog | HIGH | New/Edit item form UI is raw: no field grouping, inputs invisible on light+dark, no required/optional markers, no guidance, action buttons scroll away | Open `/inventory/items/new` | Card-sectioned guided form, required/optional markers, hints, sticky actions, visible inputs | FIXED (committed) — full redesign of shared ItemFormPanel; re-verify live |
| 4 | 2026-06-29 | 01 Items/Catalog | MEDIUM | Create flow forces draft→activate two-step (default isActive=false + two co-equal buttons); confusing for everyday 1-5 location retailers | Open New item | Default new items ACTIVE (one primary "Add item"); "Save as draft" stays as a quiet secondary. isActive is a visibility flag only (gates POS catalog), not a data guard; DB+DTO already default true | FIXED (committed) |
| 5 | 2026-06-29 | 01 Items/Catalog | LOW/UX | No total item count on list (harder to gauge catalog size) | Items list | "N items" total footer | FIXED (committed) — reuses meta.total, en+ar |
| 6 | 2026-06-29 | 01 Items/Catalog | NOTE | Import data: all 150 items missing Arabic name (name_alt) and brand | DB recon | Not a screen bug; import-completeness gap. Form supports both fields; consider import enrichment | LOGGED (data observation) |
| 7 | 2026-06-29 | 01 Items/Catalog | CRITICAL | EVERY imported item is uneditable: editing cost/price/brand throws "unit of measure cannot be changed" + mislabels SKU as "already in use". Import stored unit "Each"; update normalizes to "each" and compares vs raw "Each" → false unit-changed → 409 immutability guard | Edit any imported item, change cost, Save | Re-sending an unchanged unit must not trip the guard | FIXED (2e5a0b41) — compare normalized-to-normalized; only write unit when actually changed |
| 8 | 2026-06-29 | 01 Items/Catalog | CRITICAL | New-item create 400 "Request validation failed: description expected string, received null" — form clears empty optional fields to null but create DTO rejected null on description (and nameAlt) | Add item with name+SP+unit, Save | Create accepts null for cleared optional fields | FIXED (2e5a0b41) — description+nameAlt .nullable() on create schema |
| 9 | 2026-06-29 | 01 Items/Catalog | HIGH | On edit, optional Category/Tax Group fail save with "Invalid UUID" (empty string sent for unselected picker); Category also not persisting the item's existing value | Edit item, Save | Empty optional picker = null, no error | FIXED (2e5a0b41) — coerce ""→null at load; show "None" for empty category |
| 10 | 2026-06-29 | 01 Items/Catalog | HIGH | Clicking Save with validation errors gives NO toast/feedback; errors only visible after manual scroll up | Submit invalid form | Toast + auto-scroll to first error | FIXED (2e5a0b41) — onInvalid toast + scrollIntoView |
| 11 | 2026-06-29 | 01 Items/Catalog | MEDIUM | Edit form prices render at raw 6dp ("27.190000") not tenant currency precision (KWD 3dp) | Edit any item | 3dp via shared currency-precision util | FIXED (2e5a0b41) — formatToDecimals on load |
| 12 | 2026-06-29 | 01 Items/Catalog | LOW | SKU help marker is a "(?)" text glyph, inconsistent with the Info "i" icon used everywhere else | Edit item, SKU field | Info icon | FIXED (2e5a0b41) |
| 13 | 2026-06-29 | 01 Items/Catalog | LOW | "150 items" total count is small/floating at bottom-left of list; should be a clearer count display | Items list footer | Prominent, properly placed count | FIXED (4ccab4ab) — count pill moved into page header |
| 14 | 2026-06-29 | 03 Barcodes | HIGH | Barcode Type (EAN-13/UPC-A/etc) was NOT validated against the value — a 5-digit value could be saved as "EAN-13" and then fail at the scanner | Add barcode with mismatched type | Reject invalid format client + server | FIXED (4ccab4ab) — shared validateBarcodeFormat (check digits); inline client error + server refine |
| 15 | 2026-06-29 | 01 Items/Catalog | MEDIUM | Item form showed every Details field (Weight, OEM/Part Number) for all industries regardless of relevance, unlike the import template which is industry-adaptive | Open form as auto-parts tenant | Industry-adaptive field visibility, single source of truth | FIXED (4ccab4ab) — shared resolveItemFormFieldVisibility; import config now consumes same sets; hides Weight for auto-parts, keeps fields that already have data |
| 17 | 2026-06-30 | 01 Items/Catalog | HIGH | Edit item: Category shows "None" + "Invalid UUID" on save though list shows a real category (DB clean: valid UUID, active cat). Client schema enforced .uuid() on dropdown-sourced category/taxGroup; also an item whose category was deactivated would show None and silently clear it on save | Edit BAT-0069, Save | Category preselects + never dropped; no client uuid error | FIXED (0cdf7855) — drop client .uuid() (server authoritative); always inject loaded category as option; coerce ""->null in payload |
| 18 | 2026-06-30 | 02 Categories | HIGH | Edit/Delete actions are hover-only (`opacity-0 group-hover:opacity-100`) — unreachable on touch/tablet, the primary way MENA retail staff use this. Undiscoverable even on desktop | Open `/inventory/categories` on a tablet (or without hovering) | Actions always visible + keyboard-focusable | FIXED (a7c0fe99) — removed hover gating in category-tree-node |
| 19 | 2026-06-30 | 02 Categories | MEDIUM | No item count anywhere on the category tree — storekeeper cannot see how many items a category holds, and only the child-category count badge exists. Also means delete is hard-blocked via 409 with no proactive scope warning | Open categories list | Per-category item count badge + count shown in delete dialog before confirm | FIXED (a7c0fe99) — API grouped count → itemCount badge + delete-dialog reassign flow (move to category / uncategorize); + drill-through, summary header, inactive chip, search filter, 29 API tests |
| - | 2026-06-30 | 02 Categories | PASS | Category names render as full human labels (Batteries/Oils & Fluids); Oils & Fluids correctly shows no Arabic name (clean, no broken label); other 7 show Arabic secondary names | Live screenshot | clean bilingual render | VERIFIED PASS |
| - | 2026-06-30 | 04 Pack Units | PASS | Conversion-integrity invariants verified at code/DB level: item_pack_units.conversion_factor numeric(19,6) + DB CHECK >0; stock_ledger_entries.quantity numeric(19,6) (fractional-safe); unique (tenant,item,name) + ConflictException; toBaseQuantity uses decimal.js (qty×factor, no float drift) | code/DB recon | wrong factor cannot silently corrupt stock | VERIFIED PASS |
| 20 | 2026-06-30 | 04 Pack Units | MEDIUM | Optional/advanced item sections (Pack Units, Barcodes) render their FULL add-form always-expanded on every item detail — a wall of machinery (carton factors, discount type/value, default-sell/purchase) on all 150 items for a 1-shop trader who sells everything by the piece | Open any item, scroll to Pack Units/Barcodes card | Collapsed-by-default when empty, expanded when populated; header count badge + plain-language one-liner | FIXED (e094a524) — new CollapsibleSectionCard; collapsed when count=0 |
| 21 | 2026-06-30 | 04 Pack Units | MEDIUM | Typing 0 in Units-per-pack fires an ugly NATIVE browser validation bubble ("Value must be greater than or equal to 0.000001") — unbranded tech jargon, hijacks submit before our own validator | Add pack unit, factor 0, submit | Our friendly inline "must be greater than 0" message | FIXED (e094a524) — removed native min= attr; rely on client validator |
| 22 | 2026-06-30 | 04 Pack Units | LOW | Pack-unit add-form had no required/optional markers (inconsistent with the main item form which marks every field) | Open pack-unit form | Asterisks on Name/Units-per-pack/Discount-value; (Optional) on alt-name/discount-type | FIXED (e094a524) — shared RequiredMark/OptionalTag |
| 23 | 2026-06-30 | 04 Pack Units | LOW | Alt-name (Arabic) input missing dir="auto" — RTL text renders LTR inside the box | Type Arabic pack name | dir="auto" correct directionality | FIXED (e094a524) |
| 24 | 2026-06-30 | 04 Pack Units | LOW | Discount type/value pair consistency validated client-side only; a direct API POST with discountType but no discountValue would persist null value (not user-facing, backend hardening) | Direct POST /:id/pack-units | Zod createPackUnitSchema enforces the pair | OPEN (batch — backend) |
| 25 | 2026-06-30 | 02 Categories | HIGH | Tenant-isolation gap: category DELETE statement scoped only by `eq(id)`, not `and(eq(id), eq(tenantId))` like every other mutation in the service. Guarded in practice by the prior tenant-checked lookup, but a guessed/leaked id is one refactor away from deleting another tenant's category | Code recon (found by Wave-4 test author) | DELETE guards id AND tenantId (defense-in-depth) | FIXED (a7c0fe99) — and(eq(id),eq(tenantId)) on tx.delete |
| 16 | 2026-06-29 | 01 Items/Catalog | MEDIUM | Tax Group field shown to non-VAT tenants (e.g. Kuwait) who only have a seeded "No Tax" group — pointless field | Open form as KW tenant | Hide Tax Group when tenant has no real VAT (data-driven, default still posts) | FIXED (4ccab4ab) — isNoTax flag on tax-groups response; field hidden when all groups no-tax |
| 26 | 2026-06-30 | 05 Warehouses/Locations | CRITICAL | Tenant-isolation gap (bug #25 repeated): warehouse/zone/bin DELETE statements scoped by `eq(id)` only, not `and(eq(id),eq(tenantId))`. Guarded in practice by the prior tenant-checked lookup but a guessed/leaked uuid + valid JWT is one refactor from deleting another tenant's location | Code recon | DELETE guards id AND tenantId on all three services | FIXED (31936ecd) — and(eq(id),eq(tenantId)) on bins/zones/warehouses delete |
| 27 | 2026-06-30 | 05 Warehouses/Locations | HIGH | `deleteBin` had NO guards: a bin referenced by `stock_ledger_entries` (binId FK is onDelete:restrict) throws an unhandled Postgres 500, not a clean message | Delete a bin used in a movement | Block with friendly ConflictException ("has stock history, deactivate instead") | FIXED (31936ecd) — proactive ledger count check before delete |
| 28 | 2026-06-30 | 05 Warehouses/Locations | MEDIUM | `deleteWarehouse` checked default + zones but NOT stock_ledger history; a warehouse with movement history but no zones would 500 on the warehouseId FK (onDelete:restrict) | Delete warehouse with stock history, no zones | Block with friendly ConflictException | FIXED (31936ecd) — ledger count guard added |
| 29 | 2026-06-30 | 05 Warehouses/Locations | MEDIUM | Deactivating the SOLE default warehouse was not blocked — a branch could silently end up with no active default | Edit default warehouse, set inactive | Block: "assign a different default first" (mirrors delete guard) | FIXED (31936ecd) — sole-default deactivation guard in updateWarehouse tx |
| 30 | 2026-06-30 | 05 Warehouses/Locations | LOW | Zone/bin empty states are bare text ("No zones in this warehouse.") with no "optional / you don't need this" framing for a 1-warehouse shop; sub-sections always render once a warehouse is expanded | Open /settings/locations as single-warehouse tenant | Reassuring optional framing so simple traders aren't pushed through zone/bin machinery | FIXED (9966b46d) — zone/bin empty copy reframed as explicitly optional (en+ar) |
| 31 | 2026-06-30 | 05 Warehouses/Locations | LOW | No success toast on warehouse/zone/bin create/edit — dialog closes silently; first-time MENA retail users may not notice the list updated | Create a warehouse | Success toast on each mutation | FIXED (9966b46d) — success toast on every branch/warehouse/zone/bin create/edit/delete (en+ar) |
| 32 | 2026-06-30 | 05 Warehouses/Locations | MEDIUM | Single-location tenant's only warehouse is buried behind the branch chevron — the branch row is collapsed by default so the one thing that matters (Main Warehouse - Shuwaikh) needs two clicks to see | Open /settings/locations as Asala (1 branch) | Sole branch auto-expands on load so the warehouse is immediately visible | FIXED (80e0859d) — ref-guarded auto-expand when exactly 1 branch; self-disables at 2+ branches; zones/bins stay collapsed (opt-in machinery) |
| 33 | 2026-06-30 | 05 Warehouses/Locations | MEDIUM | Warehouse/zone/bin Code is a mandatory free-text field marked "Cannot be changed after creation" — a non-technical shop owner has no naming scheme and dead-ends ("no idea what code to put in") on every create | Add warehouse "Spare Store" | Code auto-suggested from the Name, still editable | FIXED (9966b46d) — shared slugifyCode; code auto-follows Name (slugified) until user edits; server stays uniqueness authority; Arabic-only names yield blank (no garbage code) |
| 34 | 2026-06-30 | 05 Warehouses/Locations | MEDIUM | Code auto-fill dead after touch: typing in Code then deleting it left the field permanently in "user owns it" mode, so typing the Name afterwards no longer generated a code | Type in Code, clear it, type Name | Clearing Code re-arms name-driven auto-fill | FIXED (383454ba) — codeEdited keys off whether field is non-empty (all 4 dialogs) |
| 35 | 2026-06-30 | 05 Warehouses/Locations | MEDIUM | Branch dialog showed the "Auto-filled from the name" hint but did NOT actually auto-derive a code (only warehouse/zone/bin did) — owner still dead-ends on the Code field | Add branch "Test Branch" | Branch code auto-derives from name like the others | FIXED (383454ba) — slugifyCode wired into branch-dialog |
| 36 | 2026-06-30 | 05 Warehouses/Locations | MEDIUM | New-branch Timezone defaulted to "UTC" which then failed the "valid timezone" check, showing a red error on a fresh form; wrong for a Kuwait shop | Open Add branch | Default to a sensible timezone / show placeholder, not an errored UTC | FIXED (383454ba) — default to browser-detected tz (Asia/Kuwait for a KW user); placeholder shown if unrecognized |
| 37 | 2026-06-30 | 05 Warehouses/Locations | HIGH | Deleting a branch that still has warehouses throws an opaque DrizzleQueryError 500 (warehouses.branch_id FK is onDelete:restrict, code 23503) — vague server error to the user | Delete a branch with a warehouse | Clear ConflictException, delete blocked | FIXED (82472c25) — warehouse-count guard in deleteBranch + tenant-scoped delete (defense-in-depth) |
| 38 | 2026-06-30 | (systemic) delete endpoints | HIGH | Codebase-wide audit (triggered by #26/#27/#37): 4 tenant delete endpoints would throw a raw Postgres FK-violation 500 (onDelete:restrict) with real data — deleteTaxGroup (9+ ref tables), deleteTaxCode (journal_entry_lines), POS tender-type remove (pos_payments), pack-unit removePackUnit (7 line tables) | Delete a tax group used on any invoice line | Friendly 409 ConflictException, not a 500 | FIXED (8dc7e69a) — isForeignKeyViolationError(23503) backstop on each; catches future FKs too; deleteBin wrapped in txn (TOCTOU); FK→409 unit tests (99 pass) |
| 39 | 2026-06-30 | (systemic) delete endpoints | MEDIUM | Same audit: 5 deletes scoped by id only, not id+tenant/parent (defense-in-depth gap; low real risk on per-tenant DBs but off-convention) — roles deleteRole/revokeRole, tax-config deleteTaxRate, notification removeRecipientRule, price-lists removeItem | Code recon | DELETE scoped by id AND tenant/parent | FIXED (8dc7e69a) — +tenantId / +parent id on all five |
| - | 2026-06-30 | 05 Warehouses/Locations | PASS | Default-warehouse invariant holds: create/update atomically flip the prior default off (tenant+branch scoped); transit cannot be default; sole-default delete blocked. Bin-code format regex + whitespace rejection OK; transit type exposed in UI + i18n parity (store/warehouse/transit en+ar); actions NOT hover-only (bug #18 absent); no hardcoded currency/2dp | code recon | invariants hold | VERIFIED PASS |
| 40 | 2026-06-30 | 06 Stock Levels | CRITICAL | Zero-stock / no-movement items INVISIBLE on /inventory/stock: searching FLT-0004 (on_hand 0) returns "No stock records found". list()+lowStock() drove FROM materialized_stock_levels INNER JOIN items, so items with no msl row (qty=0 imports never post one — apply.service ~L812 `if (qtyBase<=0) continue`) vanish; storekeeper can't see out-of-stock items to reorder | Search FLT-0004 (founder screenshot) | Every catalog item shows with On Hand 0 / OutOfStock | FIXED (b261c723; commit msg labels this #37) — driver flipped to items LEFT JOIN materialized_stock_levels (+LEFT JOIN warehouses); COALESCE on_hand/avg/value→0; warehouseId filter pushed into JOIN ON; tenant isolation on items.tenantId; lowStock COALESCE; 39/39 tests pass |
| 41 | 2026-06-30 | 06 Stock Levels | HIGH | Money rendered hardcoded 2dp with NO currency (display.ts displayMoney minFD/maxFD=2). KWD 3dp values truncated; Σ tie-out to 190,717.320 impossible on screen; no currency shown (bad for multi-currency customers) | Avg Cost / Total Value columns | Tenant currency precision (KWD 3dp) + currency code | FIXED (b261c723; commit msg labels this #34) — useTenantCurrency()+getCurrencyDecimals (mirrors items-table); Total Value shows code ("190,717.320 KWD"); displayMoney extended with optional {decimals,currencyCode}, default unchanged for other callers |
| 42 | 2026-06-30 | 06 Stock Levels | MEDIUM/UX | Reorder button forced a Purchase Order AND the reorderItemId param was ignored (no line prefill) — storekeeper landed on an empty PO header | Click Reorder on a low/out item | Choose PO vs Direct Purchase; item auto-added as a line | FIXED (b261c723) — new reorder-choice-dialog (PO/Direct Purchase tiles + hints); both targets read reorderItemId and seed the item as a line (seed-once guarded): DP panel + PO detail (addLine.mutate after draft+warehouse resolve) |
| 43 | 2026-06-30 | 06 Stock Levels | LOW/UX | Item-detail "go back" always returned to /inventory/items even when arriving from the stock screen | Stock row → item detail → Back | Returns to the screen the user came from | FIXED (b261c723) — item-form-panel back referrer-aware via ?from= (open-redirect guarded: must start "/" not "//"); stock rows navigate with ?from=/inventory/stock |
| 44 | 2026-06-30 | 06 Stock Levels | MEDIUM | committed (reservedQty), in_transit, and available=on_hand−committed exist in materialized_stock_levels but are never selected/returned/shown — storekeeper can't see available-to-sell | Stock levels table | Surface Available/Committed columns | LOGGED (batch — needs API field + column; deferred to avoid cluttering Bader's single-location view until confirmed) |
| 45 | 2026-06-30 | 06 Stock Levels | LOW | GET /low-stock silently caps at 200 rows with no truncation signal in meta | Low-stock filter with >200 low items | meta flag when truncated | LOGGED (batch — backend) |
| - | 2026-06-30 | 07 Stock Ledger | PASS | Ledger backbone solid (code recon): ZERO update/delete paths on stock_ledger_entries in apps/api; posts atomic (ledger+materialized level+GL outbox in one txn w/ SELECT FOR UPDATE); event_id NOT NULL+unique (replay-safe); WAC on decimal.js banker's-rounding 6dp; reverse()=INSERT mirror w/ partial-unique reverses_entry_id (no double-reversal). Correction: a movement-history screen DOES exist (/reports/stock-movements) — checklist's "no screen" gap is stale | code recon | invariants hold | VERIFIED PASS |
| 46 | 2026-06-30 | 07 Stock Ledger | MEDIUM | Ledger immutability was application-only; service comment claimed a Postgres trigger that did not exist | Code recon | DB-level append-only guard | FIXED (b67887e2) — migration 0140 trg_stock_ledger_entries_immutable raises check_violation on UPDATE/DELETE; idempotent; INSERT/reverse() untouched; auto-applies to tenants on deploy |
| 47 | 2026-06-30 | 07 Stock Ledger | HIGH | Stock Movement Ledger report shows NO source-document column/link — cannot trace "which document caused this row" (API stubs docNumber:null) | Open /reports/stock-movements | Clickable source-doc column resolving human doc number per source type | OPEN (deferred — next step; needs per-module join across 7+ source tables) |
| 48 | 2026-06-30 | 07 Stock Ledger | HIGH | Unit Cost / Value rendered 2dp (16.13) not KWD 3dp (16.130); formatMoney hardcoded min/max 2 | /reports/stock-movements | Tenant currency precision via shared util | FIXED (b67887e2) — getCurrencyDecimals + useTenantCurrency in table cells + CSV export |
| 49 | 2026-06-30 | 07 Stock Ledger | HIGH | "Balance" column was a single global cumsum across UNLIKE items (wiper+filter+tyre = 41,75,89) — meaningless; no item filter existed | /reports/stock-movements | Running on-hand per (item,warehouse) | FIXED (b67887e2) — per-key seed map (grouped before-window + before-offset aggregates, deterministic occurredAt,createdAt,id order); + searchable item filter; opus reviewer CONFIRMED-CORRECT; 17 jest tests |
| 50 | 2026-06-30 | 07 Stock Ledger | HIGH | Warehouse filter showed ONLY "All warehouses" despite tenant having 2 warehouses — WarehouseFilter got no branchId so useWarehousesQuery was enabled:false, never fired | /reports/stock-movements | All tenant warehouses listed | FIXED (b67887e2) — new useAllWarehousesQuery fires unconditionally; branch-scoped callers unaffected |
| 51 | 2026-06-30 | 07 Stock Ledger | MEDIUM | Module filter mis-mapped: bar (barcode sequence counter, not a movement) + pr (purchase return) bucketed under Inventory; cnt + ob_inv (opening stock) missing so Asala's opening rows would be hidden under Module=Inventory | Module filter = Inventory | Correct doc-type→module mapping | FIXED (b67887e2) — drop bar; pr→purchase; add cnt+ob_inv→inventory; verified vs documentType enum |
| 52 | 2026-06-30 | 07 Stock Ledger | MEDIUM | Filters (esp. Module) + Balance header had no explainer for non-technical owner | /reports/stock-movements | Info (i) tooltips | FIXED (b67887e2) — accessible InfoTip (keyboard-focusable, aria-label) on warehouse/movement-type/module + balance header (en+ar, no em dashes) |
| 53 | 2026-06-30 | 07 Stock Ledger | LOW | Item-search dropdown swallowed errors (showed "No items found" on a failed search); InfoTip icon was aria-hidden+asChild (invisible to screen readers); em dash in branch/warehouse option labels | frontend-reviewer | Defensive error state + a11y + no em dash | FIXED (b67887e2) — explicit searchError branch (en+ar); TooltipTrigger button w/ aria-label; "·" separator |
| 54 | 2026-06-30 | 07 Stock Ledger | LOW | reverse() does not block reversing a reversal row (chain possible; one-reversal-per-original constraint still holds) | Code recon | Pre-check original.reversesEntryId IS NULL | OPEN (batch) |
| 55 | 2026-06-30 | 07 Stock Ledger | LOW | findByItemWarehouseOrdered / findBySourceDocument unbounded (no LIMIT); safe today (internal FIFO/recon use), risk if exposed via an endpoint | Code recon | Cap/paginate if exposed | OPEN (batch) |
| 56 | 2026-06-30 | 08 Adjustments | HIGH | Zero-cost INCREASE silently dilutes WAC for items with existing cost history. Service guard (stock-adjustments.service.ts ~L858) only blocks unitCost when item has NO WAC history; an explicit `unitCost:"0"` on an item with stock passes DTO (moneySchema is `>=0`) and re-weights average DOWN: newWac=(onHand×wac+qty×0)/(onHand+qty). Storekeeper "found stock" entry at 0 corrupts valuation + 1141 tie-out | POST increase for FLT-0001 (wac 2.590, on_hand>0) with unitCost=0 | Spec: cost required/positive on increase, blocked server-side. Actual: posts, dilutes WAC | CONFIRMED LIVE (founder 2026-06-30: posted a 0-cost increase, avg cost moved 122.520→138.641 — corruption real). FIXING — server rejects unitCost≤0 on increase + form pre-fills current WAC at 3dp, required; +accounting-reviewer +jest |
| 57 | 2026-06-30 | 08 Opening Stock | MEDIUM | Manual opening-balance endpoint (POST /tenant/stock-adjustments/opening-balance → createOpeningBalance, ~L397) has NO server idempotency key: each call mints a fresh randomUUID adjustmentId so the event_id unique index does not dedup; two calls double-post. Live opening uses import path B (opening-stock-import.service.ts CAS content-fingerprint claim) which IS guarded; manual endpoint is the gap | Hit POST /opening-balance twice same lines | One opening posting per item/warehouse; second blocked/deduped | OPEN (batch — add caller idempotency key or per-item opening uniqueness) |
| 58 | 2026-06-30 | 08 Adjustments | MEDIUM | Confirm dialog (adjustment-confirm-dialog.tsx) shows only line count + type + direction — NO item / qty delta / cost impact / total value / GL accounts. Spec (08 §1) requires a change summary so storekeeper cannot post a WAC-affecting change blind | Open adjustment confirm dialog | Per-line summary: item, qty delta, cost, total value, target GL accounts | OPEN (fix candidate — enrich confirm dialog) |
| 59 | 2026-06-30 | 08 Adjustments | MEDIUM | Running total in adjustment form summary hardcoded 2dp (adjustment-form-panel.tsx ~L413 formatNumber min/maxFractionDigits:2) — systemic KWD-3dp bug (#2/#11/#41/#48 family). KWD totals rounded on screen vs server 6dp | Increase with KWD 3dp cost, view summary total | Tenant currency precision (KWD 3dp) via shared currency-precision util | OPEN (fix candidate) |
| 60 | 2026-06-30 | 08 Adjustments | LOW | No client hint that a BLANK cost on an increase auto-uses current WAC; cost not required client-side on increase (costOk allows ""). Combined with #56, a storekeeper has no feedback on what cost will be applied | New increase, leave cost blank | Hint "will use current average cost" or require explicit cost | OPEN (batch) |
| - | 2026-06-30 | 09 Transfers | PASS | Cost-carry + net-zero + atomic-pair + negative-guard verified at code level: send captures source WAC into stock_transfer_lines.outboundUnitCost (svc:392/454); receive uses that verbatim (svc:717), blends dest WAC via recalculate(currentOnHand,currentWac,qtyRcvd,outboundUnitCost) (svc:742-749) — destination does NOT revalue at its own avg; OUT(−) + in-transit(+) on send, in-transit(−) + IN(+) on receive in one TX with SELECT FOR UPDATE (deterministic lock order by itemId); source negative guard fires BEFORE any row written (svc:380-389, strict=block / flexible=flag); GL send DR1142/CR1141, receive DR1141/CR1142, shortfall DR5200/CR1142 → in-transit nets zero | code recon | invariants hold | VERIFIED PASS |
| 61 | 2026-06-30 | 09 Transfers | CRITICAL | No fiscal-period guard on send() or receive() — assertPeriodOpen() is called ONLY in cancelSent() (svc:1026); send (svc:314) and receive (svc:557) post GL JEs into soft/hard-locked or future periods with no check | Backdate occurredAt into a locked period and send/receive | Both legs assert period open like cancelSent | FIXED (staged) — assertPeriodOpen added to send() (fromWh entity, sentAt) + receive() (toWh entity, receivedAt); accounting-reviewer CONFIRMED entity matches GL posting entity; jest covers HardLocked/SoftLocked/backdated |
| 62 | 2026-06-30 | 09 Transfers | HIGH | Receive silently falls back to zero cost when outboundUnitCost is null (`?? "0.000000"`, svc:717) — would post zero-value TRANSFER_IN, understating destination inventory instead of failing loud | Receive a line whose send leg never captured cost | Throw (fail-loud); never post a zero-cost receive | FIXED (staged) — throws ConflictException inside TX before any GL write; jest covers null cost |
| 63 | 2026-06-30 | 09 Transfers | HIGH | FIFO items use the WAC consume path on send (svc:392 wacEngine.consume for ALL items regardless of item_costing_configs.costingMethod) — outboundUnitCost = running average, not FIFO layer cost; FIFO layers not consumed; destination carries wrong cost | Transfer a costingMethod='fifo' item | Cost-carry uses FIFO layer cost for fifo items | WON'T FIX NOW (founder decision 2026-06-30 — targeting WAC industries; latent for WAC tenants, revisit if FIFO is sold) |
| 64 | 2026-06-30 | 09 Transfers | HIGH | No cancel-sent button in UI: backend POST /:id/cancel-sent exists (controller:111) but transfer-detail-panel only renders Send/Cancel (draft) + Receive (sent) — a storekeeper who mis-sends has NO way to recall stock from in-transit limbo without raw API | Send a transfer, then try to undo it in the UI | Cancel-sent action on InTransit transfers (with reason + confirm) | FIXED (staged) — "Recall Transfer" button on sent transfers; confirm dialog w/ required reason + loading/error/success; new useCancelSentTransferMutation; frontend-reviewer CONFIRMED (incl DialogDescription a11y fix) |
| 65 | 2026-06-30 | 09 Transfers | MEDIUM | Serial-tracked transfer cannot be received via UI: receive dialog renders only qtyReceived + discrepancyReason, no serial entry (detail-panel:356-448), payload omits serialNumbers; backend (svc:638-665) throws BadRequest when send-leg serials require matching receive serials | Send then receive a serial-tracked item | Serial entry cells in receive dialog | FIXED (staged) — reused SerialEntryCell in receive dialog, pre-filled w/ sent serials, qty auto-driven by serial count (partial receipt by removing serials); payload carries serialNumbers; receive DTO already accepts + service validates (verified end-to-end) |
| 66 | 2026-06-30 | 09 Transfers | MEDIUM | Delivery note prints raw itemId UUID instead of item name (delivery-note.tsx:150) though line.itemName is available on the response | Print a delivery note | Show item name (fallback to id) | FIXED (staged) — itemName ?? itemId in bdi |
| 67 | 2026-06-30 | 09 Transfers | LOW | Delivery note total qty hardcoded toFixed(2) (delivery-note.tsx:169) — fractional/6dp units mis-rounded; no shared precision util | Print delivery note for a fractional-unit item | Use base-unit precision | FIXED (staged) — strips trailing zeros (parseFloat(toFixed(6))), preserves fractional |
| 68 | 2026-06-30 | 09 Transfers | LOW | incrementInTransit for the destination warehouse is passed the SOURCE warehouse legalEntityId + currency (svc:439-448) — wrong in-transit row attribution if dest is a different legal entity (multi-entity) | Inter-entity transfer | Use destination warehouse legalEntityId/currency | FIXED (staged) — resolve dest warehouse→branch→legalEntity, pass dest entity+currency to incrementInTransit; accounting-reviewer CONFIRMED dest-entity is correct owner of in-transit value, no GL net-zero impact (GL still books on source) |
| 69 | 2026-06-30 | 09 Transfers | LOW | Duplicate item across lines guarded only by DB unique (transferId,itemId) (schema:175); create() batch-inserts with no app dedup → surfaces as raw DB error not a friendly BadRequest | Add same item twice on a draft | Friendly "item already on this transfer" | FIXED (staged) — Set-based dedup in create() throws BadRequestException naming the item before any insert; jest covers |
| 70 | 2026-06-30 | 08 Adjustments | MEDIUM/UX | Success shows BOTH a toast AND a dialog at once — redundant + confusing (founder screenshot) | Post an adjustment | One confirmation only (keep success toast, drop redundant success dialog) | CONFIRMED LIVE — FIXING (frontend) |
| 71 | 2026-06-30 | 08 Adjustments | MEDIUM | Confirm dialog shows raw item UUID (not name/code) + no proper money formatting (re #58) | Open adjustment confirm dialog | Item name + SKU, qty delta, unit cost (KWD 3dp), total value, target GL accounts | CONFIRMED LIVE — FIXING (frontend) |
| 72 | 2026-06-30 | 08 Adjustments | MEDIUM | Unit-cost input does NOT apply tenant/country default decimals (KWD 3dp) when edited | Edit unit cost on adjustment form (founder screenshot) | Input formats/pads to tenant currency precision (3dp KWD) | CONFIRMED LIVE — FIXING (frontend) |
| 73 | 2026-06-30 | 06/08 Stock Levels | MEDIUM | Avg cost (correct value) and On Hand render not at 3dp on /inventory/stock after adjustment (founder screenshot 6.22) — #41 covered avg/total money but on-hand qty + this view still 2dp | View stock level post-adjustment | Avg cost KWD 3dp; on-hand at item UOM precision | CONFIRMED LIVE — FIXING (frontend) |
| 74 | 2026-06-30 | 08 Adjustments | MEDIUM | Another adjustment-area screen renders amounts not at KWD 3dp (founder screenshot 6.23) | View screen | Tenant currency precision via shared util | CONFIRMED LIVE — FIXING (frontend) |
| 75 | 2026-06-30 | 08 Adjustments | HIGH | GL journal entry DESCRIPTION/narration shows raw item UUID instead of human item name/code + doc number (founder screenshot 6.24) — unreadable audit trail in accounting | Open GL JE from an adjustment | Narration: "Stock adjustment ADJ-xxx — <item name> (SKU)" | CONFIRMED LIVE — FIXING (backend JE builder) |
| 76 | 2026-06-30 | 08 Adjustments | HIGH | Cannot open an individual stock adjustment (list rows not clickable → no detail view) AND no GL→adjustment source link (ties to #47). Storekeeper/accountant cannot trace or audit a single adjustment | Click an adjustment row; click GL source | Row opens adjustment detail (GET /:id exists); GL JE links back to the adjustment | CONFIRMED LIVE — FIXING (frontend detail route + GL source link) |
| 77 | 2026-06-30 | 08 Opening Stock | HIGH | Opening-balance line accepts unitCost "0" (openingBalanceLineSchema used non-negative moneySchema) — zero-bases WAC and dilutes the first real receipt (same family as #56). Critical for Asala, whose ENTIRE dataset is opening-only | POST /opening-balance line unitCost "0" | Strictly positive unit cost required on opening | FIXED (uncommitted) — new positiveMoneySchema (parseFloat>0) on openingBalanceLineSchema.unitCost; surfaced by accounting-reviewer adversarial pass on #56/#75; 63/63 jest pass |
| - | 2026-06-30 | 10 Stock Counts | PASS | Count→post backend path hardened & verified by recon: systemQty frozen at create; approve-post re-reads LIVE on-hand under FOR UPDATE and posts set-to-counted delta (absorbs concurrent-movement drift); F2 double-finalize no-op + header row lock; G3 single active count per warehouse; zero-WAC found-stock uses item-master cost_price else 0 (no WAC dilution, #56 family); decrease consumes at WAC via wacEngine.consume (no re-weight); all batches+serial transitions+header flip in ONE tx (atomic, reservations released on failure); uncounted lines (null countedQty) filtered out = zero variance NOT zero stock; FULL count blocks post until every line counted (F6); period guard on countDate occurredAt | code recon | — | VERIFIED PASS (backend) |
| 78 | 2026-06-30 | 10 Stock Counts | MEDIUM | Variance VALUE (money) hardcoded to 2dp on variance-review + list panels — breaks KWD 3dp; BAT-0074 −2×43.960=−87.920 renders "−87.92" (loses 3rd dp); summary surplus/shortage/net same | variance-panel.tsx:110,296-299 + list-panel.tsx:184-186 | Tenant currency precision via getCurrencyDecimals/useTenantCurrency (KWD 3dp) | FIXED (32b9a8aa) — verify live |
| 79 | 2026-06-30 | 10 Stock Counts | MEDIUM | Worksheet has NO route-leave/beforeunload guard when count lines have unsaved edits — storekeeper navigates away mid-count and silently loses entered quantities | sheet-panel.tsx hasPendingChanges | Confirm-on-leave / beforeunload when pending changes exist | FIXED (32b9a8aa) — verify live |
| 80 | 2026-06-30 | 10 Stock Counts | MEDIUM | countedQty `<Input type=number min=0>` does NOT block keyboard-typed negatives client-side (HTML min non-enforcing); server DTO rejects but no inline UI error | sheet-panel.tsx:60 QtyInput | Clamp/validate to >=0 + inline error; never submit negative | FIXED (32b9a8aa) — verify live |
| 81 | 2026-06-30 | 10 Stock Counts | LOW | `line.itemName ?? line.itemId` shows raw item UUID if name null (deleted item/join miss) on variance + sheet panels (#75 family) | variance-panel.tsx:270, sheet-panel.tsx:136 | Show SKU/placeholder, never raw UUID | FIXED (32b9a8aa) — verify live |
| 82 | 2026-06-30 | 10 Stock Counts | LOW | Em dash separator in user-visible header strings `{docNumber} — {warehouseName}` (violates no-em-dash convention) | sheet-panel.tsx:364, variance-panel.tsx:140 | Non-em-dash separator | FIXED (32b9a8aa) — verify live |
| 83 | 2026-06-30 | 10 Stock Counts | LOW | No drill-down from a POSTED count to its generated GL journal entry / ledger rows (#76 family) — accountant cannot trace count→JE | variance-panel.tsx | Link posted count → source JE / ledger movements | FIXED (32b9a8aa) — count persists posted_adjustments (mig 0142); detail links to each adjustment + its GL JE |
| 84 | 2026-06-30 | 10 Stock Counts | LOW | No empty-warehouse guard on create: empty warehouses list renders a Select with no items + no message | form-panel.tsx | Friendly empty/disabled state when no active warehouse | FIXED (8dc75d1e) — verify live |
| 85 | 2026-06-30 | 10 Stock Counts | LOW | List panel: no warehouse filter, no date-range filter, no CSV export of variance | list-panel.tsx | Warehouse+date filters and CSV export (cross-cutting #6) | FIXED (32b9a8aa) — verify live |
| 86 | 2026-06-30 | 09 Transfers | CRITICAL | PHANTOM STOCK via transfer: send shipped MORE than on-hand at source (founder PROD: BAT-0074 shipped 999 of 108 → source -931; FLT-0004 shipped 1 from already -1 → -2). Cost-carry/net-zero held but source driven negative. Root cause: send() honored tenant negativeStockPolicy which DEFAULTS to "flexible" (getNegativeStockPolicy ?? flexible) | Transfer qty > on-hand, save/send/receive | A transfer can NEVER drive source negative — for ANY tenant on ANY setting | FIXED (staged) — UNCONDITIONAL ConflictException on wouldGoNegative (removed policy branch + dead getNegativeStockPolicy), + negativeStockPolicy:"strict" forced into decrementOutbound (defense-in-depth); transfers no longer consult tenant setting (mirrors sales hard-block; POS stays flexible); accounting-reviewer CONFIRMED atomic inside FOR UPDATE txn, already-negative source blocked; jest asserts block holds when tenant policy=flexible |
| 87 | 2026-06-30 | 09 Transfers | HIGH | TOCTOU: assertPeriodOpen runs just OUTSIDE the write txn in send()/receive()/cancelSent() — a concurrent period-close between check and outbox insert could post into a now-closed period. Pre-existing pattern shared with cancelSent, NOT a regression from #61/#86 | Concurrent period-close during a transfer | Move period assert inside the txn (or period advisory lock) for all three | FIXED (staged, narrowed) — assertPeriodOpen moved INSIDE the txn (after status UPDATE/FOR UPDATE locks) for send()/receive(); cancelSent already inside; accounting-reviewer CONFIRMED strictly-better + atomic rollback. Residual: validatePeriod reads via singleton conn (documented) — full closure needs SELECT FOR UPDATE on fiscal_periods row in shared service (deferred, out of transfers scope) |
| 88 | 2026-06-30 | 09 Transfers | HIGH | Item-search dropdown on /inventory/transfers/new was CLIPPED/invisible — results rendered as an absolute div inside a TableCell, clipped by table overflow/stacking context (outer overflow-visible at L239 insufficient) | Founder PROD: type SKU on a transfer line, results not visible | Working dropdown | FIXED (staged) — reused shared BillLineSearch (purchase line picker) rendered ABOVE the table so the dropdown lives outside the table DOM; removed inline per-row search; frontend-reviewer CONFIRMED purchase callers unaffected |
| - | 2026-06-30 | 11 Serial Numbers | PASS | COGS = specific identification, NOT WAC, for serial-tracked lines on BOTH channels: cost-helpers.ts buildLineCostMap reads each serial's own acquisitionCost at claim time (POS pay L862 "F3: serial-tracked lines sell at the unit's SPECIFIC acquisition cost, not pool WAC"; Sales via claimForSale costAtSale) | code recon | serial COGS = its own cost | VERIFIED PASS |
| - | 2026-06-30 | 11 Serial Numbers | PASS | Sales claimForSale (serial-allocation.service.ts L213+) is race-safe: reads serials IN allowed status inside the confirm TX, asserts rows.length===serialNos.length AND updated.length===serialNos.length → a concurrent claim drops the count and the whole invoice confirm rolls back. No double-sell on the Sales path | code recon | second concurrent claim conflicts | VERIFIED PASS |
| - | 2026-06-30 | 11 Serial Numbers | PASS | trackingType is IMMUTABLE once any stock movement/serial/batch history exists — guarded app-layer (assertLockedFieldsUnchanged inside SELECT FOR UPDATE, items.service.ts L641) AND by DB trigger trg_items_locked_fields_immutable (migration 0112). Cannot convert standard↔serial on a stocked item or orphan history | code recon | conversion blocked with friendly 409 | VERIFIED PASS |
| - | 2026-06-30 | 11 Serial Numbers | PASS | Per-serial stock ledger: every serial movement writes a dedicated stock_ledger_entries row with serialNumberId stamped and |qty|===1 (stock-ledger.service.ts L45,63-65); on_hand = Σ ledger = count(available serials); append-only via trg_stock_ledger_entries_immutable (migration 0140). Status flip + ledger written in same TX (stay in sync) | code recon | serial count reconciles to on-hand | VERIFIED PASS |
| - | 2026-06-30 | 11 Serial Numbers | PASS | Serial uniqueness: DB unique (tenantId,itemId,serialNo) + friendly app pre-check on create() (ConflictException, not raw 500) + isUniqueConstraintError catch on race; in-input duplicates rejected before DB write | code recon | duplicate serial blocked, friendly error | VERIFIED PASS |
| - | 2026-06-30 | 11 Serial Numbers | PASS | Inbound serial count===qty enforced on adjustment-increase serial capture (stock-adjustments.service.ts ~L980-1055): validates trackingType==='serial', integer qty, serialNumbers.length===qty, no intra-batch dups, inserts status='available' with acquisitionCost=line unitCost | code recon | 5 units ⇒ exactly 5 serials | VERIFIED PASS |
| - | 2026-06-30 | 11 Serial Numbers | PASS | Bader (no-serial owner) is not forced: /inventory/serial-numbers nav entry is hidden when the tenant has zero serial-tracked items (use-nav-items.ts:46); the 8,820 standard units never demand serials; trackingType defaults 'none' | code recon | serial machinery invisible until opted in | VERIFIED PASS |
| 89 | 2026-06-30 | 11 Serial Numbers | HIGH | POS validateAndReserve (pos-serial-numbers.service.ts L143-153) issues conditional UPDATE available→reserved WITHOUT asserting rowsAffected===1. Narrow race: two concurrent add-line calls both read 'available', both fire the optimistic UPDATE; loser silently updates 0 rows yet returns void → cart believes it reserved a unit it didn't get → double-book risk at pay (two ledger/COGS rows for one physical serial). Sales claimForSale + serial-numbers.transition already assert rowcount; POS is the outlier | Two concurrent POS carts add the same serialNo on a serial-tracked item, settle both | Loser must throw ConflictException ("could not reserve, try again"), never silently succeed | FIXED (committed 9ccb1762) — validateAndReserve adds .returning() + asserts length===1, throws ConflictException on 0 rows; nestjs+accounting reviewers PASS (retry-safety mitigated by pay() draft-guard short-circuit before markSold); 2 new race jest specs |
| 90 | 2026-06-30 | 11 Serial Numbers | HIGH | POS markSold (pos-serial-numbers.service.ts L164-180) UPDATE reserved/available→sold WITHOUT asserting rowsAffected===1 — defense-in-depth: if a serial was already flipped 'sold' by a racing pay(), the second markSold silently misses but pay() still proceeds to insert its own pos_transaction + ledger -1 + COGS for that unit → double consumption / negative on-hand | Race two settlements claiming the same reserved serial | markSold must assert exactly 1 row flipped; 0 ⇒ ConflictException, roll back the sale | FIXED (committed 9ccb1762) — markSold adds .returning() + asserts length===1, throws ConflictException; rolls back pay() tx (ledger -1 + COGS unwound atomically); accounting-reviewer CONFIRMED on-hand↔ledger tie holds |
| 91 | 2026-06-30 | 11 Serial Numbers | MEDIUM | Inconsistent specific-ID cost guard: when a serial's acquisitionCost IS NULL (e.g. opening-balance serial), Sales claimForSale THROWS hard ("no positive acquisition cost"); POS silently falls back to item WAC (cost-helpers buildLineCostMap `?? wac`, only a costZeroLines flag) → same unit costs differently per channel; cost-identity loss on POS | Sell an opening-balance serial with null acquisitionCost via POS | POS should match Sales: refuse / hard-variance a null-cost serial, never silently substitute WAC | FIXED (committed 9ccb1762, founder ruling=long-term-correct) — buildLineCostMap (POS-only: pay()+sync; returns/voids use snapshotted costAtSale) now THROWS BadRequestException on a serial sale line with missing/non-positive acquisition cost, mirroring Sales claimForSale message exactly; no silent WAC substitution; 6 cost-map integrity specs |
| 92 | 2026-06-30 | 11 Serial Numbers | MEDIUM | No per-serial status-transition history: the serial row stores only CURRENT status + a single updatedAt (overwritten each transition). available→reserved→sold→returned→available is reconstructable from stock_ledger + audit_logs (doc-level) but NOT directly auditable on the unit; internal markSold/transition (sale fan-out) bypass the @Audited controller decorator | Inspect a returned-then-resold serial's history | Per-unit immutable transition trail (who/when/from→to) or documented reconstruction path | OPEN (batch) |
| 93 | 2026-06-30 | 11 Serial Numbers | HIGH | acquisitionCost rendered RAW in list + detail drawer (list-panel:199-204, detail-drawer:244-246) — `{serial.acquisitionCost}` string straight from numeric(19,6), so KWD shows up to 6dp with no currency symbol, no locale formatting (shared formatCurrency/getCurrencyDecimals/useTenantCurrency unused). Systemic 2dp-vs-3dp family (#2/#41/#73/#78) | Open /inventory/serial-numbers with a costed serial | Tenant currency precision (KWD 3dp) via shared util, with symbol | CONFIRMED (code) — batched |
| 94 | 2026-06-30 | 11 Serial Numbers | MEDIUM | warehouseId UUID shown as fallback when warehouseName null (list-panel:194, detail-drawer:214: `warehouseName ?? warehouseId`) — raw UUID leak (#75 family) | Serial whose warehouse join misses | Render placeholder/SKU, never raw UUID | CONFIRMED (code) — batched |
| 95 | 2026-06-30 | 11 Serial Numbers | HIGH | Add-serial dialog called useItemsQuery({limit:200}) but the items list DTO caps limit at 100 → GET /tenant/items?limit=200 returns 400 "Too big: expected <=100"; the whole serial-numbers page threw a "something went wrong" toast and the item dropdown was empty (founder PROD) | Open /inventory/serial-numbers | Page loads; picker lists serial-tracked items | FIXED (committed) — added server-side trackingType filter to items list (DTO+service); picker now useItemsQuery({trackingType:"serial",limit:100}) fetching only the small serial subset (no client-side filter, no cap breach); items.service spec asserts tracking_type predicate |
| 96 | 2026-06-30 | 11 Serial Numbers | MEDIUM | Backend 409 "already exist" surfaced as one generic error toast; the conflicting serial(s) are not highlighted inline (intra-dialog dups ARE highlighted) | POST a serial already in DB | Parse 409 body, red-chip the offending serial(s) | CONFIRMED (code) — batched |
| 97 | 2026-06-30 | 11 Serial Numbers | LOW | Empty-state copy conflates "no results for current filter" with "zero serials ever" — both show "created automatically via GRN or POS" (inventory.json:509-510) | Filter to a status with no matches | Separate emptySearchDescription vs zero-records copy | CONFIRMED (code) — batched |
| 98 | 2026-06-30 | 11 Serial Numbers | LOW | serialNo mono spans lack dir="ltr" (list-panel:174, detail-drawer:194) — serial strings may render RTL when mixed with Arabic context | View serials in ar locale | dir="ltr" on serial-number spans (bidi isolation) | CONFIRMED (code) — batched |
| 99 | 2026-06-30 | 11 Serial Numbers | LOW | Hardcoded English placeholder "e.g. 12" in warranty-months inputs (detail-drawer:91, add-dialog:287) — not via next-intl | Open add/edit warranty in ar | Move placeholder to i18n key | CONFIRMED (code) — batched |
| 100 | 2026-06-30 | 09 Transfers | CRITICAL | A tenant can create only ONE draft transfer ever: drafts insert transfer_number='DRAFT' literal (svc:286) under unique (tenant_id,transfer_number); 2nd draft → Postgres 23505 → 500 ISE (founder PROD, api log duplicate key (tenant,DRAFT)) | Save a 2nd draft transfer when one already exists | Unlimited drafts; real TRF numbers still unique | FIXED (348122ea) — migration 0141 replaces the unique constraint with a PARTIAL unique index WHERE transfer_number <> 'DRAFT' (idempotent, auto-applies per tenant on deploy); db-reviewer CONFIRMED safe (DROP CONSTRAINT frees backing index before CREATE), no existing-data risk; jest: two drafts same tenant OK |
| 101 | 2026-06-30 | 09 Transfers | MEDIUM | Duplicate-add fires TWO identical toasts for one attempt (founder screenshot) | Add same item twice via picker | One toast | FIXED (348122ea) — stable toast id keyed to item id (sonner dedup); single Toaster confirmed; founder OK keeping toast over dialog |
| 102 | 2026-06-30 | 09 Transfers | LOW | No back navigation from new-transfer form or transfer detail screen (unlike items pages) | Open /inventory/transfers/new or a transfer detail | Back-to-list link | FIXED (348122ea) — "Back to Transfers" Link (ArrowLeft) on both screens, reusing item-form-panel pattern; en+ar |
| 103 | 2026-06-30 | 11 Serial Numbers | HIGH | Serial-numbers nav was gated ONLY on settings.inventoryConcept ∈ {serialized,mixed} (use-nav-items.ts) — NOT on whether any serial-tracked item exists. Founder marked an item serial-tracked but the sidebar entry never appeared (Asala concept is simple/standard, unrevisitable post-onboarding). Supersedes the earlier PASS note that claimed the nav keyed off serial-item presence | Mark an item serial-tracked, check sidebar | Serial nav appears the moment any item is serial-tracked | FIXED (committed) — gate now ORs in a cheap useItemsQuery({trackingType:"serial",limit:1}).meta.total>0; Bader (zero serial items + simple concept) still sees nothing |
| - | 2026-06-30 | 09 Transfers | PASS | Negative-stock block CONFIRMED LIVE on PROD: transferring 1 from FLT-0004 (on hand -2) rejected with "Insufficient stock ... requested 1.000000, on hand -2.000000. Stock transfers cannot drive source on-hand negative." (#86 fix verified by founder screenshot) | Founder PROD | block fires | VERIFIED PASS |
| 104 | 2026-06-30 | 09 Transfers | MEDIUM | Send/receive/cancel ERROR shows TWO toasts: component's clean message + global MutationCache "Something went wrong: <msg>" (transfer mutations had no local onError so the global guard at query-provider.tsx:81-83 fired too). Distinct from #101 (picker dup) | Trigger any transfer mutation error (e.g. send from negative) | One clean toast | FIXED (3ebbd18c) — 5 transfer mutations carry a noop local onError so the global guard bails; component try/catch is the single toast source |
| 105 | 2026-06-30 | 09 Transfers | LOW | Qty rendered at raw 6dp ("1.000000") on transfer detail Qty Sent/Received + receive dialog + form pack-unit hint (numeric(19,6) string straight to screen) | Open a transfer detail | Strip trailing zeros | FIXED (3ebbd18c) — reused shared formatQty (reports/lib/format) on display cells; input fields untouched |
| 106 | 2026-06-30 | 09 Transfers | LOW | Negative-stock block message names the raw item UUID ("Insufficient stock for item e94d1898-...") instead of item name/SKU — ugly user-facing error (svc ConflictException uses line.itemId; #75 UUID-leak family) | Trigger the block | Name the item (e.g. "Air Filter 1 (FLT-0004)") | FIXED (4d606009) — getLinesScoped LEFT JOINs items (tenant guard on join), itemLabel() + resolveItemLabels single-query; all transfer error messages (insufficient/serial/no-cost/dup) name item; nestjs-reviewer confirmed tenant-safe + no N+1; 66 tests |
| 107 | 2026-06-30 | 10 Stock Counts | HIGH/UX | Worksheet+variance screens show the FROZEN system qty (snapshot at create) and variance = counted − frozen, but approve-post applies set-to-counted against LIVE on-hand. After a movement during the count (founder made a −2 adjustment on BAT-0074 then counted the new figure), the screen shows the old qty and a variance that does NOT match what posts (posts 0, correctly avoiding double-count). Logic correct (prevents double-applying the adjustment); display erodes trust ("expected −2, shows no variance, old system qty") | Start count, adjust an item, count it, open variance | Surface CURRENT live on-hand + ACTUAL delta that will post when it differs from frozen, with a "stock changed since count started" note | FIXED (32b9a8aa) — get() returns liveOnHand+postDeltaQty; variance panel shows Current stock + Change to apply + drift banner; summary driven by live delta; accounting-reviewer verified value display (unknown WAC shows "-" not 0). Posting logic unchanged |
| 108 | 2026-06-30 | 10 Stock Counts | HIGH/UX | User stuck: no Back link, no Cancel button, no Edit/reopen after submit. A FULL count snapshots all items and (correctly) refuses to post until every line counted; with 1 item counted the user can't post, can't cancel (no UI button though API allows it), and can't start a new count (one-active-count guard) | Submit a part-counted full count | Back-to-list link; Cancel button (draft/in_progress/pending_review); Edit/reopen (pending_review→in_progress) | FIXED (afd5971a) — back link + Cancel(+confirm) on worksheet & variance, Edit/reopen on variance; new POST /:id/reopen with CAS status guard (nestjs-reviewer caught the race); 7 jest specs; en+ar | FIXED — verify live |
| 109 | 2026-07-01 | 11 Serial Numbers | HIGH | Adding the FIRST serial 400s "expected object, received string": serial-numbers-api createSerialNumbers (POST) and updateSerialWarranty (PATCH) passed body: JSON.stringify(input), but apiClient JSON-stringifies the body itself → double-encoded → server parses to a string, not an object | Add any serial via the dialog | Serial created; warranty saved | FIXED (committed) — pass raw object (body: input) on both; apiClient owns serialization. Swept apps/web/src/features for other apiClient double-encodes — none (only use-mira-voice uses raw fetch, correct) |
| 110 | 2026-07-01 | 12 Batches | HIGH | Write-off button posts a REAL GL inventory write-down (Dr write-down / Cr inventory, control 1141) with NO confirmation dialog — `batches-list-panel.tsx` `onClick={() => writeOff.mutate(batch.id)}` fires directly. One stray click zeros a lot's qty and moves money. Violates checklist 1.5 + "assume dumb customers" (destructive + financial action with no confirm/data-loss warning) | Click "Write off" on any active batch | Confirm dialog ("this zeros the lot and posts an inventory write-down — sure?") with the lot no + qty × cost amount, before any post | FIXING (AlertDialog confirm + qty×cost preview; isPending already guards double-click) |
| 111 | 2026-07-01 | 12 Batches | HIGH | NO DB CHECK constraint on `item_batches.qty_remaining >= 0` despite the schema comment claiming "checked at DB level". Negative-batch guard is service-layer only (decrementBatchQty throws, but it's currently UNCALLED; live qty is maintained by stock-ledger.service incrementBatchRemaining). A concurrent/buggy path could drive a lot negative with no DB backstop, breaking Σ batch = on-hand | DB-level recon (no SELECT needed) | DB CHECK constraint enforces qty_remaining >= 0 as the backstop | FIXING (idempotent migration adds CHECK + corrects the schema comment) |
| 112 | 2026-07-01 | 12 Batches | MEDIUM | Batch list unit cost hardcoded `minimumFractionDigits: 2` → KWD 3dp truncated; no currency code shown. Same systemic 2dp bug fixed for Stock Levels (#41). qtyRemaining already uses 0–6 dp correctly | View Unit Cost column for a KWD lot | 3dp (tenant precision) via getCurrencyDecimals + currency code | FIXING (useTenantCurrency + getCurrencyDecimals, mirrors items-table/#41) |
| 113 | 2026-07-01 | 12 Batches | HIGH | POS add-time FEFO (`pos-transactions.service.ts` ~L366) pre-selects a lot by `item_batches.warehouseId` (the ORIGIN column) instead of ledger-derived per-warehouse availability. Since migration 0112 made per-warehouse qty a ledger projection, a lot fully transferred OUT of the register's warehouse can still be pre-selected, while the correct ledger-derived `selectFefoBatch` sits UNUSED. Actual consumption attribution (movement-attribution → BatchPickerService.pick) IS ledger-correct, so on-hand stays right, but the displayed/pre-chosen lot can disagree with what is truly consumed (FEFO-display correctness) | Founder DB confirm: transfer a batch-tracked lot out of a warehouse, then sell that item at a register in that warehouse | POS pre-selects only lots with ledger qty>0 at the register warehouse (route add-time read through selectFefoBatch) | OPEN — POS file shared with serial (11) agent this session; defer edit, founder to confirm via DB then schedule wiring swap |
| 114 | 2026-07-01 | 12 Batches | LOW/DESIGN | COGS on a batch-tracked sale uses pool WAC (`readWacByItem`), NOT the consumed lot's `unitCost`. This is BY DESIGN in a WAC-target system (FIFO cost-carry was won't-fix-now, see #63) — batch tracks identity/expiry/FEFO physical rotation, valuation stays WAC so Σ stock value ties to 1141. Risk is only that `batch.unitCost` shown in the UI can read as a costing layer it isn't | n/a (design note) | Document: batch is identity+expiry tracking; valuation/COGS is WAC; lot unitCost is the received reference cost, informational | OPEN/by-design — verify Σ(qtyRemaining×unitCost) is reconciled as a reference only, not asserted = 1141 |
| 115 | 2026-07-01 | 12 Batches | LOW | GRN receive (`grn-create-panel.tsx`) captures batchNumber + expiryDate per line but NO manufacturedDate field, so the checklist "manufactured ≤ expiry" guard cannot be exercised from the receive UI; the column exists on item_batches but is never populated on the live receive path | Receive a batch line | Optional manufactured-date input; server validates manufactured ≤ expiry | OPEN |
| 116 | 2026-07-01 | 12 Batches | LOW | Batch row is not clickable — no detail drawer/view (checklist 1.3 expects batch detail with GRN drill-down link). All fields are in the row, but GRN drill-down + supplierBatchRef detail have no surface | Click a batch row | Detail drawer with GRN link resolving to the source document | OPEN |
| 117 | 2026-07-01 | 13 Valuation | HIGH | Valuation report renders money at hardcoded 2dp, not tenant currency precision. `reports/lib/format.ts:37` `formatMoney` defaults min/max FractionDigits=2 and the report calls it bare (no currency context). For KWD (3dp) the golden total 190,717.320 displays as "190,717.32" and every avgUnitCost/totalValue/subtotal/grandTotal drops the 3rd decimal; CSV export inherits the same truncation. Sibling `stock-movement-ledger-report.tsx` does it right (useTenantCurrency + getCurrencyDecimals → maximumFractionDigits). Systemic across the reports slice (balance-sheet etc.) but CRITICAL on the tie-out keystone | Open /reports/inventory-valuation as KWD tenant; totals show 2dp | KWD 3dp everywhere via getCurrencyDecimals(currency), screen == CSV == GL | FIXED — inventory-valuation-report.tsx now threads moneyOpts(getCurrencyDecimals) through every formatMoney incl CSV+chart; web typecheck green |
| 118 | 2026-07-01 | 13 Valuation | MEDIUM | Ledger-vs-cache drift + negative-stock signals computed but never surfaced. Backend returns `summary.ledgerDriftCount`, per-row `ledgerDrift`, and `summary.negativeValueCount` (F1/F11 safety); DTO comment states "the UI must warn rather than present it as authoritative". `inventory-valuation-report.tsx` renders NO banner/badge/row-highlight for either. A cache/GL divergence on the "now" path (the tie-out keystone) would go silently unnoticed | n/a (any drift row) | Warning banner when ledgerDriftCount>0 ("N items disagree with the ledger, showing ledger value") + negative-stock badge | FIXING with #117 |
| 119 | 2026-07-01 | 13 Valuation | LOW | As-of / point-in-time valuation has no UI entry. Backend `asOfDate` param is fully implemented (ledger-derived point-in-time that ties to GL at that date), but `inventory-valuation-report.tsx` only wires warehouse+category filters and never sends asOfDate. Period-close / historical valuation is inaccessible from the web UI; checklist §1 as-of item cannot be exercised | /reports/inventory-valuation has no date picker | Add an as-of date filter to the report filter bar | FIXED (a63b1036) — AsOfDateFilter added to the report filter bar (native date input, max=today blocks future, default today, falls back to today if cleared); asOfDate always in query params so date change refetches; api helper already passed it through. frontend-reviewer APPROVED; web typecheck + i18n en/ar green |
| 120 | 2026-07-01 | 13 Valuation | CRITICAL | MASTER TIE-OUT BROKEN (live prod, Al-Asala). GL 1141 (Balance Sheet) = 190,835.270000; ledger Σ signed total_cost (what the valuation report displays) = 190,889.530000 → report ≠ balance sheet by **+54.260 KWD**. Separately the materialized cache = 190,905.651073, off from the ledger by +16.121073 on ONE item (bcee9438, the #56 zero-cost-increase item: cache still holds corrupted avg 138.641073 vs ledger 122.520000 — cache never rebuilt after the ledger correction). Net cache-vs-GL = +70.381 KWD. LOCALIZED: entire 54.26 is in the ADJUSTMENT path — grn/trf/pos tie to the fils; ledger adj+opening=190,673.55 vs GL opening 190,717.32 + adj −98.03 = 190,619.29. PINPOINTED: ONE ledger doc 300913c7 = movement_type='adjustment_increase', total_cost 54.260, occurred 2026-07-01 07:44:18 — gl_1141=NULL (never posted). REFRAMED root cause: its accounting event is STUCK 'pending' in accounting_event_outbox (29 pending rows 06-29..07-01; newest = 07:44:18.68 = this doc). The GL post was never delivered because the in-process emit didn't post it AND OUTBOX_POLLER_ENABLED=false on prod (poller off → no retry/drain). Journal posting dedupes on eventId (safe to drain). So it's a PARTIAL POST caused by lossy GL delivery, not a compute error (posted adjustments tie by construction). (The other join-NULL row 4be6063d=190,717.32 is opening — benign, its OB_INV-0001 journal exists under a different source_document_id and ties in aggregate.) Separately cache stale +16.121 on item bcee9438 (needs rebuild from ledger) | prod DB: SELECT SUM(debit-credit) 1141 vs Σ ledger signed total_cost | Σ ledger total_value = GL 1141 exactly at every cut | INVESTIGATING (localization SQL requested from founder) |
| 121 | 2026-07-01 | 13 Valuation | HIGH→CLOSED | RESOLVED by founder cleanup (B1 now shows all 153 items across 2 real warehouses, 8,838+4=8,842 units, no orphan warehouse; the ~1,100-unit gap was zero-value rows since removed). Original: ~1,100 on-hand units carry an orphaned/unjoinable warehouse_id. `SELECT SUM(on_hand) FROM materialized_stock_levels` = 9,942 but the per-warehouse breakdown (JOIN warehouses) sums to only 8,842 units (8,838 Shuwaikh + 4 Test Branch); the 1,100-unit remainder carries exactly 0 value so it hides in value totals but is dropped by any warehouse-joined query. The valuation report uses the same INNER JOIN warehouses, so those rows silently vanish from the report (unit count understated; would leak value if those rows were ever non-zero) | prod: SUM(on_hand) vs per-warehouse sum | Every stock row references a live warehouse | CLOSED — not reproduced after cleanup |
| 122 | 2026-07-01 | 13 Valuation (delivery) | CRITICAL | Lossy GL delivery: accounting_event_outbox has 29 rows stuck status='pending' (2026-06-29..07-01) because OUTBOX_POLLER_ENABLED defaults "false" and prod runs poller OFF (outbox-poller.service.ts:113-118). At least one (the 54.26 adjustment_increase, #120) never posted its 1141 journal → ledger>GL. The poller is the durability backstop for any inline-emit failure; disabled = every missed emit is permanently lost with no retry. Journal posting dedupes on eventId (listener :295-298) so draining is double-post-safe | prod: SELECT status,COUNT(*) FROM accounting_event_outbox | Poller enabled in prod (or adjustments get a reliable inline emit); pending backlog drained | ROOT-CAUSED+FIXED (code): inventory.adjustment.posted/transfer.completed/purchase.payment.posted were absent from DOMAIN_EVENT_TYPES so the poller mis-routed them to postFromEvent (needs `lines`)→dead-letter, leaving fire-and-forget inline emit as sole delivery; a lost emit (restart 07:44) dropped the 54.26. Fix: registered all 3 in DOMAIN_EVENT_TYPES (inventory.constants.ts) + spec. accounting-reviewer APPROVED (eventId dedup DB-unique+guards, replay uses original business date/no FX re-rate, no double stock). Track2 ops: confirm periods open→set OUTBOX_POLLER_ENABLED=true→drain→verify 1141=190,889.53→rebuild cache row bcee9438 |
| 123 | 2026-07-01 | 13 Valuation (opening) | HIGH/LATENT | Separate latent bug (not this tenant's 54.26): inventory-import.apply.service.ts:194-200 first-writer-wins skip posts opening stock to the ledger unconditionally but posts NOTHING to GL 1141 when isInventoryControlSeeded()=true (checks only net≠0, never that the amount matches). A 2nd inventory import re-seed adds ledger value with zero GL → ledger>GL drift, mislabeled reconciled=true | re-run inventory import when 1141 already seeded | reconcile-to-ledger true-up: post Dr1141/CrOBE for the delta between Σ opening-ledger value and current 1141 opening net; idempotent | FIXED (8d1de1a9) — seeded branch calls reconcileInventoryOpeningToLedger; delta read on SAME basis as report (signed stored total_cost over opening_balance movements) so 1141 = report exactly; in-lock advisory-lock re-read of both sides + deterministic eventId dedup; negative-delta refused+flagged; accounting-reviewer APPROVED after basis switch (qty*unit_cost→total_cost) + TOCTOU close; 221 api tests green |
| 124 | 2026-07-01 | 11 Serial Numbers | CRITICAL | Manually adding serials created item_serial_numbers rows but posted NO stock movement: on-hand stayed 0 (item "Out of Stock" despite 3 available serials), the add dialog had NO acquisition-cost field, so every serial had null cost basis → later tripped the specific-ID COGS guard at sale. Broke on_hand=count(available serials) invariant | Add serials, view /inventory/stock (founder PROD: BAT-SERIAL-TEST on-hand 0) | Serials add to on-hand at an entered acquisition cost, with GL | FIXED (3f1915e1) — add dialog now routes through the increase-adjustment engine (type "Found"): atomic on-hand + stock ledger + GL + serial rows (available, acquisitionCost=unitCost, purchaseDocId=adjustment). Required "Acquisition cost per unit" field added (positive, tenant dp). Reuses already-reviewed 08 engine, backend unchanged. NOTE: "Found" posts Dr Inventory / Cr inventory-gain — standard ERP treatment for recording owned stock post-onboarding; flag if a different counterpart wanted |
| 125 | 2026-07-01 | 11 Serial Numbers | MEDIUM/UX | Typing a serial already in the chip list silently vanished (dedup with no feedback) — user thinks it was added | Type SN-AAA-001 twice in the add dialog | Show "already in list, skipped" note | FIXED (3f1915e1) — skipped serials surfaced inline; paste hint (comma/newline) added |
| 126 | 2026-07-01 | 11 Serial Numbers | MEDIUM/UX | Server-side "already exists" 400 would force the user to retype the whole entered list (imagine 100s of serials) | Add a serial that already exists in DB | Preserve entered list on error | FIXED (3f1915e1) — dialog stays open with serials intact on any submit error; friendly copy. (Which-exist inline highlighting still batched, see #96) |
| 127 | 2026-07-01 | 11 Serial Numbers | HIGH/UX | Serial detail is an ephemeral right-drawer (component state only) — no URL reflection, lost on refresh, not shareable/back-safe (founder: "so so bad"). Purchase/sale ref + acq cost showed "—" with no manual entry path (they populate from the adjustment/GRN, sale doc, and the acq cost on add — pre-#124 all null) | Open a serial detail, refresh | URL-reflected detail (route or ?serial=id); populated refs after #124 | FIXED (07650cef) — drawer reflects the open unit in the URL (?serial=id) via App-Router useSearchParams (refresh/back/share work); serial responses now resolve purchaseRef (stock_adjustments doc no.) + saleRef (pos_transactions/invoice no.) instead of raw ids |
| 128 | 2026-07-01 | 11 Serial Numbers | MEDIUM/UX | Edit-warranty shows an "Expiry Date" label but no way to set it — expiry is derived at sale (saleDate + warrantyMonths), so pre-sale it is always "—", reads as broken | Open Edit Warranty | Hide/grey expiry with "set automatically at sale" hint, or allow explicit override | FIXED (07650cef) — expiry field now carries a "Set automatically when the unit is sold" hint so it no longer reads as broken |
| 129 | 2026-07-01 | 11 Serial Numbers | LOW/UX | No way to edit/delete a mistyped serial after creation (serialNo immutable once it has ledger history by design, but a pre-sale correction/delete path is missing) | Mistype a serial, try to fix | Allow delete/correct of an available serial with no sale history (reverse its inbound) | OPEN (batch) |
| 130 | 2026-07-01 | 11 Serial Numbers | LOW/FUTURE | Warranty card photo upload → send to customer WhatsApp at sale | — | Attach + WhatsApp warranty card | LOGGED IN LINEAR (future) |
| 131 | 2026-07-01 | 12 Batches | HIGH | Stock Adjustment (New Adjustment) had NO batch-number field, so a batch-tracked stock-IN (PurchaseReceived/Found/Other-increase) is rejected server-side: "Item … is lot/serial-tracked: batch-tracked receipt without a batch number. The movement cannot be recorded without resolvable batch/serial attribution." A batch item literally could not receive opening/adjustment stock. Backend DTO already accepted batchNumber/expiryDate (adjustmentLineSchema) — pure frontend gap (form captured serial intent but never batch) | Create batch-tracked item → New Adjustment, type PurchaseReceived, add item, Submit (founder repro, TEST-BAT-AZW9) | Per batch-tracked increase line: a required Batch/Lot No. input + optional expiry, sent to the server which find-or-creates the lot | FIXED — threaded trackingType through item search → FormLine; batch/expiry sub-row rendered for batch-tracked increase lines (required batchNo, aria-invalid, i18n en+ar); buildPayload sends batchNumber/expiryDate; linesValid blocks submit until batchNo entered; web typecheck+i18n green |
| 132 | 2026-07-01 | 12 Batches | MEDIUM | Duplicate error toasts on a failed adjustment (one raw message + one "Something went wrong: …"). Root cause is SYSTEMIC: global MutationCache.onError bails only when `mutation.options.onError` (hook-level) is set, but the adjustment form handles errors at the CALL site (`mutate(…, { onError })` — to branch on the negative-stock conflict), which TanStack v5 does NOT expose on mutation.options → global handler AND call-site handler both fire | Trigger any adjustment error (e.g. #131 repro) | One toast per failure | FIXED — added `meta.suppressGlobalErrorToast` to useCreateAdjustmentMutation and taught the global bail-out to honor it. Reusable pattern for any call-site-onError mutation |
| 133 | 2026-07-01 | 06 Stock Levels | MEDIUM | Item detail "Stock by Warehouse" rendered Average Cost at 6dp ("8.530000") and On Hand at "90.000000" — money not at tenant precision (KWD 3dp), qty not trimmed. Same systemic 2/6dp family as #41/#112 | Open a batch item detail (founder screenshot) | Avg Cost 3dp ("8.530"), On Hand trimmed ("90") | FIXED — item-stock-display.tsx now uses useTenantCurrency+getCurrencyDecimals for Avg Cost, formatNumber 0–6dp for On Hand |
| 124 | 2026-07-01 | Doc-numbering (product-wide) | CRITICAL | REAL ROOT CAUSE = LIVE CODE BUG (not user/data): doc-numbering.service.ts reserveNumber (:497/508) + reserveNumbers (:595/606) read next_number from a RAW tx.execute(SELECT *) where the pg/Neon driver returns bigint as a STRING, so `currentNumber + 1` = STRING CONCAT ("1"+1="11"). EVERY document post (jrn/adj/trf/cnt/grn/pay/pinv/ob*) on EVERY tenant appends a digit (1→11→111…) until it overflows bigint (~18 posts) and blocks ALL journal posting. Al-Asala had 14 corrupt sequences (all repunits). FIXED f540c87c (Number() coercion in both reserve paths + regression tests, 42 pass). Data remediation: reset corrupt sequences (next_number>1) to 200000 AFTER deploy, then re-drive outbox. Original symptom: document_sequences.next_number corrupted to a 20-digit repunit (11111111111111111111) for the JE (JRN) sequence (id cba5d5b4…) → overflows bigint → EVERY journal-entry post for Al-Asala fails (DocNumberingService.reserveNumberOn). This is why enabling the poller did NOT move GL 1141 (re-emitted 54.26 hit this + failed) and why new adjustments do not reflect in GL. Existing entry_numbers show ever-growing 1s (JRN-11111 … JRN-11111111111111111111) — sequence has been emitting garbage. ROOT CAUSE = DATA pollution (direct DB write of synthetic JRN-1111… entry_numbers + repunit next_number during prod testing), NOT a live API bug: the update DTO already caps nextNumber at 999_999_999 (doc-numbering.dto.ts:129) and reservation only does +1, so the API cannot produce a repunit. Reset is permanent. bigint mode:number float64 also loses precision >2^53. | POST any stock adjustment; API log shows document_sequences overflow | Reset next_number to safe headroom above max real seq + re-drive stuck outbox rows (dedup-safe); optional DB CHECK(next_number BETWEEN 1 AND 999999999) after data clean | RESET RUNBOOK DELIVERED (data fix) + DB-CHECK SHIPPED (a63b1036): migration 0143 adds CHECK(next_number BETWEEN 1 AND 999999999) on document_sequences; column stays number mode (cap < 2^53 kills the float64 hazard, no type-surface change). database-reviewer APPROVED. DEPLOY GATE: sweep every tenant DB for next_number>999999999 and reset to 1 BEFORE migrate, else Railway pre-deploy fails on that tenant (al_asala + merpec already clean) |
| 125 | 2026-07-01 | 08 Adjustments (preview) | MEDIUM (was reported CRITICAL) | RECLASSIFIED: backend is CORRECT — stock-adjustments.service resolveLineBaseQuantities() applies resolvePackUnit before the tx (svc:743-782), ledger stores base qty (24) and total_cost=baseQty×unitCost (204.72), GL payload == ledger by construction (proven by 3 new jest tests). The bug is FRONTEND PREVIEW ONLY: adjustment-form-panel confirmLines computed lineTotal from stale l.quantity ('1') for pack lines, so the confirm dialog DISPLAYED 8.53/17.06 instead of 204.72 — persisted value was always correct | Adjustment: add pack-unit item, qty 2 Box, view confirm dialog total | Preview total == posted total_cost | FIXED — confirmLines derives base qty via computeBaseQty; +3 backend costing tests (66 pass); both typechecks clean |
| 126 | 2026-07-01 | 13 Valuation (delivery) | HIGH | Outbox poller marks DOMAIN-event rows status='completed' even when the downstream async JE post FAILS (re-emit is fire-and-forget; a throwing @OnEvent listener does not mark the row failed). Observed: 29 rows 'completed' yet GL 1141 unchanged and the 54.26 JE never posted (blocked by #124). False durability — a 'completed' outbox row does not guarantee the JE landed, and such rows won't auto-retry | enable poller with a failing JE post | JE-post rows ARE result-driven (poller awaits postFromEvent→markFailed, verified); the 'completed' rows are DOMAIN rows marked completed after emitAsync while the JE rides a SEPARATE accounting.post row — safe by design EXCEPT the post-commit optimistic COGS emit swallows errors (inventory-event.listener.ts:537-547). Minimal fix: drop the in-process COGS emit (poller re-emit already covers) so one awaited JE path + periodic NOT-EXISTS reconcile alert | FIXED (a63b1036) — removed the post-commit in-process COGS emit + dead emitCogsAccountingPost; the transactional accounting.post outbox row (inside tx, same eventId/amount) posted by-result by the poller is now the single JE path (dedup via journal_entries.event_id unique index). Added throttled (10min) NOT-EXISTS reconcile-drift WARN scoped to accounting.post rows, diagnostic-only, failures swallowed. accounting-reviewer + nestjs-reviewer APPROVED; 88 api tests pass. FOLLOW-UP DONE (4d2e5605): inbound sale_return COGS reversal + negative-stock true-up in-process emits also removed — each was a pure duplicate of its transactional outbox row (sale_return→postFromEvent DR1141/CR5100; true-up→accounting.post), now single-path + atomic with the ledger write (outbox insert inside tx, strictly safer than swallow-and-log). Deleted unused emitCogsReversalAccountingPost. accounting+nestjs re-APPROVED; typecheck green, 56 tests pass |
| 127 | 2026-07-01 | 13 Valuation (chart) | LOW | Category breakdown chart: when one category dominates (Oils & Fluids ~all value), every other category's bar renders at ~1px (pct=value/grandTotal) and olive opacity fades with index, so only the top bar is visible. Cosmetic; data is correct | valuation report chart with a dominant category | Min-width/log-scale or per-category color so all categories read | FIXED (a63b1036) — index-faded opacity replaced with a 6-entry brand palette (citron #979C1A dominant + olive family, fill-only never text), min bar width 1%→2% so every non-zero category renders; value labels remain authoritative. frontend-reviewer APPROVED |
| 128 | 2026-07-01 | Accounting listener (minor) | LOW | Zero-value adjustment (unitCost/totalCost 0, e.g. legacy #56 zero-cost increase item bcee9438) makes InventoryAccountingListener build a JE with a zero-amount 1141 line → JournalPostingService rejects "a line must be either a debit or a credit" and the event retries/dead-letters. Cosmetic (a zero-value adjustment correctly has no GL impact and no JE needed) but produces error-log noise + DLQ churn | re-drive a zero-cost adjustment event | listener skips zero-amount lines / emits no JE when net value is 0 | FIXED (a63b1036) — InventoryAccountingListener adds exact-Decimal isZeroAmount(), continues past zero-totalCost lines (adjustment + both transfer branches) and early-returns with no JE when lines.length===0; mixed batches still post a balanced JE for non-zero lines only. accounting-reviewer APPROVED; specs cover zero→no-JE + mixed-batch |
| --- | 2026-07-01 | 13 Valuation | RESOLVED | MASTER TIE-OUT ACHIEVED + UI-verified: ledger Σ = GL 1141 = materialized cache = report grand total = 190,818.670 KWD (state moved from the 190,717.320 golden baseline due to session test adjustments; the INVARIANT holds, which is what matters). Root cause chain: valuation display (#117) → outbox delivery gap (#122) → doc-numbering string-concat overflow (#124, product-wide, fixed f540c87c). Remediation: sequence reset to 200000 + outbox re-drive + full cache rebuild from ledger. #118 drift banner cleared | /reports/inventory-valuation vs Balance Sheet 1141 | report = cache = ledger = GL at 3dp | VERIFIED PASS (founder UI 2026-07-01) |
| 130 | 2026-07-01 | Purchase (Direct) — pack costing preview | HIGH | Direct Purchase line total ignored the pack conversion: lineTotal() valued against the raw typed pack qty (e.g. 10 Box) instead of the base qty (120 Each), so a pack line previewed 10×unitCost=120 while the backend correctly posts baseQty×unitCost=1,440 to GL 1141 (unit cost is per BASE unit by system convention — founder confirmed: price = unitCost × base pieces). 12× understatement in the confirm preview → a user trusting it would post far more value than intended. Also the unit-cost input never re-formatted to tenant precision (showed "12" not "12.000" for KWD 3dp). Backend + submit path were already correct (submit used computeBaseQty); GRN/bill/return panels already route totals through computeBaseQty — defect localized to direct-purchase-panel. Twin of #125 (adjustment preview) | Purchase → Direct → New; add a pack-unit item (BAT-0069), Unit=Box (1 Box=12 Each), Qty 10, cost 12 → Total shows KWD 120.000 (should be 1,440.000) | preview line total == posted value (baseQty × unitCost); unit cost renders at tenant dp | FIXED (1c51b0d9) — lineTotal derives baseQty via computeBaseQty with the SAME args as submit (preview==posted); unit-cost input formats to getCurrencyDecimals on blur, guarded on currencyCode+isPositiveDecimal. frontend-reviewer APPROVED; web typecheck green. Founder posted the test in base unit (Each) so NO corrupt data — tie-out 190,818.670 intact |
| 131 | 2026-07-01 | 13 Valuation (delivery) | CRITICAL | Live incident found during A3 (WAC-on-issue): a POS sale posted revenue (JRN, 4110=135) but its COGS never hit the GL — Balance Sheet 1141 stayed at the pre-sale 190,938.67 while the valuation report (ledger/cache) correctly showed 190,849.968, off by exactly the COGS 88.702; P&L had NO 5100 line. Root cause: our own a63b1036/4d2e5605 made the outbox the SOLE COGS path, but OUTBOX_POLLER_ENABLED defaults "false" and was unset in the env, so 2 accounting.post rows sat status='pending' forever (no error, never drained). Before that change the in-process fast-path emit posted COGS immediately even with the poller off; removing it turned a poller-off config into silent COGS loss (ledger>GL). Founder ruling: keep inline as primary, outbox as backup INCASE inline fails | POS sale of a WAC item; check Balance Sheet 1141 vs valuation report + P&L 5100 right after | report = GL 1141, COGS posts, no divergence | FIXED (2046cd49) — (1) RESTORED dual-path COGS delivery: inline emit posts JE immediately + durable outbox row inside tx is the backup the poller drains if inline fails; journal_entries.event_id unique index dedups both legs (no double-post); covers outbound COGS + sale_return reversal + negative-stock true-up; spec asserts BOTH legs. (2) Poller safe-by-default (opt-OUT: runs unless OUTBOX_POLLER_ENABLED explicitly "false" or NODE_ENV=test) so a forgotten env var can never silently drop COGS again. (3) P&L + Balance Sheet now render KWD 3dp (getCurrencyDecimals, screen+CSV; extends #117). accounting+nestjs+frontend APPROVED; api+web typecheck green, 77 api tests pass. Live self-healed once founder set OUTBOX_POLLER_ENABLED=true (poller drained → 1141=190,849.97, P&L 5100=88.70). OPS: keep the env var set until 2046cd49 deploys (then it is the default) |
| 132 | 2026-07-01 | 08 Adjustments (preview) | MEDIUM | Adjustment footer "Total" and confirm dialog "Grand Total" both show 0.000 when the line uses a PACK unit (e.g. Box) instead of the base unit Each — the per-line Total in the confirm dialog is correct (3,275.520) but the aggregate reads 0.000, so a user sees a zero-value adjustment and may distrust/abort or misread it. Root cause: totalValue reduce summed parseFloat(l.quantity), but for a pack line the typed qty lives in l.unitValue.unitQty and l.quantity is empty → parseFloat("")=NaN → line skipped → 0. Works for Each (l.quantity populated). AdjustmentConfirmDialog renders this same totalValue as Grand Total, so both surfaces read 0. Backend correct (posts base_qty×unitCost, #125). Twin of #125/#130 pack-preview class | Adjustments → New → Increase → item with a pack unit, pick Box qty 32 → footer Total 0.000; confirm dialog per-line 3,275.520 but Grand Total 0.000 | footer + grand total = base_qty × unitCost (3,275.520) | FIXED (fdb3eed6) — totalValue derives base qty via computeBaseQty exactly like confirmLines (packActive l.unitValue.unitPackId!==null → computeBaseQty(l.unitValue,l.packUnits), else l.quantity); one fix corrects footer + grand total; Each path unchanged; NaN guard intact. frontend-reviewer APPROVED; web typecheck green |
| 137 | 2026-07-01 | 10 Stock Counts | LOW | Variance / shrinkage report has a backend endpoint (GET /variance-report, service.varianceReport) but NO frontend screen consumes it — "total shrinkage this period" is not surfaced to users | grep web for variance-report | Build a shrinkage report screen (per-item variance across posted counts, date range, warehouse) | OPEN (follow-up — deferred at close; endpoint ready, UI unbuilt) |
| 138 | 2026-07-01 | 10 Stock Counts | LOW | No integer-only guard on countedQty for unit/piece-tracked items — a count of 1.5 pieces is accepted (DTO allows 6dp decimals for all items regardless of tracking/UOM) | Count a piece item as 1.5 | Reject/round non-integer count for integer-UOM items (client + server) | OPEN (follow-up — deferred at close) |
| 139 | 2026-07-01 | 10 Stock Counts | NOTE | Not live-tested for Al-Asala (out of tenant scope: single location, no serial/batch items): serial-scan reconciliation, batch-layer variance, blind-mode hide, server-side permission block, backdated-into-locked-period reject. All code-verified; smoke-test before GA on a tenant that uses them | — | Live smoke test on a serial/batch/multi-user tenant pre-GA | LOGGED (untested, low risk for this tenant) |
| - | 2026-07-01 | 10 Stock Counts | SIGN-OFF | Submodule 10 CLOSED. All CRITICAL/HIGH/MEDIUM fixed & founder-verified live: warehouse picker, 8th-grade tooltips, recovery controls (back/cancel/edit+reopen CAS), drift-aware review (#107), display batch #78-85, zero-cost-found post 500 (#134), save-before-submit (#135), worksheet+review search & recent-on-top (#136). Backend hardening PASS (snapshot-frozen, set-to-counted, idempotent double-finalize, atomic, uncounted=zero-variance-not-zero-stock, zero-WAC found cost, WAC-not-reweighted-on-decrease). Follow-ups #137/#138 deferred; #139 untested-out-of-scope. EXTERNAL DEPENDENCY: count→GL 1141 tie-out validates only once the prod outbox poller is enabled/drained (#120, valuation session) | — | — | CLOSED (2026-07-01) |
| 134 | 2026-07-01 | 10 Stock Counts | CRITICAL | approve-post 500 when a count posts a SURPLUS on a zero-WAC item with no master cost. Count supplies unitCost "0.000000" (physical truth, no cost basis) but the #56 WAC-dilution guard added by a parallel session (66bc9040) rejects any explicit <=0 cost on an increase → throws inside the post tx → whole count rolls back. Two parallel hardening sessions (08 adjustments + 10 counts) edited the shared adjustment path without reconciling. Only surplus/found variance hits it; zero-variance + manual adjustments unaffected | Post a count with a found item at zero cost | Zero-cost found line posts at unit cost 0 | FIXED (b6cc9d2a) — added allowZeroCost carve-out to postAdjustmentInTx→applyIncreasesBatched; count sets it true (physical truth), user adjustments never do so #56 still protects them. Root-caused by independent trace agent; 2 regression specs (posts at 0 with flag; still rejected without); 79 api specs pass |
| 135 | 2026-07-01 | 10 Stock Counts | HIGH/UX | Submit-for-review without hitting Save silently discarded typed counts → count submitted with everything null → variance 0 (phantom). Data-loss trap | Type counts, click Submit for review without Save | Auto-save then submit; block submit if save fails | FIXED (b6cc9d2a) — Submit auto-saves pending counts first (awaited), aborts with clear message if save fails; impossible to submit and lose numbers |
| 136 | 2026-07-01 | 10 Stock Counts | MEDIUM/UX | No way to find a product on the worksheet or review screen except scrolling; just-added items buried | Large count, find one item | Search bar (name/SKU) on worksheet + review; recently-counted items bubble to top | FIXED (b6cc9d2a) — controlled search filter on both screens (composes with variances-only toggle); first-touch of a line pushes it to top (most-recent-first, no focus loss mid-type); en+ar |
| 140 | 2026-07-01 | 11 Serial Numbers | CRITICAL | POS return did NOT flip the serial's status: createReturn() never touched serials, so a returned serialized unit stayed 'sold' forever and could never be resold (founder: "returned SN-AAA-099 but it still shows SOLD"). The SALE_RETURN inbound restocked on-hand but the specific serial was orphaned in 'sold' | POS return a serialized sale, check serial list | sold → available; unit re-enters sellable pool | FIXED (07650cef) — createReturn now calls serialNumbers.release() (sold→available, clears saleDoc) for each returned serialized line INSIDE the return tx (atomic with refund JE + restock); return line also records which serial came back. nestjs-reviewer PASS |
| 141 | 2026-07-01 | 11 Serial Numbers | MEDIUM | POS sell of a DEFECTIVE serial reported "already been sold" instead of "defective" — markSold's 0-row guard threw a hardcoded already-sold message regardless of the real blocking status | POS-sell a serial that is defective (founder: SN-AAA-004) | Message states the ACTUAL state (defective/in transit/returned/sold/not found) | FIXED (07650cef) — markSold re-reads on 0-row update and throws a status-specific message (still fails loud, rolls back pay tx). nestjs-reviewer PASS |
| 142 | 2026-07-01 | 11 Serial Numbers | HIGH | Acquisition cost rendered raw 6dp ("99.000000", no symbol) in the serial list AND detail (numeric(19,6)) — not tenant currency precision (KWD 3dp). Same #93 family, both surfaces | View serial list/detail with a costed serial | KWD 3dp via shared formatCurrency/getCurrencyDecimals | FIXED (07650cef) — list + detail format acq cost at tenant decimals; add-dialog cost input resolves tenant decimals (was defaulting 2dp when currency code hadn't loaded) |
| 143 | 2026-07-01 | 11 Serial Numbers | MEDIUM/UX | Skipped-duplicate note was near-invisible (olive text on cream); vague submit error ("may already exist or cost invalid") didn't tell the user WHICH problem | Type a dup serial / submit an existing serial | Visible alert + decisive error | FIXED (07650cef) — skipped shows an amber alert box with icon (role=alert); submit surfaces the actual server message (existing serial vs bad cost) |
| 144 | 2026-07-01 | 11 Serial Numbers | MEDIUM (feature) | No way to reactivate a serial after marking it defective (founder). Restore path missing | Mark defective, try to make it available again | A restore action defective→available | FIXED (07650cef) — POST /serial-numbers/:id/restore (guarded, idempotent, no ledger — mirrors mark-defective) + "Mark available" action on defective rows/detail with confirmation. 3 service specs; nestjs-reviewer PASS |
| 145 | 2026-07-01 | 11 Serial Numbers | MEDIUM (feature) | No delete/correct path for a mistyped serial (re #129). Deferred because a serial now carries an inbound ledger entry, so delete must post a compensating stock decrease to keep on_hand=Σledger — a proper stock-out, not a row delete | Mistype a serial, try to remove it | Per-serial "remove" that reverses its inbound (1-unit decrease at its acq cost) + retires the serial | OPEN (next batch — needs stock reversal to stay tied) |
