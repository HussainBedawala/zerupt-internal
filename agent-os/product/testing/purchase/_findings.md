# Purchase Module — Findings Log

> Log every real defect found during a live testing pass. One row per finding. Fix by severity; re-run the affected checklist item after each fix and mark it FIXED with the commit.

Severity rubric: see [`../README.md`](../README.md). CRITICAL / HIGH / MEDIUM / LOW.

| # | Date | Submodule | Severity | Summary | Repro (steps → expected vs actual) | Status | Commit |
|---|------|-----------|----------|---------|-------------------------------------|--------|--------|
| 1 | 2026-07-02 | 01-suppliers | HIGH | AP aging empty while Outstanding-payable KPI shows KWD 3,500.000 (opening AP invisible in aging) | On `/purchase`: top KPI "Outstanding payable" = KWD 3,500.000, but "Accounts payable aging" section says "No outstanding payables" → expected: aging reconciles to the 2111 GL figure (3,500.000 across buckets); actual: opening AP posted as an opening journal, not a bill, so the aging report (bill-derived) shows nothing. Self-contradiction on one screen. **Root cause:** `SupplierApBalanceService.agingBuckets` excluded `isOpening=true` bills while the KPI query (`purchase-overview.service.ts`) did not. **Fix:** removed the `isOpening=false` filter so aging includes opening bills → reconciles to KPI + 2111. (Follow-up ticket: consolidate the 2 duplicate AP-aging implementations — `suppliers/supplier-ap-balance` doc-based vs `reports/ap-aging` GL-native.) | FIXED | a5691d08 |
| 2 | 2026-07-02 | 01-suppliers | MEDIUM | No-op save shows false "Supplier updated successfully" toast | Edit supplier → change nothing → Save → expected: no-op rejected / "no changes" message, no empty UPDATE; actual: success toast shown as if updated. | FIXED | a5691d08 |
| 3 | 2026-07-02 | 01-suppliers | MEDIUM | Duplicate-name shows a red error toast AND the confirm dialog (redundant/confusing) | Create supplier with existing name → expected: only the "Duplicate supplier name" confirm dialog (Cancel/Create anyway); actual: a red "Something went wrong…" error toast ALSO fires alongside the dialog. The toast frames an intended guard as an error. | FIXED | a5691d08 |
| 4 | 2026-07-02 | 01-suppliers | MEDIUM | Create form: required vs optional fields not visually distinguished; general UI polish | `/purchase/suppliers/new`: only Name is required but nothing marks it; fields look uniform → clerk can't tell what's mandatory. Expected: clear required markers + cleaner layout. | FIXED | a5691d08 |
| 5 | 2026-07-02 | 01-suppliers | MEDIUM | Secondary-language name label renders mangled ("العربية name") | Create/Edit form secondary-name label shows "العربية name" (Arabic word + literal English "name"). Expected: a clean localized label (e.g. "Name (Arabic)" / "الاسم بالعربية"), via useBilingualLabels(). | FIXED | a5691d08 |
| 6 | 2026-07-02 | 01-suppliers | LOW | Supplier code format SUP-0001 too long/confusing; not locale-aware | Auto-gen code is `SUP-0001` (4-digit). Founder wants shorter, e.g. `SUP-001`, and locale-aware. | FIXED | a5691d08 |
| 7 | 2026-07-02 | 01-suppliers | LOW | Edit form: "Upload image" button not vertically aligned with the avatar | Edit supplier → Image row: button sits misaligned relative to the circular avatar. Cosmetic. | FIXED | a5691d08 |
| 8 | 2026-07-02 | 01-suppliers | MEDIUM | No UI to Block a supplier (only Activate/Deactivate) though backend supports blocked + blockedReason + downstream enforcement | Backend has active/inactive/blocked status machine with block-reason requirement and downstream requireActiveSupplier checks, but the UI exposes only activate/deactivate → block path unreachable from UI. **Verified:** block+reason IS wired end-to-end via the Edit form's Status dropdown (reveals reason field, client+server+DB-CHECK validated). Everyday action = Deactivate; block-with-reason lives in Edit. Kept as-is for simplicity. | RESOLVED (by design) | — |
| 9 | 2026-07-02 | accounting (TB) | LOW | Trial Balance: search bar placed below the date range; should sit to the right of the dates | `/accounting/trial-balance`: search input is on its own row under From/To; move inline to the right of the date range for tighter UI. (Cross-module, found while verifying AP tie-out.) | FIXED | a5691d08 |
| 10 | 2026-07-02 | 03-direct-purchase | HIGH | Zero-cost line silently accepted → corrupts WAC | Recon-confirmed before live test: `unitCost` used `moneySchema` (`.nonnegative()`), so a direct-purchase line with `unitCost = 0` was accepted, posting a zero-cost receipt into stock and permanently diluting the item's weighted-average cost for every future sale. Negative cost was already rejected; zero was the gap. **Fix:** added `positiveUnitCostSchema` (`parseFloat > 0`, message "Unit cost must be greater than zero") in `direct-purchase.dto.ts`, mirroring inventory BUG #56 stock-adjustments guard. Server now rejects 0 cleanly at validation. | FIXED | a4b9d4c7 |
| 17 | 2026-07-02 | 03-direct-purchase | MEDIUM | Branch not pre-filled; forces a needless pick on a single-branch shop | `/purchase/direct/new`: Branch dropdown started empty, forcing every entry to select it even though Al-Asala has one branch. **Fix:** auto-select the first active branch on load (user can still change). | FIXED | 9427be05 |
| 18 | 2026-07-02 | 03-direct-purchase | LOW | Branch/bank pickers were plain Selects; inconsistent with supplier combobox | Founder wants one consistent picker. **Fix:** extracted a reusable, generic `SearchableCombobox` (`@/components/searchable-combobox`, accessor-driven, optional labels, clearable) and used it for supplier + branch + bank account. Drop-in for any screen needing a searchable single-select. | FIXED | 9427be05 |
| 19 | 2026-07-02 | 03-direct-purchase | MEDIUM | Default unit cost shows 6dp (2.590000) instead of tenant currency precision | Picking/seeding an item populated unit cost from the raw 6dp MONEY_SCALE cost price, so the input read `2.590000` for a KWD (3dp) tenant. **Fix:** `toCurrencyCost()` normalizes to `getCurrencyDecimals(currencyCode)` on pick and seed (blur already normalized). | FIXED | 9427be05 |
| 20 | 2026-07-02 | 03-direct-purchase | LOW | Payment/settlement section polish | Redesigned settlement into a bordered card: Paid/Credit as radio choice-cards with helper text, segmented payment-method control, combobox bank picker, and a plain-language summary ("Paying KWD X now." / "KWD X added to the supplier's account."). | FIXED | 9427be05 |
| — | 2026-07-02 | 01-suppliers | INFO | Edit scope confirmed OK by design | Question raised: should Edit let users change invoices/documents? Answer: NO — edit is master details only; posted documents are immutable. No change needed. | RESOLVED | — |
| 11 | 2026-07-02 | 02-purchase-orders | MEDIUM | Non-positive line qty gave no visible feedback | Recon corrected severity: server already rejects qty<=0 (`quantitySchema.positive()` in dto) so the `-5` in the screenshot was never committed — but the client showed `-5` in the box with NO inline message, so it looked accepted (confusing). **Fix:** inline error under qty (and price) inputs when non-positive/invalid, clearing when valid. | FIXED | 807af64f |
| 12 | 2026-07-02 | 02-purchase-orders | HIGH | On-order / incoming qty is not tracked at all (feature never built), so nothing to surface | Recon: PO confirm/cancel DO emit `purchase.order.confirmed`/`.cancelled` (durable outbox), but **no inventory listener consumes them**, there is **no `on_order_qty` column** in schema, and the "onOrder" mentions in `inventory.constants.ts`/`purchase-orders-events.ts` are aspirational comments only. So the events fire into the void. Correct fix = full lifecycle feature: schema column + inventory listener (confirm +, cancel −, **GRN receipt −**, GRN void +, short-close abandon −) + include in stock GET + UI. **DEFERRED to the GRN/inventory build (submodule 04):** shipping only confirm/cancel now would overstate on-order after the first goods receipt (a wrong number) — violates never-show-wrong-numbers. Tracked as a dedicated feature, not a same-session fix. | DEFERRED | — |
| 13 | 2026-07-02 | 02-purchase-orders | MEDIUM | Two-screen flow: line items only addable AFTER draft is created (confusing/misleading for new users) | `/purchase/orders/new` collects header only + "Create draft"; lines are added on a separate detail screen. Founder: merge into one screen. Decision: single-screen frontend orchestration (create-draft → add-lines behind one "Create order" button); backend unchanged. | FIXED | 807af64f |
| 14 | 2026-07-02 | 02-purchase-orders | MEDIUM | Line qty & unit price inputs show 6 decimals (2.590000 / 10.00000); editing doesn't normalize to currency precision | Detail line inputs display 6dp raw MONEY_SCALE; typing a value doesn't round/format to KWD 3dp (price) on blur. Expected: price 3dp, qty sensible precision, normalize on blur. | FIXED | 807af64f |
| 15 | 2026-07-02 | 02-purchase-orders | MEDIUM | Draft shows full `DRAFT-<uuid>` in breadcrumb + title | Detail header reads "Order DRAFT-c2ef32bf-8ca6-...". A clerk shouldn't see a raw UUID as "the number". Show friendly "New order (Draft)" / hide the uuid; real PO- shows only after confirm. | FIXED | 807af64f |
| 16 | 2026-07-02 | 02-purchase-orders | MEDIUM | Cancel/Confirm buttons placed under the title, not top-right | Draft detail has Cancel order / Confirm order below the title; confirmed detail already has actions top-right (Print/Receive/Cancel). Make draft actions top-right too for consistent UX. | FIXED | 807af64f |
| 17 | 2026-07-02 | 02-purchase-orders | MEDIUM | Supplier picker is a plain native dropdown — can't type / can't search by code | Create form supplier field opens a static list; unlike item search you can't type to filter or search by supplier code. Make it a searchable combobox (matches code + name), matching the item-search pattern. | FIXED | 807af64f |
| 18 | 2026-07-02 | 02-purchase-orders | MEDIUM | No Close / short-close button on a confirmed PO | Confirmed PO detail shows only Print / Receive goods / Cancel order — no Close. Checklist requires close from confirmed (short-close) and received. Add a Close button with a clear "remaining qty will be abandoned" explanation. | FIXED | 807af64f |
| 19 | 2026-07-02 | 02-purchase-orders | MEDIUM | Arabic locale renders amounts in Arabic-Indic digits (٢٥,٩٠٠) + wrong grouping/precision, inconsistent with rest of app | PO list in Arabic shows ٢٥,٩٠٠ and ٠,٠٠٠ (2dp comma) while other screens use Western digits + KWD 3dp. Decision: force Western digits + correct KWD 3dp formatting consistently on PO screens. | FIXED | 807af64f |
| 20 | 2026-07-02 | 02-purchase-orders | MEDIUM | Pack-unit line control (Box / =24 Each) styling is out of place; likely shared component affecting all screens | The stacked pack picker + qty box looks misaligned/cramped on the line row and founder reports the same look on other screens. Restyle the shared LineUnitPicker for clean alignment. | FIXED | 807af64f |
| 21 | 2026-07-02 | 02-purchase-orders | MEDIUM | Confirm dialog closes on error, losing typed approval fields | On confirm failure (wrong PIN / over-threshold) the AlertDialog closes then toasts error; user loses approvedBy/PIN and must reopen + retype. Keep dialog open with inline error. | FIXED | 807af64f |
| 22 | 2026-07-02 | 02-purchase-orders | MEDIUM | List supplier filter missing; search only filters current page | List has status filter + a text box but no supplier dropdown; text search filters only the already-fetched page, so a PO on page 2 isn't found. Wire supplier filter + server-side search. | FIXED | 807af64f |
| 23 | 2026-07-02 | 02-purchase-orders | LOW | Total of added lines not prominently shown while adding | Founder didn't see a running total near the lines (summary card exists above). Ensure a lines-footer/total is visible where items are added. | FIXED | 807af64f |
| 24 | 2026-07-02 | 02-purchase-orders | LOW | No CSV export on the PO list | List/report screens should offer CSV export per systemic finding #6; PO list has none. | FIXED | 807af64f |
| 25 | 2026-07-02 | 06-landed-costs | LOW | No CSV export on the landed-costs list | `/purchase/landed-costs`: list had no CSV export button (systemic finding #6). **Fix:** added Export CSV button mirroring the PO-list pattern (#24) — paginated fetch, RFC-4180 escaping, UTF-8 BOM, capped/empty/error toasts; new testid `landedCosts.exportButton`; en+ar keys. | FIXED | b0da7e05 |
| 26 | 2026-07-02 | 06-landed-costs | MEDIUM | GRN selection is a plain scroll-through checkbox list; no search/filter, feels inconsistent | `/purchase/landed-costs/new`: confirmed-GRN targets picked from a raw scroll list, no search, layout off vs pickers used in 02/03. **Fix:** added a search Input (client-side filter over the fetched confirmed GRNs) + swapped the hand-rolled checkbox list for the shared `MultiSelectList` primitive (citron-accent, bidi-safe, already used in serial-pickers), extended with an optional rich `content` node to keep the number/date/total row layout. en+ar keys. | FIXED | b0da7e05 |
| 27 | 2026-07-02 | 06-landed-costs | MEDIUM | Create is a two-screen flow (create draft → add components on detail) — same confusing pattern fixed on PO (#13) | `/purchase/landed-costs/new` collected only the header + GRNs, then bounced to the detail page to add the freight/duty charges. Founder: merge into one screen. **Fix:** create panel now has an inline charges editor (add/remove charges in local state with running total, allocation-method + credit-account per charge); one "Create landed cost" button orchestrates create-draft → sequential add-component (backend unchanged) then navigates to the draft detail for review + Post. Post stays a deliberate separate step (irreversible). Manual allocation intentionally stays on the detail draft (it references GRN line ids only addressable after the draft exists). Also replaced the free-text credit-entity UUID box with the reusable `SupplierCombobox` for the payable/freight-forwarder case (systemic #4). | FIXED | b0da7e05 |
| 28 | 2026-07-02 | 06-landed-costs | MEDIUM | `by_weight` allocation shown as "(unsupported)" and omitted from the method picker — but backend fully supports it | The allocation-method label read "By weight (unsupported)" (en) / "بالوزن (غير مدعوم)" (ar) and the detail add-component picker hard-excluded `by_weight` with a stale "API rejects it" comment. Recon + code read confirm the backend fully supports `by_weight` (basis = qty × item.weightKg) and fails loud with a clear message if a targeted item has no weight. **Fix:** corrected both labels, added `by_weight` to the detail picker and the new create-screen picker, removed the stale comment. | FIXED | b0da7e05 |
| 29 | 2026-07-02 | 06-landed-costs | LOW | Stale code comments claim landed-cost accrual posts to 2121 (GRN accrual) — actual account is 2122 | Three comments (`landed-costs.events.ts`, `account-mapping-defaults.ts`, `purchase-accounting.listener.ts`) said "accrual → 2121". Verified against the mapping table: `purchase.landedCost.allocated` / `landed_cost_accrual` → **2122** (distinct from GRN's 2121), correct per spec. Comments were misleading (conflation risk the checklist explicitly warns about). **Fix:** corrected all three comments to 2122; no logic change (code was already correct). | FIXED | b0da7e05 |
| 30 | 2026-07-02 | 06-landed-costs | CRITICAL | Posting ANY landed cost 500s — `document_type` enum has no `'lc'` value in any DB | Live: Post an LC draft → 500. `DocNumberingService.reserveNumber` runs `SELECT … FROM document_sequences … document_type = 'lc' FOR UPDATE` → Postgres `invalid input value for enum document_type: "lc"` (22P02). **Root cause:** `'lc'`/`'pr'` were added to the Drizzle schema enum (`enums.ts`) but the `ALTER TYPE … ADD VALUE` was NEVER emitted — an unrelated migration (0046) baked the members into its schema snapshot while they sat in `enums.ts`, so `drizzle-kit generate` has since believed they exist and produced no DDL. No dev/prod DB ever got them; LC posting (first `'lc'` consumer) fails, and purchase returns (`'pr'`, submodule 08) would fail identically. **Fix:** authored migration `0146_purchase_lc_pr_document_types.sql` via drizzle `generate --custom` (journal `when` tool-stamped, per never-hand-edit-journal rule) — `ALTER TYPE document_type ADD VALUE IF NOT EXISTS 'lc'` + `'pr'`. Idempotent; Railway pre-deploy propagates to all prod tenants; applied to the live Al-Asala tenant DB for immediate re-test. | FIXED | 32fa2f8d |
| 31 | 2026-07-03 | 06-landed-costs | CRITICAL | After the enum fix, posting an LC still 500s: "No sequence configured for document type LC" | Post LC draft → 500 `NotFoundException: No sequence configured for document type LC`. **Root cause:** the onboarding doc-numbering materializer (`materialize-doc-numbering.ts`) `SEED_DOC_TYPES` never included `LC` (or `PR`) — so the per-branch `document_sequences` row was never created, and `reserveNumber` throws (no lazy create). The file's own comment warns "any reserved-but-unseeded type breaks its posting path for every tenant." Same latent break for purchase returns (`PR`, submodule 08). **Fix (reuse, not new code):** switched `LandedCostsService.post` + `PurchaseReturnsService.confirm` from `reserveNumber` → the existing self-heal `reserveOrSeedNumber` (seeds the sequence with the canonical branch prefix on first use); extended each service's `resolveBranchContext` to also return `branches.code` for the prefix. Added `LC`/`PR` to `SEED_DOC_TYPES` so new tenants seed upfront. | FIXED | 162a29d1 |
| 32 | 2026-07-03 | 06-landed-costs | MEDIUM | Two error toasts on a failed post (and other LC mutations) | Post failure showed both "No sequence configured…" and "Something went wrong: No sequence configured…". **Root cause:** the detail panel handles errors at the mutation call site (`mutate(..., { onError })`), which the global `MutationCache.onError` can't see, so it fires its own toast too. **Fix:** added `meta: { suppressGlobalErrorToast: true }` to all six landed-cost mutation hooks (the documented opt-out for call-site error handling). | FIXED | 162a29d1 |
| 33 | 2026-07-03 | 06-landed-costs | LOW | Charge amount on the create screen not shown at currency precision (e.g. "20" not "20.000") | The inline charge Amount input kept the raw typed value. **Fix:** on-blur normaliser reusing `formatToDecimals` + `getCurrencyDecimals` (KWD → 3dp); the added-charge list + detail already formatted via `formatMoneyAmount`. | FIXED | 162a29d1 |
| 34 | 2026-07-03 | 06-landed-costs | LOW | Draft not fully editable — no way to change header (date/GRNs/notes) after create; user asked for an "Edit draft" affordance | Components ARE editable on the draft detail (pencil/trash + Add component), but header fields (documentDate, targetGrnIds, notes) are immutable post-create because there is no backend PATCH for the LC header. **DEFERRED:** needs a new `PATCH landed-costs/:id` (header, draft-only) + wiring — a genuine feature, not a same-session trivial fix, on a rarely-used screen (persona note). Tracked for a follow-up; component-level edits cover the common case (fixing a charge). | DEFERRED | — |
| 04-1 | 2026-07-02 | 04-grn | MEDIUM | GRN "Lines to receive": Batch/Expiry/Serial columns shown for non-tracked items + column meanings unclear | `/purchase/grns/new`: Batch, Expiry date, and Serial-numbers inputs rendered for every line regardless of the item's tracking type — for plain "each" items (tracking=none, all of Al-Asala) they're pure noise/confusing. Also Ordered / Already received / Remaining / Qty to receive were unexplained, and editing "Qty to receive" doesn't move "Already received" (correct, but looks broken to a clerk). **Fix:** hide the batch/expiry columns unless ≥1 line is batch-tracked and the serial column unless ≥1 is serial-tracked (per-row `—` for untracked lines in a mixed PO); added an always-visible plain-language guide box (8th-grade wording) defining all four qty columns + header hover hints; wording makes clear Qty-to-receive is the only number the clerk changes. en+ar parity, web typecheck green. | FIXED | 8de2d9ee |
| 25 | 2026-07-02 | 05-invoices | MEDIUM | Bill detail: 2dp summary + 6dp line inputs (not tenant precision); raw DRAFT-uuid title/breadcrumb | Bill draft showed Subtotal/Tax/Total/Balance at 2dp and qty/price inputs at 6dp; title + breadcrumb showed `DRAFT-c4d8...`. **Fix:** currency-aware 3dp everywhere via `displayMoneyByCurrency`; normalize qty/price on blur; friendly "New bill" for drafts. | FIXED | ddc89f58 |
| 26 | 2026-07-02 | 05-invoices | LOW | "Add expense / service line" button had no explanation | Founder confused what it does. **Fix:** InfoHint explaining it adds a non-stock cost (freight/customs/service fee) posting to a chosen expense/cost account, not inventory. | FIXED | ddc89f58 |
| 27 | 2026-07-02 | 02-purchase-orders | MEDIUM | PO **create** line editor still 6dp / no normalize (807af64f only fixed detail) | `/purchase/orders/new` unit price seeded at 6dp and no blur normalization. **Fix:** seed + normalize to tenant precision, mirroring direct-purchase. | FIXED | ddc89f58 |
| 28 | 2026-07-02 | 09-ap-aging | MEDIUM | AP aging table hardcoded 2dp + manual currency suffix | `/purchase` overview aging showed `1,100.00 KWD`. **Fix:** currency-aware `displayMoneyByCurrency` → 3dp. | FIXED | ddc89f58 |
| 29 | 2026-07-02 | 03-direct-purchase | MEDIUM | No way to see direct purchases made (route just redirected to /new) | Founder could see POs but not DPs. **Fix:** new list at `/purchase/direct` (API list endpoint + query hook + panel: date/supplier/bill#/GRN#/status/total, row→bill). | FIXED | ddc89f58 |
| 30 | 2026-07-02 | 04-grn | MEDIUM | GRN detail: batch/expiry columns for untracked items + header actions spread by justify-between | Void/Print/Create-bill spread across the row; batch/expiry columns always shown. **Fix:** group actions top-right; hide batch/expiry unless a line carries tracking. | FIXED | 8a9be57e |
| 31 | 2026-07-02 | cross-cutting | MEDIUM | Spurious red "Request validation failed" toast alongside real success (GRN confirm) | Global `QueryCache.onError` toasted every background-refetch error after a broad `invalidateQueries` (e.g. GRN confirm). **Fix:** don't toast when the query already has cached data — only initial-load failures toast; inline isError still surfaces real errors. App-wide. | FIXED | 8a9be57e |
| 32 | 2026-07-02 | inventory (batches) | LOW | `item_batches.qty_remaining` had no DB-level non-negative guard | Service validated but no DB backstop for the Σ(batch)=on-hand invariant. **Fix:** CHECK `qty_remaining >= 0` (migration 0145). | FIXED | 292917ec |
| 07-1 | 2026-07-02 | 07-supplier-payments | LOW | Payment-type toggle rendered as two detached outline buttons, off-brand | `/purchase/payments/new`: Standard/Advance were two separate `Button`s with a gap, not a segmented control. **Fix:** proper on-brand segmented toggle (muted track, ink-on-cream active chip with shadow, role=tablist/tab). | FIXED | c0ad5554 |
| 07-2 | 2026-07-02 | 07-supplier-payments | MEDIUM | Branch picker used raw shadcn `Select` (off-brand full-width citron highlight), not the shared component | Inconsistent with the supplier picker (SearchableCombobox) on the same form. **Fix:** branch now uses shared `SearchableCombobox` (bidi-safe, subtle accent hover, auto-search when >6 branches). Systemic #4. | FIXED | c0ad5554 |
| 07-3 | 2026-07-02 | 07-supplier-payments | MEDIUM | Amount inputs showed raw 6dp (`24.500000`) and didn't normalize to country precision on edit | Pay-full seeded the raw API balance; typed amounts/discount/advance stayed at whatever precision. **Fix:** `toCurrencyAmount` via `getCurrencyDecimals`/`formatToDecimals`; pay-full + onBlur on allocation/discount/advance inputs normalize to tenant precision (KWD 3dp). Systemic #2. | FIXED | c0ad5554 |
| 07-4 | 2026-07-02 | 07-supplier-payments | MEDIUM | Bank/cheque payment felt like it was missing a field — no way to choose which bank on the create screen | The specific cash/bank GL account is (correctly) chosen at post time in PostPaymentDialog, but the create form gave no indication, reading as a dead-end. **Fix:** inline hint under Method for bank_transfer/cheque: "The specific bank account is chosen when you post this payment." (backend/GL flow unchanged — account belongs to the posting event). | FIXED | c0ad5554 |
| 07-5 | 2026-07-02 | 07-supplier-payments | MEDIUM | Payments list amount hardcoded to 2dp (`displayMoney`) — wrong for KWD | `payments-list-panel.tsx` formatted the total at 2dp regardless of tenant currency. **Fix:** `displayMoneyByCurrency` + `useTenantCurrency` → 3dp for KWD. Systemic #2. | FIXED | c0ad5554 |
| 33 | 2026-07-03 | 05-invoices | MEDIUM | New-bill form used plain shadcn `Select` for supplier & branch (not the reusable searchable comboboxes) | `/purchase/invoices/new`: supplier and branch were static dropdowns (can't type / search by code), inconsistent with orders/new which uses `SupplierCombobox` + `BranchCombobox`. **Fix:** swapped both to the reusable comboboxes (self-fetching, searchable, code+name match), mirroring order-create-panel. | FIXED | 37587850 |
| 34 | 2026-07-03 | 05-invoices | MEDIUM | Bill create was a two-screen flow (create header draft → redirect to detail to add lines) | Same confusing pattern already fixed on PO (#13) and landed-costs (#27): user saves a header, gets bounced to another screen to add items, feels like "did it save / where are my items". **Fix:** merged into one screen via client orchestration (create-draft → loop add item/expense lines behind one "Create bill" button; backend unchanged); empty draft still allowed (add later); partial-failure redirects to the draft so nothing is lost. Confirm/void stay deliberate separate steps. | FIXED | 37587850 |
| 07-6 | 2026-07-03 | 07-supplier-payments | HIGH | Solo owner can NEVER reverse a payment — reversal hard-required a second approver | Reverse dialog forced an "Approving manager" dropdown that read "No other active users to approve"; `reversePaymentSchema` hardcoded approvedBy/approvalPin required + `reverse()` called `verifyApproval` unconditionally, unlike post/allocate which gate on tenant `requirePaymentApproval` (default off). **Fix:** reverse now routes through `maybeVerifyApproval` (approvedBy/pin optional in DTO); `ReversePaymentDialog` takes a `requireApproval` prop and only shows PIN fields when the tenant requires it; `reversalApprovedBy` written `?? null`. Security-reviewed: no weakening when approval ON (full SoD+PIN+permission preserved); server re-derives the setting so no trust-boundary bypass; audit intact (`reversedBy` always recorded). | FIXED | 13a07c5e |
| 07-7 | 2026-07-03 | 07-supplier-payments | MEDIUM | Post dialog defaulted a bank transfer to Petty Cash (1111) + listed all cash accounts + raw Select | For method=bank_transfer the account picker auto-selected `accounts[0]` (code-sorted → 1111 Petty Cash) and listed every cash-equivalent account. **Fix:** filter the list by the tender's `group` (bank_transfer→bank; cheque→cheque+bank), default to a bank account, fall back to full list if empty; swapped the raw `Select` for the shared `SearchableCombobox`. Updated unit test to assert the bank default. | FIXED | 13a07c5e |
| 07-8 | 2026-07-03 | 07-supplier-payments | LOW | Payment date didn't default to today — clerk forced to open the date picker | **Fix:** create form seeds paymentDate to today's date. | FIXED | 13a07c5e |
| 07-9 | 2026-07-03 | 07-supplier-payments | LOW | Method dropdown (and branch) not using the shared picker component | **Fix:** method now uses the shared `SearchableCombobox` (searchable off); branch already converted in 07-2. Supplier + branch + method + bank-account all use the one component now. | FIXED | 13a07c5e |
| 07-10 | 2026-07-03 | 07-supplier-payments | MEDIUM | Bank-account hint + early-payment discount confusing; total didn't reflect discount | Create hint implied a bank was already chosen; discount field didn't say amount-vs-percent and the payment total didn't change when a discount was typed. **Fix:** reworded bank hint to plain "you'll choose which account the money comes out of when you post"; discount label gets an `InfoHint` (it's an amount, reduces the bill, not cash out) + 8th-grade hint; added a live "Cleared from bills" breakdown (cash + discount) so the effect is visible. | FIXED | 13a07c5e |
| 07-11 | 2026-07-03 | 07-supplier-payments | LOW | Draft payment showed raw `DRAFT-<uuid>` title, which wrapped the header and pushed the Post button below the title | **Fix:** drafts show a friendly "New payment (draft)" title + breadcrumb (mirrors the bills fix); the short title keeps the Post/Reverse actions top-right where users click. | FIXED | 13a07c5e |
| 35 | 2026-07-03 | 05-invoices | CRITICAL | From-GRN bill confirm WITH a price variance silently posts NO journal (missing 5210 mapping + swallowed error) | Confirming a GRN-matched bill whose billed price ≠ frozen receipt cost (a real price variance) produces **no journal at all**, yet the `purchase.invoice.confirmed` outbox row is marked `completed` with no error. Proven on prod: PINV-200005 (SUP-0001, GRN-200005 FLT-0002 ×4 billed @3.250 vs cost 3.000; payload accrualCleared 12.000 / priceVariance 1.000 / payable 13.000) → 0 journal rows, 2121 still holds the uncleared −12.000, SUP-0001 party 2111 short by 13.000, reconcile invariant broken. Standalone bill same session posted fine; no event_id dedup. **CONFIRMED ROOT CAUSE (empirical, prod + code):** (1) SEED DRIFT — the `account_mappings` row `(purchase.invoice.confirmed, purchase_variance, 5210)` is MISSING for this tenant. The default EXISTS in code (`account-mapping-defaults.ts:161`) but the tenant was provisioned before it and never backfilled; DB only has `purchase_variance` under `purchase.return.confirmed`. A bill-confirm with a variance adds the `purchase_variance` line type → `resolveAccountsBatch` throws NotFoundException. A matched bill billed AT cost (no variance) omits that line type and posts fine. (2) SILENT-LOSS — the bill JE posts via the listener's fire-and-forget `emit("accounting.post")` whose handler swallows the error (`journal-posting.service.ts:769-790`); the poller marks the DOMAIN outbox row `completed` right after `emitAsync` (`outbox-poller.service.ts:403-406`) regardless of JE success; NO durable `accounting.post` row exists for this path (verified) so the "durable backstop" is false; the drift monitor only checks `accounting.post` rows (`452-456`) so it can't catch it. **PERMANENT FIX = (A) run `BackfillAccountMappingsService` fleet-wide (idempotent; inserts the missing default into every drifted tenant; new tenants already covered at provisioning); (B) reset PINV-200005's completed domain outbox row to pending to replay (idempotent by eventId); (C) make domain-event JE posting fail LOUD — await the post so failures mark the row failed→retry→dead-letter, + extend drift monitor to domain rows.** **PROGRESS:** (A) DONE+verified — backfilled 14 missing default mappings into Al-Asala (incl. the culprit + the entire `purchase.invoice.voided` set, a latent twin), Merpec 0 drift; faithful to the tested seed service (skipped COA-absent reverse-charge/PDC accounts). (B) DONE+verified — reset PINV-200005's outbox row, restarted the prod API poller, JE `B1SHUWAIKH-JRN-200042` posted (Dr 2121 12 / Dr 5210 1 / Cr 2111 SUP-0001 13); reconcile now HOLDS exactly (SUP-0001 GL 2111 = subledger = 2,419.000; SUP-0002 = 1,235.900). (C) fail-loud pipeline hardening IN PROGRESS (subagent). Also surfaced: the prod poll loop had gone silent on the running container (no outbox activity since 11:46 despite an eligible row) — a fresh boot drained it; worth a resilience look. | FIXED | A+B prod-data (Neon) · C 8fcf2b86 |
| 36 | 2026-07-03 | cross-cutting | MEDIUM | `GET /tenant/items?limit=200` → 400 (server caps limit at 100) breaks GRN print/detail + 4 other screens | Frontend requests `limit: 200` at 5 sites (GRN print doc, customer form price-list, 2 serial pickers, price-lists panel) but items list DTO caps `limit` at 100 → 400 validation_error, breaking those screens. App-wide convention is client requests ≤100 and narrows via filters. **Permanent fix (not just a 100-clamp, which would silently truncate >100-row tenants):** per-site scope/paginate — GRN print fetches only its own line items; serial pickers scope by item/warehouse + paginate to completeness; price-lists panel + customer picker paginate all pages. | FIXED | 29f32bd4 |
| 07-12 | 2026-07-03 | 05-invoices (expense line) | LOW | Bill "Expense / service line" Amount input didn't normalize to currency precision (showed raw `2` not `2.000`) | The expense Amount field lacked the on-blur currency-precision normalization that the item unit-price input right beside it already had. **Fix:** one-line reuse of the file's existing `toCurrencyPrice` helper on the Amount `onBlur`. Qty left as a plain count (it's a multiplier, not money). | FIXED | 08c6b2d7 |
| 37 | 2026-07-03 | 02-purchase-orders | MEDIUM | PO create line editor shows qty/unit price at 6dp by default (normalizes only on edit) | `/purchase/orders/new`: ordered qty and unit price render raw 6dp (e.g. 2.590000) on seed; correct only after the user edits. Same class as #14/#19/#27. **Fix:** seed/format to tenant precision from the start (price at currency decimals via currency-precision util, qty sensible) reusing the existing normalizer, so the default display is correct. | FIXED | fb7afcc8 |
| 38 | 2026-07-03 | 02-purchase-orders | LOW | Confirmed-PO detail action buttons not grouped top-right | Receive goods / Close order / Cancel / Print spread in the header instead of a clean top-right action group (inconsistent with GRN detail #30 + PO draft #16). **Fix:** group actions top-right, RTL-safe. | RESOLVED (already conformant) | — |
| 39 | 2026-07-03 | 02-purchase-orders | LOW | "Receive goods" vs "Close order" meaning unclear; no explanation | Clerk cannot tell the two apart. **Fix:** InfoHint (reuse shared component) on each in plain language — Receive = record arrived stock (can be partial, adds to inventory); Close = finish without the rest, no stock added. | FIXED | fb7afcc8 |
| 40 | 2026-07-03 | 05-invoices | LOW | Bill-from-GRN lets you add extra stock item lines with no explanation of the accounting effect | Adding a NEW stock line (not a matched GRN line) on a GRN-linked bill posts straight to Inventory 1141 (unmatched remainder), not GR/IR clearing. Supported + industry-normal for extra billed items, but a clerk may not realize it bypasses the receipt match (double-count risk). **Fix:** InfoHint clarifying the add-item control on GRN-linked bills (does not block; backend unchanged). Expense/service-line add (#26) stays. | FIXED | fb7afcc8 |
| 41 | 2026-07-03 | 05-invoices | HIGH | Bill VOID hard-requires manager PIN + separation-of-duties → a solo owner / single-user tenant can NEVER void a bill | Void dialog forces an approving-manager + PIN and `voidBill` calls `verifyApproval` with `requiredPermission: "purchase.bill.approve"` unconditionally, requiring a SECOND user — impossible for a solo owner (same class as payments-reversal 07-6). **Fix (mirror 13a07c5e):** route void approval through `maybeVerifyApproval` gated on a server-authoritative tenant setting (default OFF) so a solo owner voids with no PIN/second-approver; when a business turns approval ON, full SoD+PIN+`purchase.bill.approve` preserved. Void dialog takes a `requireApproval` prop, only shows PIN fields when required. All other void guards (blocked-if-paid row-lock, period gate, idempotent double-void, GRN billedQty decrement, immutable `voidedBy` audit) unchanged. | FIXED | fb7afcc8 |
| 07-13 | 2026-07-03 | 07-supplier-payments | MEDIUM | No way to fix/discard a wrong draft payment; advance "how do I allocate?" copy pointed at a non-existent button | (a) A draft created with the wrong method/allocations was a dead-end — no edit/delete. **Fix:** `DELETE /tenant/purchase/payments/:id` (draft only, hard-deletes the unposted payment + allocation rows; posted/reversed stay immutable) + a "Discard" button + confirm on the draft detail, returning to the list. Discard-and-recreate is the clean path (vs a heavier inline edit). (b) A **draft** advance showed "Apply this advance to bills using the button above" but the Apply button only appears after posting. **Fix:** status-aware empty state — a draft advance now says post first, then the apply button appears. | FIXED | 6e041c14 |
| 42 | 2026-07-03 | 05-invoices | LOW | Bill detail shows "voided by" (and likely confirmed/created by) as a raw internal user UUID instead of the person's name | Void a bill → detail shows the voider as a UUID. Should resolve to the user's display name/email (as other document headers should). **Fix:** resolve voidedBy to the user's full name/email via the existing useUsersQuery lookup (confirmed/created-by not shown here). | FIXED | 0de18999 |
| 07-13 | 2026-07-03 | 07-supplier-payments | HIGH | Payment REVERSAL posts no GL contra → 2111 understated; reconcile breaks | Reversing supplier payment PAY-200004 (6.000) set the payment `status=reversed` + reset the bill's paid_amount, but the `purchase.payment.reversed` outbox event is **`status=failed`, attempts=4**, `last_error: "Invalid event payload: expected string, received undefined … expected array, received undefined"` — so the reversal contra JE (restore Cr 2111 / Dr bank) never posted. Result: SUP-0001 GL 2111 (2,356.000) < party subledger (2,362.000) by exactly 6.000. **Fail-loud IS working (dead-lettering, not silent).** Root cause = malformed reversal event payload in the supplier-payments reversal path (missing a required string + a `lines` array). **Owner: the live 07 payments session editing `supplier-payments.service.ts`.** Fix the payload construction, then replay the dead-lettered event to post the contra and restore reconcile. NOT caused by 05 void (that JE is a clean contra). | HANDOFF to 07 session | — |
| 07-14 | 2026-07-03 | 07-supplier-payments | LOW | "Apply advance to bills" dialog Pay-amount inputs didn't normalize to currency precision (showed `12` not `12.000`) | The advance-allocation dialog's amount inputs lacked the on-blur currency-precision normalization the create form + post dialog already have. **Fix:** added `toCurrencyAmount` on-blur (KWD 3dp) to those inputs. | FIXED | 2921e3fc |
| 43 | 2026-07-03 | 05-invoices | MEDIUM | Duplicate supplier-invoice-number on create leaked a raw 500 + ERROR-log stack instead of the friendly 409 | Test 3: new bill for SUP-0001 with an already-used ref (GP-2026-118) IS blocked by the DB unique index, but `isUniqueViolation` only checked top-level `error.code`; Drizzle 0.45 wraps the pg error in a DrizzleQueryError with the 23505 code on `.cause`, so the check missed it, the friendly-409 branch was skipped, and the raw DrizzleQueryError propagated (500 + noisy ERROR stack; user got a generic error, not "already exists"). No orphan draft created (insert failed atomically). **Fix:** `isUniqueViolation` walks the `.cause` chain (bounded) so wrapped violations map to `409 DUPLICATE_SUPPLIER_INVOICE` on both create + fromGrn; unit test added for the wrapped case. | FIXED | 0a8da401 |
| — | 2026-07-03 | 05-invoices | SIGN-OFF | **Submodule 05 (Purchase Invoices / Bills) SIGNED OFF** | Live prod tie-outs all verified: Test 1 from-GRN variance bill posted Dr 2121 / Dr 5210 / Cr 2111 correctly (JRN-200044, reconcile exact); Test 2 void posted exact net-zero contra + re-opened GRN billedQty + solo-owner (no PIN); Test 3 duplicate rejected per-supplier, allowed cross-supplier. Findings #33/#34/#35/#36/#37/#39/#40/#41/#42/#43 FIXED, #38 already conformant. CRITICAL #35 (silent no-post on matched-variance bills) root-caused + fleet-backfilled + PINV-200005 repaired + fail-loud pipeline hardening shipped (8fcf2b86). Carryover to 07: payment-reversal GL contra bug (07-13) handed off. | SIGNED OFF | — |
