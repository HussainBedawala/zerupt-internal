# Inventory — Testing Findings Log

> Running log of issues found during live testing. One row per finding. Update status as fixed.
> Severity rubric: see [`../README.md`](../README.md). Fix CRITICAL/HIGH immediately; batch MEDIUM/LOW for review.

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| - | 2026-06-29 | (baseline) | PASS | Ledger-first invariant holds: on_hand = Σ stock_ledger_entries per item/location (149/149 pairs, 0 mismatch) | DB recon | on_hand ties to ledger | VERIFIED PASS |
| - | 2026-06-29 | (baseline) | PASS | Valuation ties: Σ total_value = Σ(on_hand×average_cost) = inventory control acct 1141 GL balance (all KWD, 3dp, no negatives) | DB recon | valuation = GL control | VERIFIED PASS |
| 1 | 2026-06-29 | 01 Items/Catalog | MEDIUM | Items list has NO page header/title; screen jumps straight to the toolbar | Open `/inventory/items` | A page header ("Items" + subtitle) like other modules | FIXED (committed 2e8b0e53/88b803e4) — header added to list + form; re-verify live per playbook 01 |
| 2 | 2026-06-29 | 01 Items/Catalog | MEDIUM | Cost/Selling Price render at 2dp ("8.53") not the tenant currency precision (KWD = 3dp "8.530") | Items list | Dynamic precision via shared currency-precision util | FIXED (committed) — items-table reuses getCurrencyDecimals + useTenantCurrency |
| 3 | 2026-06-29 | 01 Items/Catalog | HIGH | New/Edit item form UI is raw: no field grouping, inputs invisible on light+dark, no required/optional markers, no guidance, action buttons scroll away | Open `/inventory/items/new` | Card-sectioned guided form, required/optional markers, hints, sticky actions, visible inputs | FIXED (committed) — full redesign of shared ItemFormPanel; re-verify live |
| 4 | 2026-06-29 | 01 Items/Catalog | MEDIUM | Create flow forces draft→activate two-step (default isActive=false + two co-equal buttons); confusing for everyday 1-5 location retailers | Open New item | Default new items ACTIVE (one primary "Add item"); "Save as draft" stays as a quiet secondary. isActive is a visibility flag only (gates POS catalog), not a data guard; DB+DTO already default true | FIXED (committed) |
| 5 | 2026-06-29 | 01 Items/Catalog | LOW/UX | No total item count on list (harder to gauge catalog size) | Items list | "N items" total footer | FIXED (committed) — reuses meta.total, en+ar |
| 6 | 2026-06-29 | 01 Items/Catalog | NOTE | Import data: all 150 items missing Arabic name (name_alt) and brand | DB recon | Not a screen bug; import-completeness gap. Form supports both fields; consider import enrichment | LOGGED (data observation) |
| 7 | 2026-06-29 | 01 Items/Catalog | CRITICAL | EVERY imported item is uneditable: editing cost/price/brand throws "unit of measure cannot be changed" + mislabels SKU as "already in use". Import stored unit "Each"; update normalizes to "each" and compares vs raw "Each" → false unit-changed → 409 immutability guard | Edit any imported item, change cost, Save | Re-sending an unchanged unit must not trip the guard | FIXED (2e5a0b41) — compare normalized-to-normalized; only write unit when actually changed |
| 8 | 2026-06-29 | 01 Items/Catalog | CRITICAL | New-item create 400 "Request validation failed: description expected string, received null" — form clears empty optional fields to null but create DTO rejected null on description (and nameAlt) | Add item with name+SP+unit, Save | Create accepts null for cleared optional fields | FIXED (2e5a0b41) — description+nameAlt .nullable() on create schema |
| 9 | 2026-06-29 | 01 Items/Catalog | HIGH | On edit, optional Category/Tax Group fail save with "Invalid UUID" (empty string sent for unselected picker); Category also not persisting the item's existing value | Edit item, Save | Empty optional picker = null, no error | FIXED (2e5a0b41) — coerce ""→null at load; show "None" for empty category |
| 10 | 2026-06-29 | 01 Items/Catalog | HIGH | Clicking Save with validation errors gives NO toast/feedback; errors only visible after manual scroll up | Submit invalid form | Toast + auto-scroll to first error | FIXED (2e5a0b41) — onInvalid toast + scrollIntoView |
| 11 | 2026-06-29 | 01 Items/Catalog | MEDIUM | Edit form prices render at raw 6dp ("27.190000") not tenant currency precision (KWD 3dp) | Edit any item | 3dp via shared currency-precision util | FIXED (2e5a0b41) — formatToDecimals on load |
| 12 | 2026-06-29 | 01 Items/Catalog | LOW | SKU help marker is a "(?)" text glyph, inconsistent with the Info "i" icon used everywhere else | Edit item, SKU field | Info icon | FIXED (2e5a0b41) |
| 13 | 2026-06-29 | 01 Items/Catalog | LOW | "150 items" total count is small/floating at bottom-left of list; should be a clearer count display | Items list footer | Prominent, properly placed count | FIXED (4ccab4ab) — count pill moved into page header |
| 14 | 2026-06-29 | 03 Barcodes | HIGH | Barcode Type (EAN-13/UPC-A/etc) was NOT validated against the value — a 5-digit value could be saved as "EAN-13" and then fail at the scanner | Add barcode with mismatched type | Reject invalid format client + server | FIXED (4ccab4ab) — shared validateBarcodeFormat (check digits); inline client error + server refine |
| 15 | 2026-06-29 | 01 Items/Catalog | MEDIUM | Item form showed every Details field (Weight, OEM/Part Number) for all industries regardless of relevance, unlike the import template which is industry-adaptive | Open form as auto-parts tenant | Industry-adaptive field visibility, single source of truth | FIXED (4ccab4ab) — shared resolveItemFormFieldVisibility; import config now consumes same sets; hides Weight for auto-parts, keeps fields that already have data |
| 17 | 2026-06-30 | 01 Items/Catalog | HIGH | Edit item: Category shows "None" + "Invalid UUID" on save though list shows a real category (DB clean: valid UUID, active cat). Client schema enforced .uuid() on dropdown-sourced category/taxGroup; also an item whose category was deactivated would show None and silently clear it on save | Edit BAT-0069, Save | Category preselects + never dropped; no client uuid error | FIXED (0cdf7855) — drop client .uuid() (server authoritative); always inject loaded category as option; coerce ""->null in payload |
| 18 | 2026-06-30 | 02 Categories | HIGH | Edit/Delete actions are hover-only (`opacity-0 group-hover:opacity-100`) — unreachable on touch/tablet, the primary way MENA retail staff use this. Undiscoverable even on desktop | Open `/inventory/categories` on a tablet (or without hovering) | Actions always visible + keyboard-focusable | FIXED (a7c0fe99) — removed hover gating in category-tree-node |
| 19 | 2026-06-30 | 02 Categories | MEDIUM | No item count anywhere on the category tree — storekeeper cannot see how many items a category holds, and only the child-category count badge exists. Also means delete is hard-blocked via 409 with no proactive scope warning | Open categories list | Per-category item count badge + count shown in delete dialog before confirm | FIXED (a7c0fe99) — API grouped count → itemCount badge + delete-dialog reassign flow (move to category / uncategorize); + drill-through, summary header, inactive chip, search filter, 29 API tests |
| - | 2026-06-30 | 02 Categories | PASS | Category names render as full human labels (Batteries/Oils & Fluids); Oils & Fluids correctly shows no Arabic name (clean, no broken label); other 7 show Arabic secondary names | Live screenshot | clean bilingual render | VERIFIED PASS |
| - | 2026-06-30 | 04 Pack Units | PASS | Conversion-integrity invariants verified at code/DB level: item_pack_units.conversion_factor numeric(19,6) + DB CHECK >0; stock_ledger_entries.quantity numeric(19,6) (fractional-safe); unique (tenant,item,name) + ConflictException; toBaseQuantity uses decimal.js (qty×factor, no float drift) | code/DB recon | wrong factor cannot silently corrupt stock | VERIFIED PASS |
| 20 | 2026-06-30 | 04 Pack Units | MEDIUM | Optional/advanced item sections (Pack Units, Barcodes) render their FULL add-form always-expanded on every item detail — a wall of machinery (carton factors, discount type/value, default-sell/purchase) on all 150 items for a 1-shop trader who sells everything by the piece | Open any item, scroll to Pack Units/Barcodes card | Collapsed-by-default when empty, expanded when populated; header count badge + plain-language one-liner | FIXED (e094a524) — new CollapsibleSectionCard; collapsed when count=0 |
| 21 | 2026-06-30 | 04 Pack Units | MEDIUM | Typing 0 in Units-per-pack fires an ugly NATIVE browser validation bubble ("Value must be greater than or equal to 0.000001") — unbranded tech jargon, hijacks submit before our own validator | Add pack unit, factor 0, submit | Our friendly inline "must be greater than 0" message | FIXED (e094a524) — removed native min= attr; rely on client validator |
| 22 | 2026-06-30 | 04 Pack Units | LOW | Pack-unit add-form had no required/optional markers (inconsistent with the main item form which marks every field) | Open pack-unit form | Asterisks on Name/Units-per-pack/Discount-value; (Optional) on alt-name/discount-type | FIXED (e094a524) — shared RequiredMark/OptionalTag |
| 23 | 2026-06-30 | 04 Pack Units | LOW | Alt-name (Arabic) input missing dir="auto" — RTL text renders LTR inside the box | Type Arabic pack name | dir="auto" correct directionality | FIXED (e094a524) |
| 24 | 2026-06-30 | 04 Pack Units | LOW | Discount type/value pair consistency validated client-side only; a direct API POST with discountType but no discountValue would persist null value (not user-facing, backend hardening) | Direct POST /:id/pack-units | Zod createPackUnitSchema enforces the pair | OPEN (batch — backend) |
| 25 | 2026-06-30 | 02 Categories | HIGH | Tenant-isolation gap: category DELETE statement scoped only by `eq(id)`, not `and(eq(id), eq(tenantId))` like every other mutation in the service. Guarded in practice by the prior tenant-checked lookup, but a guessed/leaked id is one refactor away from deleting another tenant's category | Code recon (found by Wave-4 test author) | DELETE guards id AND tenantId (defense-in-depth) | FIXED (a7c0fe99) — and(eq(id),eq(tenantId)) on tx.delete |
| 16 | 2026-06-29 | 01 Items/Catalog | MEDIUM | Tax Group field shown to non-VAT tenants (e.g. Kuwait) who only have a seeded "No Tax" group — pointless field | Open form as KW tenant | Hide Tax Group when tenant has no real VAT (data-driven, default still posts) | FIXED (4ccab4ab) — isNoTax flag on tax-groups response; field hidden when all groups no-tax |
| 26 | 2026-06-30 | 05 Warehouses/Locations | CRITICAL | Tenant-isolation gap (bug #25 repeated): warehouse/zone/bin DELETE statements scoped by `eq(id)` only, not `and(eq(id),eq(tenantId))`. Guarded in practice by the prior tenant-checked lookup but a guessed/leaked uuid + valid JWT is one refactor from deleting another tenant's location | Code recon | DELETE guards id AND tenantId on all three services | FIXED (31936ecd) — and(eq(id),eq(tenantId)) on bins/zones/warehouses delete |
| 27 | 2026-06-30 | 05 Warehouses/Locations | HIGH | `deleteBin` had NO guards: a bin referenced by `stock_ledger_entries` (binId FK is onDelete:restrict) throws an unhandled Postgres 500, not a clean message | Delete a bin used in a movement | Block with friendly ConflictException ("has stock history, deactivate instead") | FIXED (31936ecd) — proactive ledger count check before delete |
| 28 | 2026-06-30 | 05 Warehouses/Locations | MEDIUM | `deleteWarehouse` checked default + zones but NOT stock_ledger history; a warehouse with movement history but no zones would 500 on the warehouseId FK (onDelete:restrict) | Delete warehouse with stock history, no zones | Block with friendly ConflictException | FIXED (31936ecd) — ledger count guard added |
| 29 | 2026-06-30 | 05 Warehouses/Locations | MEDIUM | Deactivating the SOLE default warehouse was not blocked — a branch could silently end up with no active default | Edit default warehouse, set inactive | Block: "assign a different default first" (mirrors delete guard) | FIXED (31936ecd) — sole-default deactivation guard in updateWarehouse tx |
| 30 | 2026-06-30 | 05 Warehouses/Locations | LOW | Zone/bin empty states are bare text ("No zones in this warehouse.") with no "optional / you don't need this" framing for a 1-warehouse shop; sub-sections always render once a warehouse is expanded | Open /settings/locations as single-warehouse tenant | Reassuring optional framing so simple traders aren't pushed through zone/bin machinery | FIXED (9966b46d) — zone/bin empty copy reframed as explicitly optional (en+ar) |
| 31 | 2026-06-30 | 05 Warehouses/Locations | LOW | No success toast on warehouse/zone/bin create/edit — dialog closes silently; first-time MENA retail users may not notice the list updated | Create a warehouse | Success toast on each mutation | FIXED (9966b46d) — success toast on every branch/warehouse/zone/bin create/edit/delete (en+ar) |
| 32 | 2026-06-30 | 05 Warehouses/Locations | MEDIUM | Single-location tenant's only warehouse is buried behind the branch chevron — the branch row is collapsed by default so the one thing that matters (Main Warehouse - Shuwaikh) needs two clicks to see | Open /settings/locations as Asala (1 branch) | Sole branch auto-expands on load so the warehouse is immediately visible | FIXED (80e0859d) — ref-guarded auto-expand when exactly 1 branch; self-disables at 2+ branches; zones/bins stay collapsed (opt-in machinery) |
| 33 | 2026-06-30 | 05 Warehouses/Locations | MEDIUM | Warehouse/zone/bin Code is a mandatory free-text field marked "Cannot be changed after creation" — a non-technical shop owner has no naming scheme and dead-ends ("no idea what code to put in") on every create | Add warehouse "Spare Store" | Code auto-suggested from the Name, still editable | FIXED (9966b46d) — shared slugifyCode; code auto-follows Name (slugified) until user edits; server stays uniqueness authority; Arabic-only names yield blank (no garbage code) |
| 34 | 2026-06-30 | 05 Warehouses/Locations | MEDIUM | Code auto-fill dead after touch: typing in Code then deleting it left the field permanently in "user owns it" mode, so typing the Name afterwards no longer generated a code | Type in Code, clear it, type Name | Clearing Code re-arms name-driven auto-fill | FIXED (383454ba) — codeEdited keys off whether field is non-empty (all 4 dialogs) |
| 35 | 2026-06-30 | 05 Warehouses/Locations | MEDIUM | Branch dialog showed the "Auto-filled from the name" hint but did NOT actually auto-derive a code (only warehouse/zone/bin did) — owner still dead-ends on the Code field | Add branch "Test Branch" | Branch code auto-derives from name like the others | FIXED (383454ba) — slugifyCode wired into branch-dialog |
| 36 | 2026-06-30 | 05 Warehouses/Locations | MEDIUM | New-branch Timezone defaulted to "UTC" which then failed the "valid timezone" check, showing a red error on a fresh form; wrong for a Kuwait shop | Open Add branch | Default to a sensible timezone / show placeholder, not an errored UTC | FIXED (383454ba) — default to browser-detected tz (Asia/Kuwait for a KW user); placeholder shown if unrecognized |
| 37 | 2026-06-30 | 05 Warehouses/Locations | HIGH | Deleting a branch that still has warehouses throws an opaque DrizzleQueryError 500 (warehouses.branch_id FK is onDelete:restrict, code 23503) — vague server error to the user | Delete a branch with a warehouse | Clear ConflictException, delete blocked | FIXED (82472c25) — warehouse-count guard in deleteBranch + tenant-scoped delete (defense-in-depth) |
| 38 | 2026-06-30 | (systemic) delete endpoints | HIGH | Codebase-wide audit (triggered by #26/#27/#37): 4 tenant delete endpoints would throw a raw Postgres FK-violation 500 (onDelete:restrict) with real data — deleteTaxGroup (9+ ref tables), deleteTaxCode (journal_entry_lines), POS tender-type remove (pos_payments), pack-unit removePackUnit (7 line tables) | Delete a tax group used on any invoice line | Friendly 409 ConflictException, not a 500 | FIXED (8dc7e69a) — isForeignKeyViolationError(23503) backstop on each; catches future FKs too; deleteBin wrapped in txn (TOCTOU); FK→409 unit tests (99 pass) |
| 39 | 2026-06-30 | (systemic) delete endpoints | MEDIUM | Same audit: 5 deletes scoped by id only, not id+tenant/parent (defense-in-depth gap; low real risk on per-tenant DBs but off-convention) — roles deleteRole/revokeRole, tax-config deleteTaxRate, notification removeRecipientRule, price-lists removeItem | Code recon | DELETE scoped by id AND tenant/parent | FIXED (8dc7e69a) — +tenantId / +parent id on all five |
| - | 2026-06-30 | 05 Warehouses/Locations | PASS | Default-warehouse invariant holds: create/update atomically flip the prior default off (tenant+branch scoped); transit cannot be default; sole-default delete blocked. Bin-code format regex + whitespace rejection OK; transit type exposed in UI + i18n parity (store/warehouse/transit en+ar); actions NOT hover-only (bug #18 absent); no hardcoded currency/2dp | code recon | invariants hold | VERIFIED PASS |
