# POS — Testing Findings Log

> Running log of issues found during live testing. One row per finding. Update status as fixed.
> Severity rubric: see [`../README.md`](../README.md). Fix CRITICAL/HIGH immediately; batch MEDIUM/LOW for review.

## Baseline (2026-06-29, Asala dev tenant `zerupt_tenant_dev` @ `br-old-recipe-a1d3dw26`)

**Data state:** Asala has **1 register, zero shifts / transactions / payments / receipts / cash movements.** No POS activity has been rung up yet. The data tie-out invariants (cash reconciliation, balanced JE per sale, COGS relief per line, stock deduction per line, return/void reversal) **cannot be baselined until sales exist** — defer these to the live dogfooding pass, then re-run queries Q1–Q10 from recon.

**Structural baseline (dataset-independent — DB constraints + indexes):**

| Invariant | Guard found | Verdict |
|---|---|---|
| ≤1 open shift per register | partial unique idx `pos_shifts_one_open_per_register WHERE status<>'closed'` | ✅ DB-ENFORCED (recon worried this was service-only — it is a hard DB guard) |
| Cashier ≤1 open shift | partial unique idx `pos_shifts_one_open_per_cashier` | ✅ DB-ENFORCED |
| shiftNumber monotonic/unique per register | unique `(register_id, shift_number)` | ✅ |
| Idempotent offline replay (tx + shift) | partial unique `pos_transactions_tenant_client_id_key` + `pos_shifts_tenant_client_id_key WHERE client_id IS NOT NULL` | ✅ DB-ENFORCED |
| transactionNumber unique per tenant | unique `(tenant_id, transaction_number)` | ✅ |
| Exactly one receipt per transaction | unique `(tenant_id, transaction_id)` on `pos_receipts` | ✅ |
| Receipt token unique | partial unique `pos_transactions_receipt_token_key` | ✅ |
| costAtSale ≥ 0 | check `cost_at_sale >= 0` | ✅ (but ≥0 only — zero-cost still passes; verify >0 on live tracked items, recon Q7) |
| Foreign-currency payment requires rate | check `amount_fc IS NULL OR (exchange_rate IS NOT NULL AND >0)` | ✅ |
| changeGiven ≥ 0; payment amount > 0 | checks present | ✅ |
| openingFloat ≥ 0; cash movement amount > 0 | checks present | ✅ |

## Findings

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 1 | 2026-06-29 | 02 Transaction Lifecycle | LOW | No DB CHECK backstop for `grandTotal = subtotal + taxTotal - discountTotal` (only individual non-negative checks exist). Header-total arithmetic is service-only — a service rounding/logic bug would persist with no DB guard. | `pg_constraint` scan on `pos_transactions` | A CHECK enforcing the total identity (as accounting/inventory ledgers have), or accept as service-validated + covered by tests | OPEN (confirm recon flag; decide CHECK vs test-only) |
| 2 | 2026-06-29 | (baseline) | NOTE | Asala has zero POS activity (1 register only). Data tie-out invariants undeferred until live sales rung up. | Row counts | Not a bug — testing-readiness gap. Ring up sales in the live pass, then run recon Q1–Q10 | LOGGED (superseded — prod Asala now has 1 shift + 10 sales rung up 2026-07-03) |

## UX overhaul findings (2026-07-03, from live screenshots — register screen)

> **P1–P5 SHIPPED** in commit `574c576f` (main, pushed). 14 files, reviewer panel (frontend+code) findings all folded in. web typecheck + i18n:check + 923 POS tests green. Findings #3–#6 → FIXED below. #7 partially (offline-banner logic untouched), #8 still OPEN (functional, submodule 08).

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 3 | 2026-07-03 | 02 Cart (register screen) | HIGH | **Cart is unreadable.** Item names truncate to "Ba…" while per-line Discount + Tax columns render "KWD 0.000" on every line (tenant has no tax/discount). Least-useful data (zeros) eats the space the item name needs; "KWD" repeats ~20× on one panel with no column separation. | Add 3 items to cart, view right panel | Item name legible (2 lines ok); `KWD` shown once in header not per-cell; zero discount/tax collapsed (shown only when non-zero); line = Name · Qty · Total | FIXED 574c576f |
| 4 | 2026-07-03 | 01/all | HIGH | **Dialogs break the dark theme.** POS is dark but Close Shift renders as a white modal (same will affect cash-movement/void/return). Reads as unfinished. | Open Close Shift on dark POS | All `(pos)` dialogs inherit the dark surface via a shared themed wrapper | FIXED 574c576f |
| 5 | 2026-07-03 | 03 Payment (SETTLE) | MEDIUM | **SETTLE wastes 60% of screen + cash entry built for a mouse.** Greyed catalog keeps 60% width while pay is crammed in 40%; Amount-tendered has spinner arrows (no on-screen keypad wired though numeric-keypad.tsx exists); quick-cash chips fixed 0.5/1/5/10/20 regardless of amount due; "Cash advisory" jargon. | Hit Pay (F4), view payment panel | SETTLE expands pay to reclaimed space; big keypad; smart denominations (Exact + next round notes 135/140/150); plain-language labels | FIXED 574c576f |
| 6 | 2026-07-03 | 02 Search | LOW | **Search dropdown collides with tile grid** — results overlay with a hard edge slicing a tile mid-row. Result rows (icon·name·SKU·price) are more readable than the cart itself. | Type "oil" in search | Clean elevated surface that owns the region (or replaces grid while searching); cart should borrow the result-row treatment | FIXED 574c576f |
| 7 | 2026-07-03 | 08 Close Shift | LOW | Close Shift dialog: placeholder AND helper text both read "Total cash physically in the drawer." (duplicate); shows "Offline totals… syncs when you reconnect" while top bar shows "Online" (contradiction — verify real bug vs stale copy). | Open Close Shift | De-dupe helper text; offline banner only when actually offline | OPEN |
| 8 | 2026-07-03 | 08 Close Shift | HIGH | **Blind-close summary diverges from server truth.** Close Shift dialog computed **8 transactions / net 1,795.720** ("Offline totals") while the server (prod Asala shift b61dfe82) holds **10 completed / 1,827.720** and the top bar read "Online" — a 2-sale / 32.000 KWD gap. Cashier would reconcile the drawer against a wrong expected cash. | Ring sales, open Close Shift on the register while online | Blind-close expected/summary must reflect authoritative server tally when online (or force a sync before showing totals); local IDB tally used only when genuinely offline, clearly labelled | **FIXED** (display-only bug; ledger was always correct — see root-cause note) |

### Finding #8 — root cause + fix (2026-07-03)

**Which problem:** display-only. The persisted ledger value was never wrong.

- **Bug (display):** `shift-close-panel.tsx` unconditionally sourced its summary from the local IndexedDB queue via `computeLocalZReportFromQueue(db, openingFloat)` — regardless of connectivity. Expected cash, transactions, net/cash sales, and the derived over/short all came from local IDB; it received `serverShiftId`/`isOffline` props but never used them, and always rendered the WifiOff "Offline totals" note (the "Offline while Online" tell). When the device's local queue trailed the server (sales rung on another device/session, or a cleared/partial IDB — the prod case: 8 local vs 10 server), the cashier saw a wrong expected (1,795.720 vs true 1,827.720) and a phantom over/short. The X-report dialog was already correct — it uses the server `useXReportQuery`.
- **Persisted value (safe):** `PosShiftsService.close` (`pos-shifts.service.ts:477-479`) recomputes `expectedCash` via `computeCashComponents` (DB aggregate over `pos_payments`/`pos_transactions`/`pos_cash_movements`) and `cashOverShort = actualCash − expectedCash` server-side; it only trusts client `actualCash` (the physical count). GL was correct (1,827.720). Already guarded by `pos-shifts.service.spec.ts` (close computes expected/over-short from DB, ignores client figures).
- **Fix:** when `!isOffline && serverShiftId`, the close panel now drives expected/summary/over-short from the server X-report (`useXReportQuery`), labelled "Live totals from the server"; the local IDB report is used **only** offline / pre-sync, labelled provisional. Panel also nudges the sync engine on open so pending local sales flush. New regression guard `shift-close-authoritative.test.tsx` reproduces the exact 8/1,795.720 (local) vs 10/1,827.720 (server) divergence and asserts the online close reconciles against the server tally with no phantom over/short. web typecheck + i18n:check + close-panel tests (8) green.
- **Also addresses #7's contradiction:** the "Offline totals" note now renders only when actually provisional/offline. (The duplicate helper/placeholder text in #7 is cosmetic and left as-is.)

## Live findings (2026-07-04, register/payment/receipt dogfooding — post-UI-overhaul)

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 9 | 2026-07-04 | 08 Close Shift | HIGH | **Close Shift dialog not scrollable** — content (Transfer to safe + the Close button) is cut off at the viewport bottom and the modal doesn't scroll, so the cashier cannot reach/press Close. Blocks the entire close flow. | Open Close Shift with the dialog taller than viewport | Dialog body scrolls (overflow-y-auto, max-height), Close button always reachable/sticky footer | FIXED 3d2eb4ee (scrollable body + sticky footer) |
| 10 | 2026-07-04 | 03 Payment | MEDIUM | **Change due shown in THREE places** on the SETTLE screen: "Change: KWD x" under Amount Due, a big "CHANGE DUE" banner, and "Change due: KWD x" under the quick-cash chips. Redundant/cluttered. | Tender > amount due (cash) | One prominent CHANGE DUE display; remove the other two | FIXED 3d2eb4ee (single change-due display) |
| 11 | 2026-07-04 | 07 Receipt | MEDIUM | **Receipt titled "TAX INVOICE" for a zero-tax tenant.** Kuwait tenant collects no VAT; a plain sale should read "RECEIPT"/"SALES RECEIPT". "TAX INVOICE" should be conditional on VAT registration / tax present. | Complete a sale, view receipt | Title conditional on tax/VAT-registration | FIXED 3d2eb4ee (title gated on taxTotal>0) |
| 12 | 2026-07-04 | 07 Receipt | MEDIUM | **Receipt line layout stacked + missing line total.** Each item = name on row1, "qty × unit" on row2, no extended line total. Founder wants single row: name (start) + qty/price (end); second-language alt name on the next row with qty/price; add the per-line extended total. | View receipt preview | Single-row primary + alt-name row per item; show line extended total | FIXED 3d2eb4ee (single-row layout + line total + alt-name row) |
| 13 | 2026-07-04 | 07 Receipt | LOW | **"Sale completed" header above receipt preview** — possibly redundant with the receipt itself. | Complete a sale | Decide: remove or make it a subtle confirmation | FIXED 3d2eb4ee (slimmed to checkmark) |
| 14 | 2026-07-04 | 07 Receipt | LOW | **"Send via WhatsApp" button disabled** — likely no customer/phone attached to the sale. Flow to attach a customer/phone unclear. | Post-sale screen, no customer | Enable path (prompt for phone / attach customer); clarify flow | FIXED 3d2eb4ee (real WhatsApp send flow built, not hidden) |
| 15 | 2026-07-04 | 02 Search | MEDIUM | **Price check shows "In stock: Unknown."** The price-check dialog can't resolve on-hand quantity. | Tap info (i) on a search result | Show real on-hand stock (or hide the row if genuinely unavailable) | FIXED 3d2eb4ee (lazy warehouse-scoped on-hand lookup) |
| 16 | 2026-07-04 | 08 Close Shift | HIGH | **Possible incomplete #8 fix:** Close dialog showed 8 txns / expected 1,763.720 while server held 10→11 completed (2028.700). Dialog labeled "Live totals from the server" but numbers don't match server. Verify whether display is truly server-authoritative or still stale/local. | Open Close Shift while online, compare to server | Dialog matches server tally exactly | OPEN (confirm vs screenshot timing before reopening #8) |

## Tie-out recon (2026-07-04, PROD Asala `zerupt_tenant_al_asala_auto_parts_mqy1wpk2` @ `br-red-term`, shift `b61dfe82`)

> Submodule 02 autonomous three-way tie-out over all 11 completed transactions in the open shift. Recon-only pass (no code changes per founder instruction). Cash tally 2028.700 KWD = Σ grand_total ✓.

**Header arithmetic (invariant §2):** ✅ all 11. `grandTotal = subtotal + taxTotal − discountTotal` exact on every row (incl. tx-9/tx-10 discount: 2.440 − 0.610 = 1.830). transactionNumber unique + monotonic (B1SHUWAIKHREG1-1-1..11). No voids, no held tx currently.

**Three-way tie-out (POS record ↔ GL journal ↔ stock ledger):** ✅ 9 of 11 tie out exactly (revenue JE `pos.transaction.completed` balanced Dr=Cr; COGS JE `inventory.sale`/`sale_return` balanced; matching signed stock-ledger movement at qty×costAtSale). ❌ **2 of 11 FAIL — see #17.** Two are returns (tx-3, tx-5: `type=return`, negative-qty lines, positive grand_total — correct modelling).

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 17 | 2026-07-04 | 02 / 09 Cross-module (inventory tie-out) | **CRITICAL** | **Two completed prod POS transactions relieved NO stock and posted NO COGS — three-way tie-out broken.** (a) **tx `ec78893f` / B1SHUWAIKHREG1-1-8** — 715.000 KWD sale of "Test Battery (Batch tracked)" qty 11: revenue JE posted (Dr cash / Cr sales 715), but **no `inventory.sale` JE and no `stock_ledger_entries` row** → COGS understated ~143.000, inventory overstated ~143.000. (b) **tx `9909384f` / B1SHUWAIKHREG1-1-5** — serial ECU return qty −1: revenue-reversal JE posted (5.000), but **no `inventory.sale_return` JE and no stock movement** → returned serial never restocked, COGS stays overstated 99.000. In BOTH the inventory outbox event (`pos.*.completed.inventory`) is marked **`status=completed`, attempts=0, no error**, and its payload carries the correct line (qty 11 batch / qty 1 serial) — so it is a **silent no-op**, not a thrown failure. **Root cause (localized, read-only):** the batch line had **no `batchId`** and the serial-return line had **no `serialNumbers`** (unlike the tie-ing tx-6/tx-7 which carried a `batchId`). `MovementAttributionService.attributeOutbound/attributeInbound` (`movement-attribution.service.ts:143-178`) can return an **empty movement array** — the batch idempotency short-circuit `if (await batchLineAlreadyRecorded(...)) return []` (`:153`) or an empty FEFO pick on a depleted lot. `recordAttributed` (`inventory-event.listener.ts:91-96`) returns `null` when the movement array is empty, and `applyOutbound`/`applyInbound` (`:501-505`, `:219-222`) treat `entryId === null` as **"duplicate eventId — skipping"** and `return` — writing nothing while the event completes cleanly. The POS never-block-negative policy (`blockNegativeStock=false`) is defeated because attribution bails before the negative-stock fallback. | Ring a POS sale of a batch-tracked item whose lots are depleted (no FEFO batch resolvable at add-line) — or return a serial line without the serialNumbers stamped — then query `stock_ledger_entries` + `journal_entries(source_document_type='pos')` for that tx. | Every completed sale/return line writes exactly one signed stock movement + a balanced COGS JE (Dr COGS / Cr Inventory at qty×costAtSale), even when negative stock results; an empty attribution must NOT be silently swallowed as a "duplicate". Needs engine-level fix (distinguish "already-recorded replay" from "nothing attributed", and honor the POS negative-stock fallback for unresolvable batch/serial lines) — deferred to a fix pass. | **RESOLVED** — prod backfilled + verified (2026-07-04); D1 engine fix reversed to **never-block-and-flag** (2026-07-05, see subsection below) |
| 18 | 2026-07-04 | 09 Cross-module (outbox) | ~~MEDIUM~~ → **NOT A BUG** | Original: "outbox poller appears stalled" (tx-11 rows pending at recon time). **Re-verified 2026-07-04 (12h later): all 107 outbox rows now `completed`** (newest processed 2026-07-03 21:43) — the pending rows were just mid-flight during the recon snapshot. Poller drains correctly. Downgraded/closed. | Re-query `accounting_event_outbox GROUP BY status` → 100% completed. | Poller healthy. | CLOSED (transient snapshot, not a stall) |

### Finding #17 — root cause CONFIRMED + fix (2026-07-04)

**Re-verified 12h later (2026-07-04):** still 11 completed tx (no new ones); **tx-8 and tx-5 still have ZERO stock-ledger rows** — no self-heal. Diagnosis below is derivation-verified (recomputed the deterministic per-line eventIds offline and matched them against the actual ledger rows of the *tie-ing* tx-6 exactly).

**Three defects — two already fixed upstream, one live:**
- **D3 (durability) — ALREADY FIXED.** The silent completion (outbox marked `completed`, attempts=0, no stock/COGS) is the exact silent-gap that `runDurableGated` + `DURABLE_REPLAY_MARKER` (PR #168/#169, 2026-07-03) closed. tx-8/tx-5 were rung **2026-07-01**, BEFORE that fix → pre-fix casualties. On current code a fresh oversell dead-letters LOUDLY instead of completing silently.
- **D2 (serial not copied to POS return line) — ALREADY FIXED.** Current receipted-return path copies `serialNumber` onto the return line (`pos-transactions.service.ts:1572`); tx-5 (rung 2026-07-01) predates it → stale data.
- **D1 (batch/serial oversell blocks COGS) — LIVE, being fixed.** `BatchPickerService.pick` (`batch-picker.service.ts:100-106`) hard-throws "Insufficient batch-tracked stock" when requested qty > available lots, even for a POS sale where `blockNegativeStock=false`. Since the inventory fan-out runs async AFTER the sale commits + takes cash, the throw now dead-letters forever → sale completed, COGS/stock never post → tie-out broken. **The hardened invariant `MovementAttributionService.failUnattributed` (`movement-attribution.service.ts:63-81`) forbids writing an unattributed ledger row for a tracked item**, so the correct fix is NOT "relieve unattributed negative stock". **Persona decision (counter cashier, lot-tracked auto parts):** BLOCK a batch/serial oversell at COMPLETION with a clear "only N in stock" message — before money is taken. Non-tracked items keep the never-block-negative behavior. Fix in progress (pre-completion attributability guard in `pos-transactions.service.ts` + tests; accounting/nestjs/code reviewers).

**Prod data remediation (needs founder approval — Neon MCP writes require it):** the two pre-fix prod transactions are still out of tie-out and won't self-heal: **tx-8 (`ec78893f`)** is short one `inventory.sale` movement + COGS ~143.000 (batch relief for qty 11), and **tx-5 (`9909384f`)** is missing the serial restock + COGS reversal 99.000. These need a manual corrective backfill (or a void+re-ring) on prod once the code fix lands. Do NOT auto-apply — flag for the founder.

**FIX (D1) — SHIPPED to main 2026-07-04:** pre-completion attributability guard `assertBatchLotsAttributable` in `pos-transactions.service.ts`, called inside the `pay()` DB transaction. For every batch-tracked sale line it sums available lot stock (Σ `stock_ledger_entries.quantity` per lot × warehouse, `GREATEST(0,…)` per-lot clamp to match `BatchPickerService.pick` exactly, `FOR UPDATE` locked in the picker's (expiry, created) order) and rejects with a user-friendly `UnprocessableEntityException` if the requested qty (aggregated per item across lines) can't be covered — BEFORE cash/outbox commit. A throw rolls back the whole sale atomically (status flip, receipt, payments, both outbox rows). Non-tracked items keep never-block negative behavior; serial oversell already blocks via `markSold`. **Reviewer panel (accounting + nestjs + code):** online-path logic confirmed correct (atomicity, ledger-basis parity, unit consistency, non-tracked/serial preservation); folded in — negative-lot false-positive clamp (accounting MEDIUM), deadlock-safe lock ordering (code MEDIUM), corrected overclaiming comments (nestjs CRITICAL was a doc-overclaim, not a logic defect — the guard fixes the observed single-tx prod bug; the concurrent-across-registers double-sell is a pre-existing async-relief residual, now LOUD not silent, single-register-safe). api typecheck + 176 pos-transactions + 101 pos-sync tests green. **Residuals (accepted, surfaced to founder):** (1) concurrent same-lot completions within the async fan-out window can still both pass → the loser dead-letters loudly (full close needs synchronous in-tx batch reservation — tracked follow-up); (2) offline-synced oversell is intentionally NOT guarded (never reject an already-paid device sale) → dead-letters for manual reconciliation. Both need a monitoring/reconciliation runbook.

**BACKFILL — ✅ EXECUTED + VERIFIED on prod 2026-07-04** (founder confirmed current-WAC basis). Runbook: [`study/pos/11-tie-out-backfill-and-residual-runbook.md`].
- **tx-5** (serial ECU return): re-driven through the engine → `inventory.sale_return` JE `200067` (DR 1141 / CR 5100 99.0), stock +1, serial `SN-AAA-099` restored. Tied out.
- **tx-8** (batch, 11u): engine re-drive silently no-op'd twice (prod batch-attribution path skips this historical line), so posted via one **atomic SQL transaction** mirroring the engine — balanced `inventory.sale` JE (`BACKFILL-1-8`, DR 5100 / CR 1141 **299.538459** @ WAC 27.230769), two FEFO-split ledger rows (−5 lot 521fb998, −6 lot 3b50c6d3, engine-derived event-IDs), materialized on-hand 13→2, lots restored (no negatives). **GL 1141 still ties to materialized value (…1895 vs …1899, pre-existing 0.0004 rounding) → ZERO new residual.**
- **Verification:** backstop query (completed POS tx with a tracked line missing its stock movement) = **ZERO rows** across the whole tenant (orig 11 + both backfills + founder's live sales). Three-way tie-out holds everywhere.

### Finding #17 — D1 decision REVERSED: never-block-and-flag (2026-07-05)

**Supersedes the block-at-completion decision above.** Founder ruling: a physical till must **never** refuse a real sale because system stock is behind physical reality (received-but-not-entered, quantity-typed sales, multi-channel drift, stale offline snapshots). A batch-tracked oversell must now **complete, relieve negative, and flag** — exactly like non-tracked items — instead of being blocked. The prior fix (`assertBatchLotsAttributable`, commit `f48888ca`) is **removed**.

**What shipped (engine-level, the durable fix):**
- **`BatchPickerService.pick`** gains `opts.allowNegative`. Never-block: consume all available lots FEFO, then place the shortfall as a **negative quantity on the latest-expiry lot** (a real `batch_id` — the `failUnattributed` never-null invariant is respected). If the item has **no lots at all**, find-or-create a per-item **reconciliation lot** (`"UNRECEIVED (auto)"`, race-safe). Strict mode (default, `allowNegative:false`) is byte-identical — still throws for non-POS.
- **`MovementAttributionService.attributeOutbound`** threads `AttributionInput.allowNegative` into `pick()`. Serial issues are **not** relaxed by the flag.
- **`inventory-event.listener.applyOutbound`** sets `allowNegative = !payload.blockNegativeStock` (POS ⇒ true). COGS JE + signed stock movement + `decrementOutbound(flexible)` + `NEGATIVE_STOCK_FLAG` all post on the oversell → **three-way tie-out holds on a negative sale.**
- **`pos-transactions.service.pay`** — `assertBatchLotsAttributable` (call + method) **deleted**; no pre-completion batch block.
- **`pos-sync.service`** — offline oversell now relieves-negative + flags on sync (no dead-letter); comment corrected.
- **Serials keep blocking** — a serial the system has no record of cannot be attributed (a negative serial has no physical meaning; null `serial_number_id` would break `failUnattributed` → warranty/recall/return-matching). Message made actionable: *"Serial 'X' is not in the system yet. Receive or quick-add this unit, then ring the sale again."*
- **Flag surfacing** — deliberately **NOT denormalized** (derive-don't-denormalize, per AP-subledger principle). Negative stock is already ledger-truth on the **Stock Levels report** (`deriveStatus`→`negative` badge + `negativeStockCount`, `msl_zero_or_negative_stock_idx` partial index). That is the manager's reconciliation surface; the `NEGATIVE_STOCK_FLAG` event is kept for future consumers.

**Residuals R1/R2 DISSOLVED** — both only existed because a *block* needed a *reservation* to be race-safe. With never-block there is nothing to race on (concurrent same-lot sales both complete; the lot just goes further negative), so **no reservation table is needed**. Offline and online now converge on relieve-negative + flag.

**Tests:** batch-picker (negative-on-lot, depleted-lot, recon-lot, strict-still-throws), movement-attribution (threads `allowNegative`, defaults false), inventory-event.listener (POS oversell ties out + flags; non-POS blocks), pos-transactions (block tests re-pointed to never-block completion), serial-block preserved. api typecheck + all affected suites green (414 passing). 100% on money/tie-out paths.

**Status: ✅ SHIPPED — D1 reversed to never-block-and-flag.**

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 18 | 2026-07-04 | 08 Shift Close / cross-module | HIGH | **Shift-close cash-over/short JE could NEVER post — invalid `document_type` enum.** `buildShiftClosedJePayload` (`pos-transactions-events.ts:390`) emitted `sourceDocumentType: "PosShift"`, not a `document_type` enum member (every other POS JE builder uses `"pos"`); the `journal_entries` insert threw invalid-enum → the shift's cash-variance JE dead-lettered (found live: prod shift `b61dfe82`, a 4.700 KWD cash-shortage JE stuck failing; also tripped the outbox circuit breaker). | Close a POS shift with non-zero cash over/short | `sourceDocumentType` must be a valid enum → changed to `"pos"` + regression test | **FIXED (code `81a3673b` pushed) + prod cleaned.** Founder patched the stuck outbox row (`b978bab4`) payload `PosShift`→`pos` + reset; poller posted the JE `B1SHUWAIKH-JRN-200070` (4.700 cash shortage). Shift `b61dfe82` now fully tied out. ✅ |
| 19 | 2026-07-04 | 02 Domain (margin sanity) | NOTE | tx-4 (`d0ff61bc`) sells "ECU Test Unit" (serial) at unit_price 5.000 with cost_at_sale 99.000 → COGS 99 on 5.000 revenue (−94 margin). Genuine test data (deliberately cheap sale of an expensive serial unit), NOT a bug — `costAtSale > 0` invariant holds. Flagging only so it isn't mistaken for a WAC-capture error during review. | Line inspection | No action; margin-warning UX could surface sub-cost sales for the cashier (future). | LOGGED |

## Submodule 02 — Transaction Lifecycle — SIGN-OFF (2026-07-04)

**Correctness / accounting scope: ✅ SIGNED OFF.** The three-way tie-out (POS record ↔ GL journal ↔ stock ledger) is verified whole across the entire prod tenant — backstop query (any completed POS tx with a tracked line missing its stock movement) = **0 rows**, all outbox rows healthy (0 pending/failed/dead_letter), GL 1141 ties to materialized inventory value.
- Shipped: completion guard (`f48888ca`, interim — see (B) below), shift-close enum fix (`81a3673b`), backfill of the two pre-fix prod casualties (tx-8 atomic, tx-5 engine), shift-close prod cleanup.
- Header arithmetic, transactionNumber uniqueness, discount handling, void/return reversal, COGS relief all verified on live data.

**Tracked next-steps (deliberate, NOT silent debt):**
1. **(B) Never-block-and-flag redesign — DECIDED, not yet built.** Founder decision (2026-07-04): a lot/serial-tracked POS sale that exceeds the *system's* lot count must **complete anyway**, relieve stock into the negative, and flag for reconciliation (like non-tracked items) — because the system count often lags physical reality and the till must never refuse a real sale. **This SUPERSEDES the block-at-completion guard shipped in `f48888ca`.** This also **dissolves residuals R1 (concurrent oversell) and R2 (offline oversell)** — no reservation machinery needed; the loud negative-stock flag is the honest signal. Real fix lives in the inventory engine (`batch-picker.service.ts` `pick()` stops throwing on POS shortfall → allocates negative-on-lot; keep non-POS callers blocking). Open sub-decision: serial items (can't attribute an unknown serial). **Full handover prompt prepared — hand to a fresh agent (design-first, founder-approves).**
2. **Manual functional checklist** — cart build/edit, hold & recall, void, back-office transaction list, and the defensive-UX edge cases from the 02 testing guide (`…/scratchpad/pos-02-manual-testing-guide.md`). Live dogfooding covered sale→pay→receipt; hold/recall + void + back-office list still to be exercised by the founder.

## Settings redesign findings (2026-07-04, register management)

| # | Date | Submodule | Severity | Summary | Status |
|---|------|-----------|----------|---------|--------|
| 17 | 2026-07-04 | 01 Settings | HIGH | Entire POS settings UI poor: register mgmt bolted onto receipt settings, one long ungrouped form, printer buried, live preview used a different tenant's sample data (Pacific Co/AED), alt-language label hardcoded "Arabic". | FIXED bf1ffbbc — redesigned as legal-entities-style Registers list + row→detail drawer with grouped config sections; real-tenant preview; dynamic secondary-language label |
| 18 | 2026-07-04 | 01 Settings | MEDIUM | Register code was required — user could get stuck on codes. | FIXED bf1ffbbc — code optional, auto-suggested gap-safe REG-N, auto-generated on blank |
| 19 | 2026-07-04 | 01 Settings | LOW | Opening float didn't show 0.000 until focused. | FIXED bf1ffbbc — formats to currency decimals from first render |
| 20 | 2026-07-04 | 01 Settings | LOW | Duplicate-code error shown in TWO places (toast + inline banner). | FIXED bf1ffbbc — single inline error near Code field, no toast |
| 21 | 2026-07-04 | 01 Settings | HIGH | Could DEACTIVATE a register with an OPEN shift (server had no guard); till kept selling but register vanished from shift-open picker. | FIXED bf1ffbbc — server ConflictException guard (in a transaction, TOCTOU-safe) + UI blocks deactivate with reason; shift-open already rejected inactive registers |

| # | Date | Submodule | Severity | Summary | Status |
|---|------|-----------|----------|---------|--------|
| 22 | 2026-07-04 | 01 Register/Session | HIGH | Opening a new shift right after closing one re-showed the Close Shift dialog on the fresh shift (stale `overlay:'close'` — bumpShiftVersion never reset it). | FIXED 3eee0a6b — overlay cleared on every shift open/close/reconcile; regression test added |
| 23 | 2026-07-04 | 01 Register/Session | MEDIUM | Open Shift "opening float" was a raw number input (6dp `0.000000`, spinner arrows, unformatted "5"). | FIXED 3eee0a6b — shared MoneyInput at currency precision (KWD 3dp, no spinners) |

## Live findings (2026-07-04, sale/receipt/settle dogfooding on shift #2)

| # | Date | Submodule | Severity | Summary | Status |
|---|------|-----------|----------|---------|--------|
| 24 | 2026-07-04 | 06/09 Offline+Contracts | CRITICAL | Offline sale with a PACK-UNIT item fails to sync ("Request validation failed", retry loops) — stranded on-device forever. Sync ingest schema/service never learned pack units (unitPackId/unitQty), unlike the direct add-line path. | FIXED 728b6406 — sync schema + server-side pack resolution: base qty = unitQty×factor, pack discount folded via shared helpers; posts revenue/discount/COGS/stock identical to online; accounting-reviewed clean (2 passes) |
| 25 | 2026-07-04 | 07 Receipt | HIGH | Pack-unit receipt line internally inconsistent: "1 × KWD 8.730" but line total "KWD 52.380" (pack qty × base price ≠ base total). | FIXED 728b6406 — pack-aware displayUnitPrice (lineTotal÷qty, no re-pricing) → "1 × 52.380 = 52.380" |
| 26 | 2026-07-04 | 06 Offline | LOW | Sync-queue drawer showed money at 6dp (52.380000) instead of KWD 3dp. | FIXED 728b6406 — formatted via currency formatter |
| 27 | 2026-07-04 | 03 Payment | MEDIUM | Two change-dues shown pre-complete (small "Change:" under Amount Due + big banner) — earlier dedup was incomplete. | FIXED 728b6406 — single CHANGE DUE banner |
| 28 | 2026-07-04 | 03 Payment | MEDIUM | Tapping Pay auto-opened the amount keypad; cashier should choose (type / Exact / chip). | FIXED 728b6406 — autoOpen removed; opens on tap |
| 29 | 2026-07-04 | 03 Payment | LOW | Numeric keypad dialog off-center with uneven padding. | FIXED 728b6406 — standard centered DialogContent + even gaps |
| 30 | 2026-07-04 | 03 Payment | MEDIUM | Overpaying with only non-cash tenders gave no feedback (Complete silently disabled). | FIXED 728b6406 — inline warning "change only on cash" |
| 31 | 2026-07-04 | 03 Payment | OPEN | On Account tender disabled with no customer attached (correct guard) — but is there any way to ATTACH a customer on the till? If not, On Account is unusable in practice. | OPEN — investigate customer-attach affordance on /pos |

| # | Date | Submodule | Severity | Summary | Status |
|---|------|-----------|----------|---------|--------|
| 32 | 2026-07-04 | 07 Receipt | LOW | "Reprint receipt" dialog (sync-queue reprint, queue-reprint-dialog) shows "Send via WhatsApp" disabled even for a SYNCED sale that has a receipt token. The WhatsApp-enable fix (commit 3d2eb4ee) only covered the post-sale local-sale-receipt, not the reprint dialog. | OPEN — wire the same receiptToken→wa.me flow into the reprint dialog (submodule 07/10) |

---

## Live hold/recall/void dogfooding (2026-07-05, founder on prod Asala register) — FIXED

> Four cashier-reported bugs from live use of hold, recall, and the sync-queue void. All root-caused and fixed with regression tests; reviewer panel (code-reviewer + frontend-reviewer) findings folded in. web typecheck + i18n:check + 966 POS vitest tests green.

| # | Date | Submodule | Severity | Summary | Root cause | Status |
|---|------|-----------|----------|---------|-----------|--------|
| 33 | 2026-07-05 | 02 Recall | **HIGH** | **Recall showed "recalled" toast but the cart stayed empty.** | `recallCart` wrote the IndexedDB active row but nothing rehydrated the in-memory zustand cart store — only mount-time hydration did, and it doesn't re-run on recall. `bumpHeldVersion` only refreshed the held *list*. | FIXED — new `recallHeldCart(heldId)` in `use-cart-actions.ts` does the IDB move + `setCart(fromPersistedCart(active))` + re-establishes `activeTransactionId`, mirroring mount hydration. Integration test `use-cart-actions-recall.test.tsx` reproduces the exact bug + the active-cart-occupied rejection (no data loss). |
| 34 | 2026-07-05 | 02 Hold | MEDIUM | **Hold parked the cart with no way to name it** (customer/label), so the held list was unidentifiable. | Hold fired immediately; no capture UI; `notes` field existed but was never populated. | FIXED — new `hold-dialog.tsx` (optional label, Enter-to-submit, double-submit guard) → `holdActiveCart(hasLines, label)` → `holdCart(db, id, label)` overrides `notes` (blank/whitespace keeps existing). Wired via new `"hold"` overlay (pos-store), action-bar button + F2. cart-repo tests cover label override + blank-keeps-notes. |
| 35 | 2026-07-05 | 02 Held list | MEDIUM | **Held-transactions drawer UI off** (header crammed, no total, only item count + time). | Bare `SheetHeader` (no padding/border) unlike the rest of POS; row showed only count + time. | FIXED — header/body rewritten to match `queue-drawer` (border-b p-4, padded body, Inbox empty state); each row shows label (`dir="auto"`) + item count + total (via `computeTotals`, try/catch on stale tax) + time; total wrapped `font-mono tabular-nums dir="ltr"` for RTL. |
| 36 | 2026-07-05 | 02/03 Void (sync queue) | **HIGH** | **Voiding a synced sale: (a) duplicate error toasts; (b) no void-reason capture; (c) no voided-state feedback → invited a double-void.** | (a) `useVoidTransactionMutation` had no hook-level `onError`, so the global `MutationCache.onError` fired *in addition to* the component's `catch`. (b) reason hardcoded `"other"`. (c) a successful server void never wrote back to the local queue row, so it kept showing Synced + an active Void button. | FIXED — (a) `meta.suppressGlobalErrorToast` (sanctioned opt-out) → single toast; (b) reason `Select` + details `Input` (required for synced/server voids only), composed into the server `reason`; (c) durable `voidedAt` on `SaleQueueRow` + `markVoided` repo + `isVoided` view + red "Voided" badge + hidden re-void button. 409 (`ConflictException`) reconciled locally with a friendly info toast. Server-authoritative: a local-stamp IDB failure never reports a false "void failed"; a failed local (unsynced) discard keeps the dialog open to retry. Tests: markVoided, isVoided, reason-required, voided-badge, failed-void-keeps-dialog-open. |

**Reviewer-panel findings folded in:** (HIGH ×2) server-void success must never surface a false "void failed" toast on a local-stamp failure → server-authoritative try/catch; failed unsynced void must keep the dialog open → return-value check. (MEDIUM) held-list total `font-mono/tabular-nums/dir=ltr` for RTL. **Accepted-as-is with rationale:** `voidedAt` is a local per-device offline-queue field — if IDB is cleared the whole queue list is gone too, so a false "un-voided" row cannot appear (not a durability regression); cross-tab-same-register recall lock is a documented non-goal (`recallCart`'s IDB transaction already blocks same-cart double-recall); free-text `reason` composition matches the server DTO (`reason: z.string()` only — no structured reasonCode column to target).

**Pre-existing gap discovered (out of scope, flagged for founder):** order-level discounts are NOT persisted on the active/held cart — `SaveActiveInput`/`holdCart` only carry `lines/customerId/notes`, so an order discount is silently dropped on page refresh AND on hold/recall (independent of these fixes). Needs an `ActiveCartRow`/`SaveActiveInput` schema addition — separate ticket.

## Tie-out re-recon + lifecycle code audit (2026-07-04 PM, PROD Asala `zerupt_tenant_al_asala_auto_parts_mqy1wpk2` @ `br-red-term`)

> Submodule 02 autonomous re-run over all **12** completed transactions (was 11; new pack-unit sale `b9376bef` / B1SHUWAIKHREG1-2-1 on shift #2). Manual founder guide delivered: [`02-manual-test-guide.md`](02-manual-test-guide.md).

**Three-way tie-out (POS ↔ GL ↔ stock): ✅ 12 of 12 now GREEN.** Every completed tx has ≥2 balanced journal entries (Σdebit−Σcredit = 0.000000 exact), a revenue JE (`pos.transaction.completed`/`pos.return.completed`), a COGS JE (`inventory.sale`/`sale_return`), and matching signed stock-ledger movements (sales negative, returns positive). Header arithmetic exact on all 12; transactionNumber unique + monotonic.

**Finding #17 (CRITICAL) — RESOLVED on prod.** The two previously-broken transactions were **backfilled 2026-07-04** (verified live, not assumed):
- **tx-5 (`9909384f`, serial return):** now has `inventory.sale_return` JE (DR 1141 / CR 5100 = 99.000, specific serial cost) + stock **+1** restock (backfilled 14:48). Ties out.
- **tx-8 (`ec78893f`, batch sale):** now has COGS JE described "inventory.sale (backfill finding #17)" (DR 5100 / CR 1141 = **299.538459**, basis **B / current-WAC** per the runbook) + stock **−6/−5 = −11** across two lots (backfilled 16:25). Balanced, ties out.
  - **Note (not a defect):** tx-8's backfilled COGS (299.538 on WAC basis) intentionally deviates from line `costAtSale`×qty (13.000×11 = 143.000). This was the documented [runbook](../../../../study/pos/11-tie-out-backfill-and-residual-runbook.md) basis-B decision (line costAtSale was stale/wrong), not a new tie-out break.

**Pack-unit tie-out (commit 728b6406) — ✅ CONFIRMED END-TO-END** on real data (tx `b9376bef`): line stores **base qty 6** (unit_qty 1 pack × factor 6) @ 8.730; stock relief **−6 base units** (not −1 pack); COGS 28.620 (= 6 × 4.770 cost); revenue 52.380 = pack price. Findings #24/#25 verified live.

**Lifecycle code audit (hold / recall / pay / void — `pos-transactions.service.ts`): no new CRITICAL/HIGH.**
- `pay` — status-guarded UPDATE `WHERE status='draft'` (atomic double-tap no-op); empty-cart guard (`Cannot pay a transaction with no lines`); grand-total integrity assertion inside the tx; `assertBatchLotsAttributable` pre-completion oversell guard (finding #17 D1); receipt-token minted post-commit (no orphan admin-DB row).
- `hold`/`recall` — guarded UPDATE on exact prior status → concurrent double-hold / double-recall are clean no-ops; `MAX_HELD_PER_REGISTER` enforced; held excluded from revenue by status filter.
- `void` — guarded UPDATE on exact pre-read status (double-void safe); blocks void when a return already exists (no double-reversal); atomic outbox inserts for GL reversal + inventory restock; serials released atomically; draft-void emits nothing. Combined with the shipped `runDurableGated` durability, async-relief failures now dead-letter LOUDLY (no silent gap like the pre-fix #17 casualties).

**Recon SQL note:** GL tables are `journal_entries` + `journal_entry_lines` (not `journal_lines`); join key `source_document_type='pos'` (lowercase), `source_document_id = transaction.id`. Tenant accounts: 1112 Cash Register, 4110 Product Sales, 4200 Sales Returns, 5100 COGS, 1141 Merchandise Inventory.

**Verdict:** submodule 02 data-tie-out invariants all pass on the live dataset. No code fix required this pass (the one CRITICAL was already fixed + backfilled). Remaining work is the founder's manual UI run (hold/recall/void have zero live rows — created in the guide) + the open non-blocking items below.

---

## ✅ Submodule 01 — Register & Session — SIGNED OFF (2026-07-04)

Live-tested on prod-test tenant Al Asala (KWD 3dp). All register-management + shift open/close + close-reconcile paths pass after the fixes below. Register-settings fully redesigned (Registers list + detail drawer, mirroring legal-entities). Commits: 574c576f · bf1ffbbc · 3eee0a6b (+ payment/receipt fixes 3d2eb4ee · 728b6406 verified incidentally).

Findings resolved this submodule: #4 (dark dialogs), #7 (dup helper text), #9 (close-dialog scroll), #17 (settings UI redesign), #18 (auto-code), #19 (float 0.000), #20 (single inline error), #21 (deactivate open-shift guard + TOCTOU), #22 (stale close overlay), #23 (opening-float decimals). Structural DB invariants confirmed at baseline.

**Deferred (need Manager user + approval PIN + 2nd cashier — SoD batch):**
- Manager-approval-on-large-discrepancy at shift close.
- Cross-cashier "you already have an open shift" (single-cashier tenant today).
These carry into a dedicated SoD/RBAC test pass (with submodule that touches PIN approvals).

**Open non-blocking:** #1 (grandTotal DB CHECK, LOW), #16 (verify close server-authoritative — now shows "live from server", re-confirm), #31 (customer-attach affordance for On Account — MEDIUM, likely a build), #32 (WhatsApp in reprint dialog, LOW).

---

## Submodule 04 — Discounts & Promotions — recon + code audit (2026-07-05, PROD Asala `zerupt_tenant_al_asala_auto_parts_mqy1wpk2` @ `br-red-term`)

> Autonomous three-way tie-out over discount invariants + full backend/frontend code trace. Manual founder guide delivered inline (no doc). Live discounted sales present: `-1-9`, `-1-10` (0.610 off 2.440 → 1.830), plus new voided `-2-3` (same shape) and one price-override sale `-1-4`.

**Recon — discount invariants (pass/fail on live data):**

| # | Invariant | Verdict | Evidence |
|---|-----------|---------|----------|
| R1 | Line total ≥ 0 (server rejects negatives) | ✅ PASS (+ gap fixed, see #33) | Only 2 negative `line_total` rows = returns `-1-3`/`-1-5` (negative-qty by design). No non-return negative. |
| R2 | `discountPercent ∈ [0,100]`; `discountAmount ≤ line subtotal` | ✅ PASS | **No `discount_percent` column exists** — percent is UI-input only; server stores computed `discountAmount`. 0 rows with `discountAmount > qty×unitPrice`. DB CHECK `discount_amount >= 0`. |
| R3 | ≤1 order-level discount per tx (no line+order double-count) | ✅ PASS | `order_discount_amount` is a single non-negative numeric column (not a table) → trivially ≤1. `discountTotal` stays line-only; order discount deducted separately in `grandTotal` (recompute:185). |
| R4 | `priceOverride=true ⇒ priceOverrideById NOT NULL`; neg/zero handled | ✅ PASS (service-enforced) | 0 override rows with null approver. `priceOverrideById` always server-stamped = authenticated caller (never client). Negative price rejected by `moneySchema.refine(>0)`. NO DB CHECK (service-only) — logged as note below. |
| R5 | Above-threshold discount ⇒ `discountApprovedById` (SERVER check) | ✅ PASS (server) / ⚠️ config | Gate is server-side in `addLine`(:441-463) + `updateLine`(:588+) via `pinVerification.verifyApproval` requiring `pos.discountApprove`; re-verified independently on sync. **BUT all 3 registers have `max_discount_pct_without_approval = NULL` → gate DISABLED (any discount, no approval).** See #35. |
| R6 | Header identity `grandTotal = subtotal + taxTotal − discountTotal` exact | ✅ PASS | All 3 discounted tx exact (2.440 + 0 − 0.610 = 1.830, diff 0.000000). |
| R7 | GL tie-out with discount + void reversal | ✅ PASS (verified live) | Discounted sale `-1-9` JE: DR 1112 cash 1.830 / CR **4110** sales **2.440 (GROSS)** / DR **4300 Sales Discounts** 0.610 — balanced. Void `-2-3`: reversal JE credits 4300 back 0.610, DR 4200 returns 2.440 — 4300 nets to zero. **Discount account is 4300, NOT 4190 as the checklist speculated.** |

**Coupon feature status — DEFINITIVE: no coupon-code path exists.** The `promotions` table has **no `code`/`usedCount`/`maxUses`** columns — it is an *automatic* time-bound price-markdown engine (percent_off/fixed_price/amount_off, auto-applied at price resolution), living under `inventory/promotions/`, cached offline for `resolvePromoForLine`. POS backend never calls it server-side. There is **no redeemable-code apply endpoint, no redemption count, hence no over-redemption risk** (and no support for single-use codes). Checklist §1 coupon rows + §5 "coupon table not visible" gap → **resolved: feature absent by design, not stubbed/broken.** No broken coupon UI is reachable.

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 33 | 2026-07-05 | 04 Discounts | **HIGH** | **Per-line discount exceeding the line gross drove grandTotal negative → opaque 500.** `recompute` floored each line's taxable base at 0 (`pos-transactions-totals.ts:74`) but summed `discountTotal` from the **raw unfloored** `discountAmount` (:171) and computed `grandTotal = subtotal − discountTotal − orderDiscount + taxTotal` unfloored (:185). A per-line `discountAmount > qty×unitPrice` (e.g. 11 on a 10.000 line) pushed `discountTotal` past `subtotal` → grandTotal negative → tripped the `pos_transactions_grand_total_non_negative_check` DB CHECK mid-recompute (a 500, not a clean rejection), with `grandTotal ≠ Σ lineTotal`. Reachable via `addLine`/`updateLine` (moneySchema had no upper bound) and, on the offline path, would strand an already-paid sale forever (sync rollback). | POST addLine `{itemId, quantity:"1", discountAmount:"11"}` on a 10.000 item. | Server rejects a discount > line gross with a clear message (checklist §1: "discount > line subtotal → rejected, line total cannot go negative"); equality allowed (free line). | **FIXED — SHIPPED to main `7ec768c3`.** `addLine`/`updateLine` reject with coded 422 `discount_exceeds_line_total` (updateLine also fires on a qty reduction that makes an unchanged discount exceed the new smaller gross). Offline **sync clamps** (never rejects an already-paid sale) to the line gross + flags `totalsMismatch.lineDiscountClamped` for manager review. Preserves the header identity (rejects/clamps bad input, does NOT floor the total). Tests: ceiling reject/allow/qty-reduction + sync clamp-and-flag; api typecheck + pos-transactions (94) + pos-sync (50) green. **Reviewer panel (accounting + code) then caught a 3rd path — `overrideLinePrice` lowers unitPrice (thus gross) via a field the addLine/updateLine ceiling never read, reopening the same 500 on an already-discounted line — CLOSED in follow-up `6f6a4a73`** (override re-asserts the ceiling before its DB tx; +1 test, 91 pos-transactions green). All three write paths (add/update/override) + offline sync now guarded. |
| 34 | 2026-07-05 | 04 Price override | LOW | **Price override to 0 is rejected (400), not treated as a free item.** `overrideLinePrice` uses `moneySchema.refine(v => Number(v) > 0)` so a zero unit-price 400s (defined behavior, NOT a crash — checklist §1 asked "defined, not a crash": satisfied). Free items are instead achievable via a line discount = line gross (→ lineTotal 0). | Override a line unit price to `0`. | Either accept 0 as a free item, or (current) reject cleanly and steer to discount-to-zero. | **NOTED — no fix (persona decision).** Behavior is defined + safe; free-item path exists via discount. Flag only so it isn't mistaken for a bug. |
| 35 | 2026-07-05 | 04 / 01 Register config | MEDIUM | **Discount approval gate is effectively OFF — `max_discount_pct_without_approval` is NULL on all 3 prod registers.** The server gate only runs `if (maxDiscountPct !== null)` (`pos-transactions.service.ts:441`), so with NULL, ANY discount (even 100%) is accepted with no supervisor approval and `discountApprovedById` stays null. The mechanism is correct + server-enforced (verified) — it is simply un-configured. | Query `pos_registers.max_discount_pct_without_approval` → all NULL; ring a 90% discount → succeeds, no PIN. | Founder sets a per-register threshold (e.g. 10–20%) so over-threshold discounts require a manager PIN. Consider a sane tenant default at register creation. | **OPEN — config/product decision for founder** (not a code defect). Surface a threshold field default in register settings (submodule 01 redesign already has the field). |
| 36 | 2026-07-05 | 04 / 06 Offline | LOW | **Offline sync never-blocks on missing/invalid discount approval (flags for review).** By documented offline-first policy, an offline sale whose over-threshold discount lacks a valid approval token is completed + flagged in `totalsMismatch.discountApprovalFlags`, not rejected (the sale was already rung + paid on-device). GL uses server-recomputed totals so financials stay correct; only the compliance flag is deferred. | Sync an offline line with an over-threshold discount + no valid token. | Confirm this remains intended policy; ensure the manager review surface actually shows `discountApprovalFlags` / `lineDiscountClamped`. | **NOTED — accepted risk (confirm policy).** Same never-block philosophy as finding #17's redesign. A rogue offline device could apply large unapproved discounts surfacing only post-hoc — worth a monitoring/reconciliation surface. |
| 37 | 2026-07-05 | 04 Frontend UX | LOW | **No "total is zero — are you sure?" confirmation on a fully-comped cart.** A 100%-discounted / free cart can be paid with no extra confirm (`use-cart-actions.ts` / pay flow has no zero-total guard). Discount input IS in-flight-guarded against double-tap; removing a discount stores `discountAmount = "0"` (not null); no client-only approval bypass (server re-verifies all paths). | Discount every line to its gross → grand 0 → Pay. | A "Total is zero — complete anyway?" confirm before a zero-value completion (checklist §3). | **OPEN — LOW frontend build** (submodule 04 defensive-UX edge). Batch with founder UI pass. |

### Submodule 04 — recon verdict (2026-07-05)

**Discount/GL correctness: the three-way tie-out holds on live discounted + voided-discounted sales** (revenue gross to 4110, discount contra to 4300, void fully reverses 4300; header identity exact). The one real defect (#33, HIGH — negative grandTotal via oversized line discount) is **fixed + shipped + tested** (`7ec768c3`, permanent write-boundary reject/clamp, not a floor band-aid). Coupons are definitively absent (auto-markdown engine only). Remaining items are config (#35, founder threshold), accepted offline policy (#36), and two LOW UX/defensive gaps (#34 override-zero, #37 zero-total confirm) for the founder's manual UI pass.

---

## Submodule 03 — Payment Methods — recon + code audit (2026-07-05, PROD Asala `zerupt_tenant_al_asala_auto_parts_mqy1wpk2` @ `br-red-term`)

**Live-data payment recon (all 15 pos_payments rows across shifts #1 closed + #2 open):** every payment is `cash` (no card/FX/store-credit/gift-card data yet). Invariants that CAN be exercised all pass:
- **Completeness** `Σamount − Σchange = grandTotal`: ✅ exact on all rows (157.06−0; 55−2.62=52.38; 80−5.23=74.77; …).
- **change on cash only / FX / immutability:** ✅ all cash; only 2 rows carry change (both cash); all KWD (`amount_fc`/`exchange_rate` null, CHECK holds); voided tx-2-3 still has its payment row (void = reversal, not delete/mutate).
- Card-ref / non-cash-overpay / non-round-5-fils: **no test data exists** → covered by code audit only (build card/FX/mixed sales to exercise live — see manual guide handed to founder inline).

**Code audit — server pay() path is sound:** server recomputes `exactGrandTotal` from stored `txn.grandTotal` (client figure never trusted); status-guarded `UPDATE … WHERE status='draft'` (double-tap = clean no-op); grand-total integrity assertion in-tx; receipt token minted post-commit (no orphan). `validatePayments` enforces card-reference-non-blank, non-cash-cannot-overpay, under-coverage rejection, and a net=grandTotal backstop (InternalServerError if violated). **gift_card/store_credit are server-BLOCKED** (ALLOWED set = cash/card/on_account/custom) and the UI strips them from the tender list → the checklist's "store-credit/gift-card void-restore HIGH gap" is MOOT (feature not live, can't take money). JE tender→GL mapping correct (cash→cash, card→bank, on_account→receivable). No PATCH/DELETE endpoint on pos_payments (only `lines` mutate, draft-only).

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 33 | 2026-07-05 | 03 / 08 Cash reconciliation | **HIGH** | **Drawer `expectedCash` overstated by Σ(cash change_given).** `computeCashComponents` + the Z-report `paymentBreakdown` cash line summed GROSS tendered `pos_payments.amount` and never subtracted `change_given`. For an overpaid cash sale (tender 55.000, change 2.620, sale 52.380) the drawer nets 52.380 but the system expected 55.000. **Confirmed live on prod shift #2: expected computed as openingFloat 5 + gross 135 = 140, true drawer = 5 + (135−7.850) = 132.150 → phantom 7.850 KWD shortage** + a wrong cash-variance JE at close. Dormant on shift #1 (all exact tenders, zero change). Same bug existed on the **offline** shared Z-report (`computeLocalZReport` summed gross). | Ring an overpaid cash sale (tender > total), then X-report / close the shift and compare expectedCash to opening + net cash. | Cash retained = tendered − change. `expectedCash = openingFloat + Σ(cash amount − changeGiven) − Σ(cash refunds) − payOuts + payIns`. | **FIXED — commit `d8f1fede`.** Server aggregates → `sum(amount − change_given)`; offline shared → per-sale `min(Σcash, max(grandTotal − ΣnonCash, 0))` = exact identity of Σamount−Σchange, rounding-independent. Accounting + code reviewers approved (algebraic identity proven, GL unaffected). Regression tests on both server + offline paths. **NB: prod shift #2 is still OPEN — on close it will now reconcile correctly; no historical data to backfill (persisted expectedCash is only written at close).** |
| 34 | 2026-07-05 | 03 Payment (frontend) | MEDIUM | **Card overpay can be misattributed when a cash row coexists.** `pay-surface.tsx` (`canComplete`, ~172-177) blocks non-cash overpay only when NO cash row exists; with a $0.01 cash row present, an over-tendered card row passes and the excess is absorbed as "cash change" — card overpay masquerading as cash change in the ledger. Per-tender input is not capped. | Add a card tender > remaining AND any cash row, Complete. | Non-cash tenders capped at their line amount client-side (server already rejects nonCash > total, so impact is bounded — the card row alone can't exceed total, but the mixed-row attribution is still wrong). | OPEN (MEDIUM — frontend cap per tender; server backstop limits blast radius). |
| 99* | 2026-07-05 | 03 Payment (rounding) | ~~HIGH~~ → **MEDIUM (reclassified)** | KWD 5-fils rounding parity. The "Exact" button sends `roundCashAmount(due)` (e.g. 52.380 for a 52.378 total) and the client shows change via `roundCashAmount`, while the server stores unrounded `change_given` (0.002). **Does NOT corrupt the GL** — net payments always tie to `grandTotal` exactly (enforced backstop), so the JE always balances; the ≤0.002/sale sub-coin difference is absorbed as legitimate drawer over/short at close (the checklist's feared "silently stored as a different amount / GL corruption" does not occur). Residual is cosmetic: cashier may be shown "0 change" while 0.002 is recorded. | Cash sale whose total isn't a 5-fils multiple; compare shown change vs stored `change_given`. | GL-safe today. To fully close: surface the fils rounding as an explicit line, or adopt a cash-rounding account — an **accounting-policy decision for the founder**, not a silent bug. | RECLASSIFIED MEDIUM — GL-safe; documented. Deferred to founder rounding-policy call. |

\* #99 tracks the checklist "Known gap" of the same number; not a new finding-number collision with the settings-block #33/#34 elsewhere in this log.

---

## Finding #20 (CRITICAL) — never-block batch oversell never actually worked; three-way tie-out silently broke on a cash-completed sale (DEV-446, 2026-07-05)

**Supersedes Finding #17 "D1 REVERSED — never-block-and-flag (SHIPPED)".** That change hardened `BatchPickerService.pick(allowNegative)` — but a pre-launch verification pass against **real code + migrations** (not the mocked unit tests) found the POS batch-oversell flow never reaches it, and instead **rolls back the inventory relief**. The 414 green unit tests hid this because they mock the DB (the CHECK never runs) and call `pick()` directly (a path POS does not take).

**Root cause — three latent defects, all confirmed against source:**
1. **Add-line hard block** (`pos-transactions.service.ts:415-419`, untouched by the #17 diff): a batch item with no active/expiring lot holding `qty_remaining > 0` threw `"No available batch found…"`. A fully-depleted batch item could not even be added to the cart. The till refused a real sale — the exact thing never-block forbids.
2. **CHECK-constraint rollback on a partial oversell**: POS stamped an explicit FEFO `batchId` at add-line (`:421`) and `buildLineItems` (`pos-transactions-totals.ts:283`) put it on the payload, so `attributeOutbound` took the **explicit-batchId branch — which ignored `allowNegative` and never called the picker**. The async relief's `StockLedgerService.incrementBatchRemaining` does `qty_remaining + (negative delta)`; on a lot with 3 selling 10 → `3 + (-10) = -7` → violated `item_batches_qty_remaining_non_negative` (migration 0145) → the relief transaction rolled back → the outbox dead-lettered → **COGS JE + stock movement never posted while the cash sale completed and the receipt printed.** Silent three-way tie-out break (the Finding #17/#18 failure class) + a potential outbox circuit-breaker stall for the tenant.
3. The shipped `allowNegative` picker machinery (negative-on-lot, reconciliation lot, FEFO fan-out) was **unreachable from POS**.

**Founder decision (2026-07-05): Option A — engine-picks-at-relief.** Long-term-correct; accounting-reviewed money-correct **and necessary** (fils-accurate tie-out proven for both the recon-lot and negative-on-existing-lot scenarios; the change is required because the CHECK currently rolls the sale back).

**What shipped (branch `phase-4a/dev-446-pos-never-block-tie-out`):**
- **Migration 0154** drops `item_batches_qty_remaining_non_negative`. `qty_remaining` is now a pure **ledger projection** (= Σ ledger.quantity per batch) that MAY go negative on an oversold lot; a loud schema comment forbids ever re-adding the CHECK (`packages/db/src/schema/item-batches.ts`). Accounting-verified: no GL amount is ever sourced from `qty_remaining` (all costing is item-level WAC); the reconciliation detector (`qty_remaining == Σledger`) is now the sole guard and is asserted in the tie-out test.
- **POS stops selecting a lot.** Add-line no longer FEFO-picks/stamps a `batchId` and no longer throws on a depleted batch item; sale lines carry `batchId: null`. The inventory engine FEFO-picks with `allowNegative` at relief (`attributeOutbound → BatchPickerService.pick`), fanning one line across lots and placing the shortfall as a negative on a real lot / the `UNRECEIVED (auto)` reconciliation lot. The pay-time expired/exhausted batch guard was removed (never-block: complete + flag, don't block).
- **Returns/voids stay lot-correct.** Because a sale line no longer carries a single lot, `attributeInbound` for a `sale_return` restock with no lot now heals into a **real** lot via new `BatchPickerService.resolveRestockLot` (most-negative lot first — offsets the oversell — else earliest-expiry, else the reconciliation lot) instead of throwing. Fresh GRN/purchase receipts without a batch number STILL throw (a lot's expiry/recall identity must be declared, not invented). This also fixes a pre-existing bug where batch returns silently dropped the lot.
- **P0-3 explicit-lot hole closed.** The explicit-batchId outbound branch (now non-POS only — sales invoices) gains `assertExplicitLotHasQty`: a strict (`blockNegativeStock`) caller is blocked at the **lot** level when Σledger for that lot is short (`decrementOutbound` only guarded item×warehouse aggregate). Never-block callers still go negative.
- **Dead code removed:** `BatchesService.selectFefoBatch` + `decrementBatchQty` (the legacy interim POS `qty_remaining` maintainers) had zero live callers post-change — deleted with their tests; `BatchesService` removed from the POS service.
- **Channel policy (P2-9):** `NEVER_BLOCK_SOURCE_DOC_TYPES` allowlist + `blocksNegativeStock()` helper replace the scattered `!== "pos"` check — adding a mobile-POS/kiosk channel is one edit.

**Tests (all against the new behaviour):**
- **DB-backed WAC full-lifecycle tie-out** `apps/api/src/__tests__/integration/pos-never-block-tie-out.integration.spec.ts` — oversell → receive → true-up, asserting balanced JE + COGS + signed stock movement + `qty_remaining == Σledger` on negative lots + `Σ(GL 1141) == materialized total_value` to the fils, for both the recon-lot (never-received, priorWAC 0, later receive @ 5.000 → true-up 50) and negative-on-existing-lot (3 @ 2.000, oversell 10, receive 7 @ 2.000 → no true-up) scenarios, plus idempotent replay. **[result confirmed in the run log; see verification below]**
- Unit: `resolveRestockLot` (heal-most-negative + recon fallback), `attributeInbound` sale_return heal vs grn_receipt still-throws, `assertExplicitLotHasQty` guard, fan-out rounding residual (Σ per-lot totalCost == line total to the fils), recon-lot lost-insert-race convergence, async replay no-double-post, `blocksNegativeStock` allowlist. 80 POS/inventory suites (1449 tests) green.
- **writeOff gotcha:** `BatchesService.writeOff` on a negative (oversold) lot now refuses to force-zero the row (which would desync `qty_remaining` from Σledger) and asks the manager to reconcile the negative first.

**Async cash-before-relief window (P2-12) — BY DESIGN, documented for ops:** a POS sale takes cash and completes **synchronously**; the inventory leg (COGS JE + signed stock movement + negative-stock flag) posts **asynchronously** via the transactional outbox → poller → `inventory-domain.listener` (durably gated by `runDurableGated`). There is therefore a short eventual-consistency window (typically seconds, longer under outbox backlog) where the sale exists but its stock/COGS has not yet posted. This is crash-safe: the relief is an outbox row committed in the sale's own transaction, so a crash between commit and fan-out is replayed by the poller (idempotent per-line via the `:b:0` probe + deterministic eventId — no lost or doubled COGS/stock). Managers should read negative-stock state from the **Stock Levels report / reconciliation view** (ledger-derived), which reflects the relief once the outbox drains, not instantly at sale time.

**UX shipped (founder-approved):** till product grid/search now ADD an out-of-stock/negative item with a non-blocking amber "oversell" badge (never a red hard-block); a "Needs reconciliation" filter on the Stock Levels report + a "Why is this negative?" driving-transaction drawer (shows offline-origin: transaction/offline number, Offline badge, register, shift, synced-at) + optional dashboard badge; the `UNRECEIVED (auto)` reconciliation lot is badged as a system placeholder and hidden from lot pickers/actions while staying visible in reconciliation lists; and an inline **quick-add-serial-at-till** flow (receive the physical unit via `POST tenant/pos/serial-quick-add`, then continue the sale — serials never fictitious).

**Migration rollout:** 0154 auto-applies to prod tenant DBs on Railway deploy (pre-deploy `migrate-tenants`) and to the integration test DB via the idempotent migrator. Dev tenant DBs apply manually. **Not yet pushed — awaiting founder go.**

---

## Submodule 05 — Returns & Exchanges — recon + full code audit (2026-07-05, PROD Asala `zerupt_tenant_al_asala_auto_parts_mqy1wpk2` @ `br-red-term`)

> Autonomous three-way tie-out over the two live returns (`B1SHUWAIKHREG1-1-3`, `-1-5`) + full backend (`createReturn`/`createNoReceiptReturn` in `pos-transactions.service.ts`) and frontend (return-modal, no-receipt-return-modal, back-office list) trace. Manual founder guide delivered inline (no doc). **No CRITICAL/HIGH code defect found — the expected CRITICAL (concurrent-double-return ceiling) is already protected by a Postgres transactional lock.**

**Recon — return invariants (pass/fail on live data):**

| # | Invariant | Verdict | Evidence |
|---|-----------|---------|----------|
| R1 | `type=return` ⇒ non-null `originalTransactionId` → completed sale; no orphans; cannot return a return | ✅ PASS | Both originals `type=sale`/`status=completed`; service rejects non-sale original (`original.type !== 'sale'` :1329) + non-completed (:1332). |
| R2 | **Cumulative returned qty per line ≤ original sold** | ✅ PASS | −1-2 line: sold 10, returned 2, remaining 8; −1-4 line: sold 1, returned 1, remaining 0. Never exceeded. |
| R3 | Return line `unitPrice` = original sale price | ✅ PASS | Both return lines carry original unit_price (13.500 / 5.000); code copies `origLine.unitPrice` (:1570). Discounted-sale case: residual-allocation policy splits original discount/tax across partial returns exactly (:1534). |
| R4 | Cash refund decreases `expectedCash`; never increases | ✅ PASS | `computeCashComponents` aggregates `type=return`+`method=cash` payments separately and **subtracts** them (`pos-shifts.service.ts:659`); refund rows carry `changeGiven=0`. |
| R5 | Three-way tie-out (4200/1112 revenue reversal + 1141/5100 inventory + signed restock movement) | ✅ PASS | **−1-3:** DR 4200 27.000/CR 1112 27.000 + DR 1141 17.740/CR 5100 17.740 + stock +2 `sale_return` @ 8.870196=17.740392. **−1-5:** DR 4200 5.000/CR 1112 5.000 + DR 1141 99.000/CR 5100 99.000 + stock +1 @ 99.000 serial-tagged. Both balanced to the fils. |
| R6 | Serial-on-return matches original sale line | ✅ PASS (record) / ⚠️ stale-data (see #38) | −1-5 return line serial = original serial `SN-AAA-099`. Serial is **auto-derived from the original line** (not cashier-entered), so a wrong-serial is structurally impossible on receipted returns. |
| R7 | Free-item (zero-price) return: refund 0, no crash, stock restored | ✅ PASS (code) | Proportional discount/tax divisor is `origQtyAbs` (>0, sale lines never zero-qty) → no div-by-zero; lineTotal computes to 0 cleanly. No live data. |

**CRITICAL question — DEFINITIVELY ANSWERED: the concurrent-double-return ceiling is DB-lock-protected, not service-only.** `createReturn` runs the whole create inside ONE db transaction guarded by `pg_advisory_xact_lock(hashtext('pos_return:'+originalTransactionId))` (:1379); the cumulative-returned re-read happens **inside** that lock (:1388), then the per-line ceiling check (:1449). Two stations each returning the full qty: the second blocks until the first commits, then sees the qty consumed → `UnprocessableEntityException`. **No double-refund possible.** No DB CHECK constraint exists, but the advisory lock + in-lock re-read fully closes the TOCTOU race. Idempotency is separately handled (clientRequestId fast-path :1306 + partial unique index `pos_transactions_tenant_client_id_key`). Checklist §5 CRITICAL gap → **RESOLVED, not a defect.**

**No-receipt return policy — implemented as cash + manager-PIN + SoD, NOT store-credit.** The checklist assumed "store-credit only, no cash without receipt." Actual impl: no-receipt refund is `refundMethod: literal('cash')`, gated by `pinVerification.verifyApproval(pos.returnApprove)` + a hard SoD guard (`approvedBy === cashierId` → Forbidden, :1734). Store credit is **not a live feature anywhere** (consistent with submodule 03: gift_card/store_credit server-blocked). The manager PIN + SoD is the compensating control. Cost basis = current WAC; zero-WAC tracked lines flagged on `totalsMismatch.costZeroLines` (:1930). **Policy divergence is intentional given the absent store-credit feature — logged as #41 (product note), not a defect.**

**Return reason is MANDATORY** (`reasonCode` enum required; `reasonNote` required when `OTHER`) → checklist §5 LOW "reason optional?" **resolved: mandatory.**

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 38 | 2026-07-05 | 05 Serial return (data) | **MEDIUM** | **Stranded serial from a pre-fix return — physically-returned ECU can't be resold.** Serial `SN-AAA-099` (returned on `-1-5`) is still `status='sold'` with `sale_doc_id` = the original sale, so on_hand shows 3 for that item but only 1 serial is `available` + 1 stuck `sold` (should be available). **Root cause is stale data, not current code:** return `-1-5` was created 2026-07-01 **16:32**, but the `serialNumbers.release()` call inside `createReturn` (commit `07650cef`) landed **19:53** the same day — 3.5h later. Current code correctly flips sold→available and clears the sale doc inside the return tx (verified). | Query `item_serial_numbers WHERE serial_no='SN-AAA-099'` → `status='sold'`. | Returned serial → `status='available'`, `sale_doc_id=null`, so it re-enters the sellable pool. | **OPEN — one-row data backfill (approve-first SQL handed to founder).** No code change needed; current returns are correct. Optionally re-scan all pre-`07650cef` serial returns tenant-wide (only this one exists on Asala). |
| 39 | 2026-07-05 | 05 Serial return (no-receipt) | MEDIUM | **No-receipt return of a serial-tracked item restocks qty but registers no specific serial.** `createNoReceiptReturn` has no serial handling (no `originalLineId`, no `release()`, no serial creation) and the no-receipt UI captures no serial field. A serial item returned without a receipt bumps on_hand +1 at current WAC but leaves zero matching `available` serials → the same qty/serial desync class as #38, on every future no-receipt serial return. | No-receipt return a serial item; check `item_serial_numbers`. | Either capture+reactivate/create the serial, or block no-receipt returns for serial-tracked items with a clear message. | **OPEN — MEDIUM (backend + UI).** Low live incidence (no serial no-receipt returns yet); batch with a serial-inventory pass. Persona: auto-parts serial items (ECUs) DO get returned — worth fixing, not launch-blocking. |
| 40 | 2026-07-05 | 05 Exchange (UX) | MEDIUM | **No guided exchange flow — cashier chains a return + a fresh sale as two unlinked transactions; a new-sale failure after the return commits is not surfaced as a paired failure.** `exchange` exists only as a type enum + back-office filter/badge; there is no exchange component/mutation. Backend event ordering is safe (both `pos.return.completed` and `pos.transaction.completed` go through the same transactional outbox → poller → no transient-negative inventory / COGS double-count). So this is a **UX gap, not a data-integrity gap**: the two txns are each correct + idempotent, just not atomic or guided. | Do an exchange: process a return, then start a new sale; if the sale errors, nothing links it to the just-issued refund. | A guided exchange that carries the cart, computes the price difference (charge more / refund less), and surfaces a return-committed-but-sale-failed state clearly. | **OPEN — MEDIUM UX build** (founder UI batch). Auto-parts exchanges are common; manual two-step works today. |
| 41 | 2026-07-05 | 05 No-receipt policy | LOW | **No-receipt refund is cash (manager-PIN + SoD), not store credit as the checklist assumed.** Not a defect — store credit isn't a live feature; PIN+SoD is the compensating control. Logged so the divergence isn't mistaken for a bug and so the policy is revisited if/when store credit ships. | — | Confirm cash-with-approval is the intended no-receipt policy pre-launch. | **NOTED — product confirmation.** |
| 42 | 2026-07-05 | 05 Back-office traceability | MEDIUM | **Return row shows no link back to its original sale.** `originalTransactionId` is not rendered anywhere in the back-office list or detail sheet (`pos-transactions-list-panel.tsx`) — a return can be filtered by `type=return` but tracing it to the origin needs a manual transaction-number lookup. Weakens the audit trail the module's tie-out philosophy depends on. | Open a return in back-office; no "original sale" link. | Render `originalTransactionId` as a link to the original sale (+ ideally "N of M units returned" on the original). | **OPEN — MEDIUM small frontend build** (needs the list/detail API to expose original transaction_number). Batch with founder UI pass. |
| 43 | 2026-07-05 | 05 Serial return UI | LOW→N/A | **Checklist expected the cashier to enter/scan the serial on a return and the system to reject a wrong serial.** Actual (receipted) model auto-derives the serial from the original sale line and matches implicitly — a wrong serial is structurally impossible, which is *safer* than manual entry (no fat-finger mismatch at speed). No UI serial field is needed for receipted serial returns. | — | — | **RESOLVED by design** (receipted). The genuine gap is the no-receipt path → tracked as #39. |

### Submodule 05 — recon verdict (2026-07-05)

**Returns are money-correct and race-safe on live data.** Three-way tie-out holds on both live returns (revenue reversal to 4200, inventory restock 1141/5100, signed stock movement at costAtSale — balanced to the fils); the cumulative-return ceiling is protected by a real Postgres advisory transaction lock with an in-lock re-read (the feared concurrent-double-refund CRITICAL is closed); idempotency, original-price copy, cash-refund→expectedCash subtraction, and mandatory return reason all pass. The frontend return + no-receipt flows are reachable and well-guarded (returnable-balance cap, positive-qty input, retained clientRequestId, disabled-while-pending, currency-precision-safe, bidi-isolated). **No code fix shipped this pass** — the only live-data defect (#38, one stranded serial) is a pre-fix stale row fixed by an approve-first backfill; the remaining items (#39 no-receipt serial, #40 exchange UX, #42 back-office link) are MEDIUM builds for the founder's UI batch, and #41/#43 are product/design notes.

---

## Submodule 05 — founder-review fixes SHIPPED (2026-07-05, commit `d8edf92f` on main)

Founder dogfooded the live return flow on Asala and flagged 8 issues (screenshots). All fixed this pass, reusing existing primitives (recon-first, 5 parallel implementation agents + 1 test-fixture agent, non-overlapping file ownership). Migration `0155` (register column) landed alongside the concurrent sales work in `cb4e2c52`; the consuming code is in `d8edf92f`. Verified: api typecheck + 419 POS tests green (added no-receipt flag-on/off + missing-credential coverage); web typecheck + i18n:check + POS vitest green (fixtures updated for Dialog/AlertDialog/useRegisterQuery).

| Founder issue | Fix | Files |
|---|---|---|
| Back-office detail sheet opens from the LEFT | now opens from `side="end"` (right in LTR, left in RTL — already CSS-logical) | pos-transactions-list-panel.tsx |
| Money at 2dp on the detail sheet (KWD is 3dp) | pass `tx.currency`/`pmt.currency` into `displayMoney` (formatter was already precision-aware) | pos-transactions-list-panel.tsx |
| Qty shown raw `-1.000000` | `formatQuantity(line.quantity, line.quantityDecimals, locale)`; API now returns per-line `quantityDecimals` (buildDetail items join) | list-panel + pos-transactions.service.ts + dto |
| Register shows a raw UUID instead of the name | `buildDetail` now joins `posRegisters` → `registerName` populated (was null → fell back to id) | pos-transactions.service.ts + dto |
| No link from a return to its original sale (finding #42) | new `originalTransactionNumber` on the detail response + a link opening the original | service + dto + list-panel |
| Return-with-receipt lookup was a bottom bar ("very bad") | converted `Sheet side=bottom` → centered `Dialog` (mirrors return-modal) | return-lookup-drawer.tsx |
| Clearing a return qty → "something went wrong" page; qty-0 → "Request validation failed" | `normalizeQtyInput` (`""`/NaN → "0") everywhere a qty feeds `new Decimal(...)`; `!hasNoReturnableItems` added to `canConfirm` (Confirm no longer clickable when all returned) | return-modal.tsx |
| Serial item at till shows price 0; no serial picker | **price 0 is a DATA state** (item `sellingPrice=0`, not a bug) → added a zero-total confirm guard (also closes finding #37); replaced the free-text serial input with a searchable + scannable **available-serial picker** (`AsyncCombobox` over new `GET tenant/pos/catalog/serials`) | pay-surface.tsx, serial-capture-dialog.tsx, use-available-serials.ts, pos-catalog.* |
| "Put in a proper exchange flow" | **new guided exchange**: Exchange action → return lookup+modal in exchange mode → on refund, cart clears to a fresh sale + an in-progress banner; the pay flow settles the price difference both ways | action-bar.tsx, register-shell.tsx, exchange-banner.tsx, return-lookup-drawer.tsx, return-modal.tsx |
| Remove mandatory manager approval on no-receipt (sole-owner) | **settings-gated like void**: new `pos_registers.require_approval_for_no_receipt_return` (default **off** → sole owner passes through); opt-in enforces SoD + manager PIN. New register-settings "Approvals" section (also surfaces the discount threshold → closes finding #35) | pos-transactions.service.ts, dto, schema/pos.ts (mig 0155), no-receipt-return-modal.tsx, register-form-*, register-detail-drawer.tsx |

**Finding status updates:**
- **#40 (exchange UX) — FIXED** (guided flow shipped).
- **#42 (back-office original-sale link) — FIXED.**
- **#37 (zero-total confirm) — FIXED** (guard on the pay surface).
- **#35 (discount approval threshold un-configured/no UI) — UI SHIPPED** (field now editable in register settings; founder still sets the value).
- **#38 (stranded serial) — RESOLVED** (founder ran the backfill; SN-AAA-099 now `available`).
- **#39 (no-receipt return of a serial item registers no specific serial) — STILL OPEN.** The new serial picker is for the SALE flow; `createNoReceiptReturn` still restocks qty without a specific serial. Lower priority (needs a manager + is rare); tracked for a serial-inventory pass.
- **#41 (no-receipt = cash + PIN, not store credit) — unchanged** (store credit still not a live feature; now the PIN itself is optional per register).

**Note (not a defect):** the ECU Test Unit has `selling_price = 0.000000` in the catalog — the founder should set a real selling price (or it will keep ringing at KWD 0, now behind the zero-total confirm).

---

## ✅ Submodule 05 — Returns & Exchanges — SIGNED OFF (2026-07-06)

Live-tested end-to-end on prod-test tenant Al Asala (KWD 3dp) + full backend/frontend code audit + founder dogfooding. All CRITICAL/HIGH pass; the founder-flagged UX issues are fixed and shipped to `main`.

**Three-way tie-out (POS ↔ GL ↔ stock) — GREEN across all tracking types on live data:**
- **none:** `-2-11` 157.060 vs `-1-1`; `-2-10` 200.980 vs `-1-11` — balanced 4200/1112 + 1141/5100 + signed restock.
- **serial:** `-1-5` ECU (serial auto-matched, released back to available after the #38 backfill).
- **batch:** `-2-12` −5 @ 65.000 vs `-1-8` — DR 4200 325 / CR 1112 325 + DR 1141 65 / CR 5100 65 (5×13.000); stock +5 `sale_return` **healed into a real active lot** (`resolveRestockLot`, finding #20) — not a null/orphan lot.

**Invariants proven (recon + live):** original-price refund, cumulative-qty ceiling via `pg_advisory_xact_lock` (no concurrent double-refund), cash-refund→expectedCash subtraction (card refund does not touch the drawer), mandatory reason, idempotent double-tap, exact-number lookup.

**Founder-review fixes shipped this pass (commits `d8edf92f`, `3276a4f5`, `a292d7c9`):**
- Back-office detail sheet: right-side (RTL-aware), KWD 3dp money, clean qty, register name, original-sale link.
- Return modal: empty-qty crash fixed, Confirm disabled when nothing returnable, qty-0 lines never sent.
- Lookup drawer → centered modal; **exact transaction-number match** (was substring→wrong sale, UI-only; backend records were always correct — verified live).
- Guided exchange flow; searchable/scannable serial picker; zero-total confirm guard.
- No-receipt approval settings-gated (default OFF, sole-owner passes through) + register "Approvals" section (also surfaces discount threshold, closes #35).
- Item-edit unblock when tracking is locked (`item-form-panel` schema `.catch`).

**Deferred / open (not blockers):**
- **#39** no-receipt return of a *serial* item restocks qty without a specific serial — rare; serial-inventory pass.
- **No-receipt approval-ON path** needs a manager user + PIN → batch with the SoD/RBAC pass (same as submodule 01 deferred manager-approval items).
- **RTL (Arabic)** visual pass of the return modal — pending a locale switch.
- Data note: ECU Test Unit `selling_price` is 0 in the catalog (founder to set); item-edit is now unblocked.

**Verdict:** submodule 05 data-tie-out + returns/exchange behavior are money-correct and race-safe on the live dataset. Signed off.

---

## Submodule 06 — Offline Mode — recon + full backend & frontend code audit (2026-07-06, PROD Asala `zerupt_tenant_al_asala_auto_parts_mqy1wpk2` @ `br-red-term`)

> Autonomous offline-invariant recon over all 14 synced-offline rows (`offline_number IS NOT NULL`, `OFF-B1SHUWAIKHREG1-*`) + full audit of the sync backend (`pos-sync.service.ts` / `.dto.ts` / `pos-sync-totals.ts` / `normalizeOfflinePayments`) and the frontend offline engine (`features/pos/offline/` — connectivity, sync-engine, cart-repo, queue-drawer, staleness, pay-surface). The two anticipated headline defects (store-credit client-only CRITICAL; 5-retry logs-only HIGH) both proved FALSE. One real defect found and fixed (is_offline provenance).

**Recon — offline invariants (pass/fail on live data):**

| # | Invariant | Verdict | Evidence |
|---|-----------|---------|----------|
| O1 | Every synced-offline row has non-null `offline_number` + `client_id`; `client_id` unique in tenant | ✅ PASS | 14 rows all have `OFF-` numbers + client_id; 23/23 client_ids distinct; partial unique index `pos_transactions_tenant_client_id_key ON (tenant_id, client_id) WHERE client_id IS NOT NULL` present + enforced. |
| O2 | Server recomputes totals at sync; mismatch stored/flagged, never silently accepted or rejected | ✅ PASS | `recompute()` is canonical (`pos-sync.service.ts:751`); `buildTotalsMismatch` writes to `totals_mismatch` jsonb, never throws (`pos-sync-totals.ts:88`). All 14 live rows `totals_mismatch=null` (client math tied out). |
| O3 | Three-way tie-out on synced-offline sales identical to online (balanced JE + COGS + signed stock) | ✅ PASS | All 14 rows: `Σdebit=Σcredit` on every JE, COGS JE + stock movement present per line. E.g. `-1-1` 244.980=244.980; `-1-8` 1014.538459 balanced, 2 stock moves. |
| O4 | Receipt token not minted until post-sync (transaction_number assigned) | ✅ PASS (structural) | Offline rows live only in IndexedDB pre-sync — never reach the DB or admin `receipt_tokens` until the sync insert assigns `transaction_number` + `receiptToken` in the same tx (`pos-sync.service.ts:673,685`). No pre-sync token can exist. |
| O5 | store_credit/gift_card never on any offline payment line | ✅ PASS | All 14 offline sales are `method=cash` only. Server double-blocks: Zod `enum(["cash","card","on_account","custom"])` (`pos-sync.dto.ts:102`) + `normalizeOfflinePayments` re-checks `ALLOWED_PAYMENT_METHODS` and throws `UnprocessableEntityException` on the sync path (`pos-transactions-payments.ts:158`). |
| O6 | Voided-offline: sale + void synced FIFO → net-zero GL + stock | ✅ PASS | `-2-3` (voided): Cash 1112 1.830 DR=CR, Inventory 1141 1.590 DR=CR, Product Sales 4110 2.440 wash, Returns 4200 2.440 wash, Discounts 4300 0.610 wash, COGS 5100 1.590 DR=CR; 2 stock moves (relief + restock). Full net-zero. |

**CRITICAL question — DEFINITIVELY ANSWERED: the store-credit/gift-card offline reject is SERVER-ENFORCED, not client-only.** Two independent server layers on the sync path reject these tenders (Zod enum at the controller + `normalizeOfflinePayments` allowlist re-check, comment "B1: Guard against tampered/legacy payloads") — a tampered/older client that queues such a line is rejected at replay with a 422, not persisted. Checklist §5 CRITICAL → **RESOLVED, not a defect** (consistent with submodule 03: gift_card/store_credit are not a live feature).

**Anticipated HIGH (5-retry surfacing logs-only) — FALSE.** `MAX_ATTEMPTS=5` (`sync-engine.ts:76`); on exhaustion the sale sits in `Failed` status with `syncError` persisted in IndexedDB and is surfaced in a real UI — the `queue-drawer.tsx` Sheet lists every queued/failed sale with error text, per-item Retry, Retry-All, and a `FailedSyncReview` component. Not console-only. Downgraded to MEDIUM (#45 — no cross-register back-office aggregation).

**Frontend engine — sound.** Offline indicator = server-ping-based (3-failure threshold, not raw navigator.onLine) driving a header `sync-status-pill`; in-progress cart persisted to IndexedDB `activeCart` (survives refresh/tab-close); FIFO drain order shift-open → cash-movements → sales(by createdAt) → shift-close, with sales blocked behind a Failed shift-open; cache-staleness banner at 1h (configurable, tiers 1h/2h/4h); client tender block mirrors the server allowlist (defense in depth).

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 44 | 2026-07-06 | 06 Offline sync | **HIGH** | **`is_offline` provenance silently lost — server trusted the client field.** Both offline-replay sync inserts wrote `isOffline: input.isOffline`; the client wasn't stamping it, so **all 14 synced-offline prod rows persisted `is_offline=false`** despite a correct `offline_number`. No GL/tie-out impact (no money sourced from the flag), but it breaks offline reporting/reconciliation filters and the finding-#20 "offline-origin / Offline badge" UX (which keys off `is_offline`). | Query `pos_transactions WHERE offline_number IS NOT NULL` → all `is_offline=false`. | The sync endpoints are offline replays by definition → server must force `is_offline=true`, not trust a client boolean. | **FIXED — commit `c6870e3a` on main.** Both inserts (`pos-sync.service.ts:188,682`) force `isOffline: true`; DTO field kept but marked accepted-but-ignored; regression tests on both shift-open + transaction inserts (51/51 sync suite green). code-reviewer + nestjs-reviewer approved. **Historical backfill pending founder go (see below).** |
| 45 | 2026-07-06 | 06 Offline sync UX | MEDIUM | **No cross-register back-office "pending sync" panel.** Failed-sync visibility is per-device only (the till's own `queue-drawer`). A manager cannot see stuck transactions across registers from one place; discovering a failure requires physically opening the stuck till. | Fail a sync on register A; check back-office for a cross-register pending list. | An aggregated back-office surface listing unsynced/failed items across registers. | OPEN — UI build for the founder's back-office batch (persona-acceptable short-term: the cashier at the till DOES see + can retry the failure). |
| 46 | 2026-07-06 | 06 Shift close | MEDIUM | **Shift-close-with-unsynced is a cashier self-acknowledge checkbox, not a manager gate.** `shift-close-panel.tsx:241` allows close when `!hasUnsynced || acknowledged`; any cashier can tick `unsynced-ack` and close with the queue stranded. Held carts DO hard-block (no override). | Offline, queue a sale, open close panel, tick the unsynced-ack box, close. | Per POS approval-gates-settings-optional: an optional per-register manager-PIN gate on close-with-unsynced (default off; never hard-strand the cashier). | OPEN — align with the deferred manager-approval/RBAC batch (submodule 01). Warn-not-block is correct for persona; the gap is self-ack vs manager-ack. |
| 47 | 2026-07-06 | 06 Clock skew | LOW | **No `offline_created_at` audit column.** Client `completedAt` (business event time) IS preserved verbatim on the row; `created_at` is server `NOW()` at insert. There is no separate offline-queued-at field. | — | Checklist §67 wants server NOW() for final createdAt while preserving a client offline timestamp — the business-time half (`completedAt`) is preserved; a distinct queued-at audit field is absent. | NOTE (by design) — clock-skew invariant satisfied via `completedAt`; a dedicated offline-queued-at audit field is a nice-to-have, not a defect. |
| 48 | 2026-07-06 | 06 Sync FIFO | LOW | **FIFO cross-shift ordering is enforced reactively (404-retry), no server dead-letter.** A transaction referencing an un-synced shift gets `NotFoundException` → client retries after shift-open replays; no orphan rows are ever created. A permanently-offline device would loop 404s with no server-side dead-letter. | — | Acceptable: no orphans, client retries indefinitely; the till's queue-drawer surfaces the stuck item. | NOTE — acceptable for persona (never-orphan holds); server dead-letter is a future hardening item. |

### Submodule 06 — recon verdict (2026-07-06)

**Offline mode is money-correct and idempotent on live data.** The load-bearing offline invariants all hold: exactly-once idempotent replay (fast-path by `(tenant, client_id)` + partial-unique-index backstop returning the existing row, never a 500/duplicate); server-recomputes-totals with never-block mismatch flagging to `totals_mismatch`; three-way tie-out on all 14 synced-offline sales (balanced JE + COGS + signed stock, identical to online) including a clean net-zero voided-offline; and the store-credit/gift-card offline reject is **server-enforced on the sync path** (the feared client-only CRITICAL is closed). The frontend engine (server-ping connectivity, IndexedDB cart persistence, FIFO drain, 5-retry queue-drawer with manual retry, 1h staleness banner) is well-built. **One real defect (#44 is_offline provenance) fixed + shipped to main this pass**; #45/#46 are MEDIUM UI builds for the founder's back-office/approvals batch; #47/#48 are LOW by-design notes.

**Founder to-do — historical backfill (#44):** 14 prod rows carry `is_offline=false` with a valid `offline_number` (pre-fix data; the code fix only affects new syncs). One-off, low-risk, per tenant DB — NOT a schema migration:
```sql
-- Transactions: offline_number is unambiguous proof of offline origin → safe blanket backfill.
UPDATE pos_transactions SET is_offline = true WHERE offline_number IS NOT NULL AND is_offline = false;  -- 14 rows on Asala
```
**Shifts are NOT a safe blanket backfill.** Both live shifts (#1 closed, #2 open) carry `client_id` but `is_offline=false`; a `client_id` alone does not prove offline origin (the online open-shift path also sets one — there is no `offline_number` equivalent on `pos_shifts`). Do NOT blanket-update `pos_shifts.is_offline`; confirm per-shift whether it was actually opened offline first (both Asala shifts were most likely opened online → no shift backfill needed).

**✅ Backfill DONE (2026-07-06):** founder ran the transactions UPDATE on prod. Re-verified: 18 offline rows (dogfooding added more), all 18 now `is_offline=true`, zero still false. New syncs post-fix already carry `is_offline=true` → code fix confirmed live. `pos_shifts` left untouched (correct).

---

## Submodule 06 — founder dogfooding fixes SHIPPED (2026-07-06, commit `b49ae9e4` on main)

Founder ran the manual guide on live Asala (offline → ring → refresh → void → reconnect → sync). Everything tied out; three UI observations surfaced, two were real defects (fixed), one was working-as-designed.

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 49 | 2026-07-06 | 06 Register layout | **HIGH** | **Cart column vanishes below 1024px with no fallback — checkout impossible on a narrow screen.** `register-shell.tsx` cart column was `hidden ... lg:flex`; the `<lg` bottom bar rendered only `ActionBar` (Hold/Recall/Return/Exchange) — no line items, no total, no PAY. A stale code comment promised "a compact bottom bar with PAY button" that was never built. Not offline-specific (affects online too). | Shrink the POS viewport below 1024px (tablet landscape / split-screen / docked DevTools) in BUILD phase. | Cashier can always see the cart + pay on any supported width. | **FIXED — `b49ae9e4`.** New `<lg` compact bar (item count + grand total via shared `formatCurrency`, PAY gated on `hasLines`) that opens the **unmodified** `CartPanel` in a bottom `Sheet`; PAY enters SETTLE via the same `handlePay`; whole block gated on `phase==="build"` (no double-render with the PaySurface overlay). Desktop layout untouched. frontend-reviewer approved (checks a–g pass). |
| 50 | 2026-07-06 | 06 Offline void | MEDIUM | **Voiding a never-synced offline sale hard-deleted it with no reason and no trace** — conflicts with "immutable audit log for every mutation", and if the customer already holds the printed `OFF-` receipt, that number now maps to nothing server-side. Mechanically it was safe (no orphan, no 404 loop). | Offline, complete a sale, open the sync queue, Void it → green toast, row vanishes silently. | Same reason capture as an online void + a durable local record; never a fully traceless discard. | **FIXED — `b49ae9e4`** (founder decision: hard-delete + require reason + tombstone). Pending void now requires the SAME `VOID_REASONS` reason/note form as the synced void; writes an immutable IndexedDB tombstone (new Dexie table `voidedQueueTombstones`, additive v5) **before** the delete (tombstone-then-delete; if the tombstone write fails the row is NOT deleted). A synced/syncing row throws a tagged `SaleAlreadyProcessingError` → routes the cashier to the server void instead of a misleading "try again". Dexie upgrade verified additive/safe (no wipe of queued sales); frontend-reviewer approved. |
| 51 | 2026-07-06 | 06 / 07 Receipt | LOW | **Post-sync reprint shows "DUPLICATE".** The `OFFLINE` badge correctly disappears once the sale gets its real number + barcode; the DUPLICATE band appears because the queue-drawer reprint hardcodes `isDuplicate={true}`. The at-checkout receipt is NOT a duplicate. WORKING AS DESIGNED — BUT the first receipt carrying the *real* server number can only be produced via that reprint dialog (the at-sale print only had the offline device number), so it always reads DUPLICATE. | Sync an offline sale, reprint from the queue drawer. | Some MENA tax-invoice rules want the customer's kept copy to be an "original"; consider a per-sale print counter so DUPLICATE applies only from the 2nd real-number print. | NOTE — product/compliance decision for the founder (no print-count tracking exists today). Not a defect in the offline flow. |

---

## ✅ Submodule 06 — Offline Mode — SIGNED OFF (2026-07-06)

Full offline-invariant recon over all synced-offline rows on live Asala (KWD 3dp) + complete backend (`pos-sync.*`) and frontend (`features/pos/offline/*`) code audit + founder dogfooding end-to-end (offline → ring 5 → refresh → void → reconnect → FIFO sync → verify) + re-test of both shipped fixes. All CRITICAL/HIGH pass.

**Load-bearing offline invariants — GREEN on live data:**
- **Exactly-once idempotent replay:** fast-path by `(tenant, client_id)` returns the existing row; partial-unique-index backstop (`pos_transactions_tenant_client_id_key`) catches the race — never a 500, never a duplicate (23/23 client_ids distinct).
- **Server recomputes totals; never-block mismatch flag:** canonical `recompute()`; divergence stored to `totals_mismatch` jsonb, never rejected, never silently accepted (all live rows tied out clean).
- **Three-way tie-out (POS ↔ GL ↔ stock)** on every synced-offline sale — balanced JE + COGS + signed stock, identical to online — including a clean net-zero voided-offline (`-2-3`).
- **Receipt token not minted pre-sync** (offline rows live only in IndexedDB until the sync insert assigns `transaction_number` + token).
- **store_credit / gift_card offline reject is SERVER-ENFORCED** on the sync path (Zod enum + `normalizeOfflinePayments` allowlist re-check → 422) — the feared client-only CRITICAL is closed.

**Fixes shipped this pass (main):**
- **#44 (HIGH)** `is_offline` provenance — server now forces the flag on offline-replay endpoints (`c6870e3a`); prod backfilled (18/18 rows now flagged).
- **#49 (HIGH)** narrow-screen cart — compact total+PAY bar + bottom-sheet CartPanel below 1024px (`b49ae9e4`); founder re-tested ✅.
- **#50 (MEDIUM)** offline pending-void — reason-gated + immutable local tombstone before delete (`b49ae9e4`); founder re-tested ✅.

**Open (not blockers — batched for the founder's back-office/approvals UI pass):**
- **#45** no cross-register back-office pending-sync panel (per-device visibility only; the till DOES see + can retry).
- **#46** shift-close-with-unsynced is cashier self-ack, not manager-gated (align with the deferred approvals/RBAC batch; warn-not-block is correct for persona).
- **#47/#48** LOW by-design notes (no `offline_created_at`; FIFO enforced reactively via 404-retry, no server dead-letter).
- **#51** post-sync reprint always reads DUPLICATE — product/compliance decision (MENA tax-invoice "original" nuance); no print-count tracking exists.

**Verdict:** offline mode is money-correct, idempotent, and never-block on the live dataset; the two dogfooding UI defects are fixed and founder-verified. Signed off.

---

## Submodule 07 — Receipt Model — recon + full backend & frontend code audit (2026-07-06, PROD Asala tenant `zerupt_tenant_al_asala_auto_parts_mqy1wpk2` + admin `zerupt_admin`, both @ `br-red-term`)

> Cross-DB recon over both the tenant DB (`pos_transactions.receipt_token`, `pos_receipts`) and the admin DB (`receipt_tokens`), plus a full code trace of the token-mint path (online `pay()` + offline sync), the public `/r/[token]` controller/page, the receipt render components, and the reprint / gift / WhatsApp actions. The anticipated headline CRITICAL (public-page tenant/PII leak or token enumeration) proved FALSE — the token model is sound. The one real gap: **returns mint no token → no public receipt for refunds.** #51 (DUPLICATE-on-reprint) resolved with a definitive policy.

**The token model (as-built, corrected vs handover):**
- Admin `receipt_tokens` is a minimal **token→tenant routing map**: `(token uuid PK, tenant_id uuid FK→tenants restrict, created_at)`. No transaction ref, no PII, no expiry (`packages/db-admin/src/schema/receipt-tokens.ts`). Index on `tenant_id`.
- Tenant `pos_transactions.receipt_token` (uuid) holds the same value per sale. The public page resolves tenant via the admin PK, then scopes the tenant-DB query by **both** `tenant_id` AND `receipt_token` — cross-tenant leak is impossible by construction.
- Token = `crypto.randomUUID()` (UUIDv4, ~122 bits) — opaque, non-sequential, unguessable.

**Recon — receipt invariants (pass/fail on live data):**

| # | Invariant | Verdict | Evidence |
|---|-----------|---------|----------|
| R1 | Token exists iff completed **sale** (numbered); none for drafts; tokens match across both DBs | ✅ PASS | All 18 `type='sale'` rows have a token; the 18 tenant `receipt_token`s **byte-for-byte match** the 18 admin `receipt_tokens.token` for tenant `3ae56ff4…` — zero orphans, zero missing. (Returns carry no token — see #52.) |
| R2 | No orphan admin token pointing at a missing tenant row; token unguessable | ✅ PASS | 18 admin tokens = 18 tenant tokens (set-equal). All UUIDv4. |
| R3 | Public `/r/[token]` is `@Public()` but token-scoped + minimal projection (no cost/WAC/margin, no internal IDs) | ✅ PASS | `public-receipts.controller.ts` `@Public()` + `ParseUUIDPipe(v4, 404)`; `toPublicResponse()` explicitly drops `transactionId/cashierId/customerId/receiptToken`, strips customer `phone`, `taxGroupId`. No cost field exists in any receipt DTO to leak. |
| R4 | 404 on bad/unknown token (no enumeration, no stack trace); `Cache-Control: no-store` | ✅ PASS | Malformed → uniform 404 via pipe; unknown → `NotFoundException`; bad-token log redacted to first 8 chars. `@Header("Cache-Control","no-store, private")`; web page `force-dynamic` + `cache:"no-store"` + `robots:{index:false}`. |
| R5 | Token minted post-commit, non-blocking, idempotent on replay (no 2nd token on re-sync) | ✅ PASS | Online: token generated pre-tx (L877), stamped in the completing tx (L883), `enqueueRegistration` awaited **after** commit (L1126). Offline sync: idempotency fast-path by `(tenant, client_id)` returns existing row with **no** token generation; generate+enqueue live only in `insertAndReconcile` on a genuinely new insert; admin insert is `onConflictDoNothing` + pg-boss `id=token`. Three-layer idempotent. |
| R6 | Money renders in tenant functional currency at correct precision (KWD 3dp), bilingual + RTL | ✅ PASS (via Intl) | Receipt docs format via `formatCurrency`→`Intl.NumberFormat({style:'currency',currency})` (KWD⇒3dp from ICU). Bidi via `@/lib/bidi` (`getContentDir`/`isolateText`), Arabic duplicate lines `dir="rtl"`. NOTE: uses Intl, not the shared `currency-precision.ts` map — see #55. |

**#51 (DUPLICATE-on-reprint) — DEFINITIVELY RESOLVED as by-design (not a code change).** Contrary to the handover, a per-sale reprint counter **does exist**: `pos_receipts(reprint_count, last_reprint_at, last_reprint_by_id)`, seeded at online completion with `reprint_count=0`, incremented atomically by the reprint endpoint (`pos-receipt.service.ts` `reprint()` — `sqlIncrement()` UPDATE with defensive UPSERT). The online receipt-document correctly derives the marker from `isReprint || reprintCount>0` → **first customer copy = ORIGINAL, 2nd+ = DUPLICATE** (correct MENA tax-invoice policy). The hardcoded `isDuplicate={true}` in `queue-reprint-dialog.tsx:91` is **correct-by-design**: the queue drawer is a *reprint* surface accessed after the fact (anti-fraud — checklist §4 wants reprints distinguishable to block duplicate-copy refund claims). The customer's ORIGINAL is the at-checkout receipt (`local-sale-receipt.tsx`, `isDuplicate` default false); for an offline sale that original is legitimately **provisional** (OFF- number, "sync pending — QR after reconnect"), which is standard offline-invoicing behavior. **No print-counter build for offline is needed (YAGNI for a Kuwait auto-parts till).** #51 → CLOSED by-design.

**Live data:** `pos_receipts` has **0 rows** — expected: there are zero online `pay()` sales in this dataset (all 18 sales are offline-synced, and the sync path stamps `receipt_token` but does **not** seed a `pos_receipts` row — see #56); the online path's completion-time seed (L914) has simply never fired here. The reprint UPSERT lazily creates the row on first reprint, so no functional gap.

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 52 | 2026-07-06 | 07 Receipt / returns | **MEDIUM** | **Returns mint no `receipt_token` → no public `/r/` receipt for refunds.** Both return completion paths (`pos-transactions.service.ts:1479` receipt-return, `:1846` no-receipt-return) insert via `insertWithTransactionNumber` and never stamp a token or seed `pos_receipts`. All 9 live return rows (`type='return'`, `-1-3,-1-5,-2-6..-2-12`) have `receipt_token=NULL`. Checklist §4 expects a public return receipt (states it is a return, shows original tx#, negative qtys — the receipt-document already renders `type='return'`). No broken-QR risk (no QR is ever shown for returns; it is simply absent), so it is a feature gap, not a broken behavior. | Complete a return; look for a public receipt QR/URL. | A refund gets a scannable public receipt, symmetric with sales. | **FIXED — commit `36c2dfaf` on main** (founder chose "mint for returns"). Both return methods now mirror `pay()`: `generateToken()` after the idempotency fast-path, `receiptToken` stamped atomically on the return header, `enqueueRegistration(token, tenantId)` awaited post-commit (non-blocking, idempotent). Public service is type-agnostic (`WHERE tenantId AND receiptToken=token`) so returns resolve with no new leak. Reviewer panel CLEAN (code-reviewer + nestjs-reviewer + security-reviewer, 0 findings; security confirmed no approver/PII/cost exposure — `returnApprovedById` is not in the public projection). Tests: mint-once + enqueue-with-(token,tenantId) + no-mint-on-replay on both paths (return suite 15 passed, no-receipt 23 passed). **Founder to verify live: complete a return → the new `/r/<token>` receipt resolves + shows negative qtys.** Historical: the 9 pre-fix return rows stay token-less (no backfill; refunds already handed a local slip). |
| 53 | 2026-07-06 | 07 Public rate limit | LOW | **Public `/r/[token]` rate limit is per-IP, not per-token.** `@Throttle({ttl:60_000,limit:20})` uses the default IP key (controller comment confirms "per IP"), so a distributed scraper is not per-token bounded. Checklist §63 wanted per-token keying. | 21 reqs to one token from >1 IP. | Per-token 20/min. | NOTE — negligible risk: 122-bit UUIDv4 tokens make enumeration infeasible regardless; per-IP throttle still caps any single host. Persona-acceptable. |
| 54 | 2026-07-06 | 07 Public projection | LOW | **`cardLast4` + `cardType` render on the unauthenticated public receipt.** Standard receipt practice (physical receipts print last-4), and the URL is guarded by a 122-bit unguessable token, but it is card-adjacent data on a no-auth surface. | Open any card-paid sale's `/r/token`. | — | NOTE — matches physical-receipt norms; token unguessability is the control. Persona-acceptable; revisit if a formal e-invoice/privacy regime applies. |
| 55 | 2026-07-06 | 07 Currency precision | LOW | **Receipt display precision comes from `Intl`/ICU (`formatCurrency`), not the shared `currency-precision.ts` domain map.** Both yield KWD 3dp today; the drift risk is only for currencies where ICU and the domain map disagree (e.g. IQD) — the exact drift the `currency-precision.ts` header warns about. | — | Single precision source across money-math and receipt display. | NOTE — no live impact (KWD correct); track as a consistency item for the currency-hardening pass. |
| 56 | 2026-07-06 | 07 Offline receipt seed | LOW | **Offline sync stamps `receipt_token` but does not seed a `pos_receipts` row** (online `pay()` seeds it at L914; sync `insertAndReconcile` does not). So offline sales have a public token but no print-tracking row until a reprint. | Sync an offline sale; query `pos_receipts` for it. | Symmetric seeding, or acknowledged lazy-create. | NOTE — reprint()'s defensive UPSERT (`pos-receipt.service.ts:90`) creates the row on first reprint; no functional gap. Optional: seed in the sync insert for symmetry. |

### Submodule 07 — recon verdict (2026-07-06)

**The receipt model is tenant-isolated, leak-free, and money-correct on live data.** The load-bearing 07 invariants all hold: token minted post-commit + non-blocking + three-layer idempotent (no 2nd token on re-sync); admin `receipt_tokens` is a minimal token→tenant routing map with byte-for-byte set-equality against the 18 tenant sale tokens (zero orphans/missing); the public `/r/[token]` surface is `@Public()` but token-scoped to a minimal projection (no cost/WAC/margin, no internal IDs, phone stripped), 404s uniformly on bad/unknown tokens with no enumeration or stack trace, sets `Cache-Control: no-store`, and tokens are 122-bit UUIDv4; money renders KWD 3dp bilingual/RTL; gift receipt hides prices/totals but shows items; WhatsApp shares only the token URL client-side (no PII, non-blocking). **#51 resolved by-design** (a real reprint counter exists; queue-drawer DUPLICATE is correct anti-fraud behavior; the at-sale receipt is the original). **#52 FIXED + shipped this pass** (`36c2dfaf` — returns now mint tokens; reviewer panel clean; awaiting founder live-verify of a return's public receipt). #53–#56 are LOW notes, persona-acceptable. No CRITICAL/HIGH found in the token/public-page recon.

### Founder dogfooding — additional findings (2026-07-06, live prod)

Founder rang online sales on live Asala and hit UI defects the recon (structural + DB) could not surface. Two real bugs + one clarity fix, all shipped in `161891c6`.

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 57 | 2026-07-06 | 07 At-checkout receipt | **HIGH** | **At-checkout receipt frozen in the pre-sync OFFLINE state on an ONLINE sale.** The POS is offline-first for every sale (queue → sync ~1-2s). `LocalSaleReceipt` read the IndexedDB queue row ONCE on mount (`useEffect` deps had no sync-status), snapshotting the pre-sync view — so an online sale's printed/on-screen receipt kept showing the OFFLINE badge, the provisional `OFF-` number, no barcode, and a disabled WhatsApp button forever, even though it synced seconds later (server number assigned, visible in the queue drawer). Customer-facing: a receipt that looks unsynced/broken. | Ring a sale while online, open/print the receipt immediately. | Once synced (~1-2s), the receipt flips to the final server number + barcode and enables the WhatsApp digital receipt. | **FIXED — `161891c6`.** The effect now polls the queue row every 2s (matching `use-queue-rows` POLL_INTERVAL_MS), stops on terminal (Synced/Failed), re-maps to the final number + barcode and sets `serverTxnId` (enables WhatsApp); capped at ~2min. code + frontend reviewers CLEAN (verified interval cleanup/no-leak/no-race). 19/19 receipt tests green. |
| 58 | 2026-07-06 | 07 Back-office receipt | MEDIUM | **Back-office transaction detail had no way to view/reprint/WhatsApp a receipt.** The `/pos/transactions` detail sheet rendered header/totals/lines/payments + a Return button only — no receipt view, no reprint, no digital send, despite the till having all of it and `GET :id/receipt` + `POST :id/receipt/reprint` existing server-side. | Open a transaction on the back-office POS transactions page. | A supervisor can view + reprint + send the receipt. | **FIXED — `161891c6`.** Added a "Receipt" dialog (view via `ReceiptDocument` with QR, Print, Reprint→DUPLICATE via `useReprintReceiptMutation`, WhatsApp) reusing the till components; gated to completed/voided (matches server `requirePrintable`); WhatsApp disabled+titled when no token; **synchronous re-entry guard** so a double-tap cannot inflate the audited `reprint_count` (code-reviewer MEDIUM, fixed). |
| 59 | 2026-07-06 | 07 Flags column | LOW | **Back-office "Flags" column all-clear icon was an unlabeled `Wifi` glyph** — read as a mystery flag on ~90% of rows. The column only ever shows two real flags (`isOffline`→WifiOff "Recorded offline"; `totalsMismatch`→AlertTriangle "manager review"); everything else is the faded no-op icon. | Hover the flags icon on a normal online sale. | The all-clear state is self-explanatory. | **FIXED — `161891c6`.** Wrapped the all-clear icon in the same Tooltip/`aria-label` pattern (`transactions.flags.none` = "Synced online, no issues", en+ar). |

**QR vs ZATCA — clarified for the founder:** the receipt QR is NOT a ZATCA e-invoice QR (ZATCA is KSA-only; Asala is Kuwait, no VAT/e-invoice). It is a plain link QR to `${NEXT_PUBLIC_APP_URL}/r/<token>` for the digital receipt. **The printed thermal receipt shows a BARCODE of the transaction number, not a QR — by design**; the QR reaches the customer digitally (WhatsApp / the public web receipt / the back-office receipt view). Printing the QR on the thermal receipt is an open product option (not built).

**Infra fix (incidental, `161891c6`):** root `eslint.config.js` scoped the Next `react-hooks/*` compiler rules to `apps/website/**` only, so lint-staged (runs from repo root) failed to resolve those rules' `eslint-disable` directives for any `apps/web/**` file. Extended the scope to `apps/web/**` too — unblocks lint-staged for all web commits, not just this one.

**Sign-off status:** token/public-page model is money-correct + leak-free (recon). Fixes shipped: #52 (returns token), #57 (frozen receipt, HIGH), #58 (back-office receipt actions), #59 (flags clarity), #60 (qty precision).

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 60 | 2026-07-06 | 07 Receipt qty | LOW | **Server-backed receipt printed raw stored quantity (`1.000000`) instead of the display form.** `receipt-document.tsx` rendered `line.quantity` verbatim; the local receipt already normalized it, so only the synced/server receipt (and the new back-office receipt view) showed 6dp. | Sync a sale, view the server receipt. | `1 × KWD 8.730` (trailing zeros stripped; real fractional qty preserved). | **FIXED — `41f3d5f7`.** Routed line quantity through the shared quantity formatter (`formatQuantity(v, null, "en")`, min 0 fraction digits), added as a `qty` prop mirroring the existing `fmt` prop; removed the dead `t_qty_placeholder` helper. Typecheck + 55 receipt tests green. |

## ✅ Submodule 07 — Receipt Model — SIGNED OFF (2026-07-06)

Cross-DB recon (tenant `pos_transactions`/`pos_receipts` + admin `receipt_tokens`) + full backend & frontend code audit (token mint, public `/r/[token]` controller/page, receipt render components, reprint/gift/WhatsApp) + founder end-to-end dogfooding on live Asala (KWD 3dp) + reviewer panels on every fix. All CRITICAL/HIGH pass.

**Load-bearing 07 invariants — GREEN on live data:**
- **Token minted post-commit, non-blocking, three-layer idempotent** (online `pay()` + offline sync); a re-synced offline sale never mints a second token.
- **Admin `receipt_tokens` = minimal token→tenant routing map**; the 18 sale tokens are byte-for-byte set-equal across admin + tenant DBs (zero orphans/missing); tokens are 122-bit UUIDv4.
- **Public `/r/[token]` is `@Public()` but token-scoped** (query scoped by tenant_id AND token → no cross-tenant leak), minimal projection (no cost/WAC/margin, no internal IDs, phone stripped, approver identity not exposed), uniform 404 on bad tokens, `Cache-Control: no-store`.
- **Money KWD 3dp + quantity display-normalized**, bilingual/RTL; gift hides prices/totals; WhatsApp shares only the token URL (non-blocking, no PII).
- **#51 DUPLICATE policy resolved by-design** (real reprint counter exists; queue-drawer DUPLICATE is correct anti-fraud; at-sale receipt is the original).

**Fixes shipped this program (erp main):** #52 returns-token (`36c2dfaf`) · #57 frozen at-checkout receipt HIGH (`161891c6`) · #58 back-office receipt actions + #59 flags clarity (`161891c6`) · #60 receipt qty precision (`41f3d5f7`) · incidental root eslint config scope fix (`161891c6`). Every fix behind the reviewer panel (code + nestjs + security for the token trust boundary; code + frontend for the web changes) — all CLEAN.

**Founder-verified live:** online-sale receipt flips to the final server number + barcode after sync (screenshot-confirmed the frozen-OFFLINE bug is fixed). **Remaining founder live re-verify on the latest deploy (non-blocking):** return now yields a public `/r/` receipt; back-office Receipt/reprint/WhatsApp; qty now renders `1` not `1.000000`.

**Open product options (founder, non-blocking):** (a) print the link QR on the thermal receipt too (today it's barcode-on-paper + QR-digital); (b) rename/redesign the back-office "Flags" column header beyond the added tooltip.

---

## Submodule 10 — Printing & Receipts — recon + agent-binary security audit + browser-dispatch audit (2026-07-06, PROD Asala `zerupt_tenant_al_asala_auto_parts_mqy1wpk2` @ `br-red-term`)

> The heart of 10 is a code audit of the two agent trust-boundary guards (SSRF + CSWSH) — the highest-risk surface in the whole POS module — plus the browser print dispatch. Printing is local hardware, so most functional items are hands-on at the founder's workstation, not SQL. The two anticipated headline CRITICALs (SSRF or CSWSH guard being frontend-only/bypassable, or missing) both proved FALSE: both guards are enforced **in the agent binary**, are DNS-rebinding / origin-spoof resistant, and are already 100% test-covered. One product decision (print the link-QR on paper) resolved into a durable country-agnostic seam + shipped.

**Tenant printer config (scopes severity) — all three Asala registers print via the BROWSER, not hardware:**

| Register | printer_type | printer_connection | host/port | cash_drawer_connected | dot_matrix_mode |
|---|---|---|---|---|---|
| Register 1 (Shuwaikh) | `none` | `browser` | null | `false` | graphics (unused) |
| Register 2 | `none` | `browser` | null | `false` | graphics (unused) |
| Register 3 | `none` | `browser` | null | `false` | graphics (unused) |

**Consequence:** Asala's live print path is `window.print()` (the browser fallback) — no thermal ESC/POS, no dot-matrix raster, no network TCP target, no cash-drawer pulse in this tenant. So: the **browser-print full-height receipt (§55/§97)** is the *live-critical* path here; **thermal/dot-matrix/Arabic-canvas/cash-drawer/SSRF-in-a-live-network-config** are **"verify at go-live for hardware tenants"** — the guard/render code is audited sound now, there's just no live network target to exercise it. (This also explains the founder's earlier "shows offline when I print" screenshot: no agent installed → browser path is correct behaviour.)

**Recon — load-bearing 10 invariants (pass/fail):**

| # | Invariant | Verdict | Evidence |
|---|-----------|---------|----------|
| P1 | Print failure NEVER blocks completion (fire-and-forget, post-commit) | ✅ PASS | Sale commits in `use-complete-sale.ts` (enqueue → clear cart) and NEVER calls `dispatchReceiptPrint`; printing is a separate manual button fired `void dispatchReceiptPrint(...)`. `dispatchReceiptPrint` never throws (top-level catch → browser fallback → `{ok:true}`). Bounded: `DEFAULT_PRINT_TIMEOUT_MS=15_000`, hard reject at `timeoutMs+1_000` (`agent-client.ts`) — no infinite spinner possible. |
| P2 | Cash-drawer pulse only when cash tender AND `cashDrawerConnected=true`, AND-ed at the instruction layer | ✅ PASS | `print-dispatcher.ts:193` `if (openDrawer && register.cashDrawerConnected)` before `cashDrawerKick()` (`ESC p` = `1b 70 00 19 fa`) bytes are appended. `openDrawer` derived from `payments.some(p=>p.method==="cash")`. Card-only → no pulse; drawer off → no pulse. No leak path. |
| P3 | Agent rejects non-RFC1918 TCP targets — in the BINARY, not frontend-only | ✅ PASS | `printer-tcp.ts sendTcp`: resolves host via `dns.lookup`, requires the **resolved IP** to pass `isPrivateOrLoopbackIp` (rejects `8.8.8.8`, `169.254.169.254` cloud-metadata, IPv4-mapped public v6, `fe80::` link-local, public v6), then connects to the **resolved literal** — closes DNS-rebinding TOCTOU. Frontend only sends host/port; it cannot make the agent skip `sendTcp`'s own resolve+check. Parse-time `isValidHost` is a second (syntactic) layer. |
| P4 | Agent rejects non-Zerupt WebSocket origins | ✅ PASS | `server.ts verifyClient` → `isOriginAllowed`: admits only `https://*.zerupt.com(:port)` + `http://localhost/127.0.0.1`; rejects suffix-spoofs (`evil.zerupt.com.attacker.com`), LAN IPs, `file://`. Browsers cannot forge Origin on a WS handshake. `origin===undefined` (native/CLI, not a browser CSWSH vector) allowed, but print/discover still require a TOFU pairing token (`checkAuth` + `timingSafeEqual`). |
| P5 | `reprintCount` monotonically non-decreasing, race-safe | ✅ PASS (structural) | `pos_receipts.reprint_count` incremented by a DB-side `sqlIncrement()` UPDATE (`reprint_count = reprint_count + 1`, not read-then-write) with a defensive UPSERT (07 §51). 0 receipt rows live (all sales offline-synced; row lazily created on first reprint) so no live monotonicity data; structurally race-safe. Back-office reprint has a synchronous double-fire guard (07 #58). |
| P6 | `window.print()` fallback forces full-height render (no truncation) | ✅ PASS (code) | `@media print` block hides `body *`, un-hides `#pos-receipt-print`, sets `position:absolute; inset:0; width:100%; max-width:none` — lifts the receipt out of any scroll container; `@page { size:80mm auto }` unbounded height. **Founder to visually confirm in Chrome + Safari with a 20+ line receipt (§55/§97 MEDIUM).** |
| P7 | Version check warns (not crashes) below minimum; WS auto-reconnects mid-shift | ✅ PASS | `hello` handshake reads `agentVersion` → `isAgentStale(v, MIN_AGENT_VERSION="0.1.0")`, surfaced non-fatally (stale agent still prints). Reconnect: module-level singleton client (separate from cart/Zustand → no page refresh, cart preserved); backoff 1s→30s, 5 attempts, + lazy reconnect on next `print()`. |

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 61 | 2026-07-06 | 10 Print dispatch | **MEDIUM** | **Print button had no in-flight guard / debounce (checklist §34).** The at-checkout Print button (`local-sale-receipt.tsx`) had no disabled/loading state and no debounce — rapid taps queued multiple concurrent print jobs (each up to 15s against an agent → duplicate physical prints on a hardware tenant). On Asala (browser path) `window.print()` is a blocking native dialog so double-tap is naturally serialised, but for future agent tenants this is a real duplicate-print risk. | Tap Print rapidly on the receipt screen. | One job per tap; button disabled while a job is in flight. | **FIXED — commit `d563d0ff` on main.** Added `isPrinting` state: button `disabled` + `Loader2` spinner while dispatching; early-return guard in the click handler; `.finally(() => setIsPrinting(false))` always re-enables (verified: `dispatchReceiptPrint` is bounded at 15s so it can never hang the guard). code + frontend + security reviewers CLEAN. |
| 62 | 2026-07-06 | 10 Receipt QR (product) | RESOLVED | **Open 07 decision — print the digital-receipt link-QR on paper?** Today paper carries a barcode of the transaction number; the `/r/<token>` QR reaches the customer only digitally. Founder asked for the *durable, country-agnostic* foundation (not a Kuwait toggle). | — | A seam where fiscal/regulatory codes (KSA ZATCA, India IRN) plug in per-country without touching receipt layout. | **SHIPPED — `d563d0ff`.** New `resolveReceiptCodeBlocks()` seam returns ordered typed code blocks (`barcode` \| `link-qr` today; `fiscal-qr` documented extension point); render layer draws whatever it's handed. Default now prints **barcode + digital-receipt link-QR** on synced sales; provisional OFF- receipts stay code-free (number not final — double-guarded, `publicReceiptUrl` only non-null once `syncStatus==="Synced"`). Reuses existing `generateQrDataUrl`. Security-reviewed: no new exposure (token is 122-bit UUIDv4, `/r/<token>` already a PII-stripped bearer-safe projection per 07). ZATCA components become a resolver case at e-invoice go-live (YAGNI now — Kuwait has no VAT). **Founder to visually confirm the QR scans to the right `/r/` page.** |
| 63 | 2026-07-06 | 10 Agent guard tests | ✅ VERIFIED | **SSRF + CSWSH guards are already 100% test-covered** (initial worry they were untested was a wrong-dir `ls`). | — | 100% coverage on the guard + never-block paths (mandate). | NOTE — `__tests__/protocol.test.ts` (SSRF: public IP, 169.254 metadata, IPv4-mapped public, fe80, public v6, post-resolution rebinding), `printer-tcp.test.ts` (rebinding-at-connect: mocked lookup→8.8.8.8 rejected, no socket opened), `server-origin.test.ts` (CSWSH spoofs). 97 agent tests green. No new tests needed; two duplicate files I'd started were deleted. |
| 64 | 2026-07-06 | 10 Agent stale-version gate | NOTE (decided by persona) | **Stale-agent policy: warn-not-block is correct.** Checklist §84/§98 flags stale agents as a security risk (might lack SSRF/CSWSH guards). | — | Persona (counter cashier, never dead-end mid-shift) vs security. | DECIDED — keep the current warn-only behaviour (`MIN_AGENT_VERSION="0.1.0"`, non-fatal banner, printing not blocked). The guards exist in the earliest distributed version and no old-agent fleet exists pre-launch, so a hard gate would only dead-end a cashier with a customer waiting. **Upgrade trigger:** the day a security-critical agent fix ships, bump `MIN_AGENT_VERSION` to hard-gate printing on agents below it. |
| 65 | 2026-07-06 | 10 Queue reprint QR | LOW | **Queue-drawer / back-office reprint shows barcode only, never the link-QR.** `queue-reprint-dialog.tsx` and the back-office reprint don't pass `publicReceiptUrl` to `LocalReceiptDocument`, so a reprint prints barcode-only. Pre-existing (not a regression from #62); the at-checkout original carries the QR. | Reprint from the queue drawer. | Reprint could also carry the link-QR. | NOTE — persona-acceptable (reprint is an internal/anti-fraud DUPLICATE surface); wire `publicReceiptUrl` through the reprint paths later if walk-in reprint QR is wanted. |
| 66 | 2026-07-06 | 10 Agent dead test dup | LOW | **`apps/print-agent/src/__tests__/printer-system.test.ts` is a tracked-but-unscanned 38-line dead duplicate** of the real 371-line `__tests__/printer-system.test.ts` (vitest `include: __tests__/**` only — `src/__tests__/` is never run). | — | No stale unscanned test copy. | NOTE (debt, out of this slice) — pre-existing tracked file from another concern; delete in a cleanup pass. Not swept into the 10 commit. |

### Submodule 10 — recon verdict (2026-07-06)

**The print trust boundary is sound and the print path never blocks a sale.** Both agent guards are enforced in the binary and are attack-resistant — SSRF via resolve-then-check-resolved-IP-then-connect-to-literal (DNS-rebinding-safe, rejects public + cloud-metadata + link-local across v4/v6), CSWSH via an Origin allowlist that rejects suffix-spoofs plus a TOFU pairing-token second layer — and both are already 100% test-covered (63). Printing is fully decoupled from the commit, never throws, and is bounded at 15s (no infinite spinner). The cash-drawer pulse is correctly AND-gated (cash tender AND drawer connected) at the instruction layer. `reprintCount` is a race-safe DB-side increment. **Two things shipped this pass** (`d563d0ff`): #61 print-button in-flight guard (§34), and #62 the country-agnostic receipt-code seam that resolves the open "QR on paper" decision into a 10-year foundation (barcode + link-QR default; fiscal-QR extension point for ZATCA/IRN). #64 stale-version policy decided (warn-not-block). **Asala prints via the browser** (all registers `printer_type=none`), so thermal/dot-matrix/cash-drawer/live-SSRF items are audited-sound-but-verify-at-hardware-go-live; the founder's hands-on items are: browser full-height render in Chrome+Safari with a long receipt (§55/§97), and that the new paper QR scans to the correct `/r/` page. No CRITICAL/HIGH found.

## ✅ Submodule 10 — Printing & Receipts — SIGNED OFF (2026-07-06)

Agent-binary security audit (SSRF + CSWSH trust boundary, read directly) + browser print-dispatch audit (fire-and-forget, drawer AND-gate, version check, reconnect, fallback, QR) + live `pos_registers` config recon + reviewer panel (security PRIMARY + code + frontend, all CLEAN) + fixes shipped and tested. All CRITICAL/HIGH pass.

**Load-bearing 10 invariants — GREEN:** print-never-blocks (fire-and-forget, post-commit, 15s-bounded, never-throws); cash-drawer pulse AND-gated at the instruction layer; SSRF guard enforced in the binary (DNS-rebinding-safe, RFC1918/loopback-only, rejects cloud-metadata); CSWSH guard enforced in the binary (origin allowlist + pairing token); `reprintCount` race-safe DB-side increment; `window.print()` fallback forces full-height render.

**Shipped this pass (erp main):** #61 print-button in-flight guard (§34) + #62 country-agnostic receipt-code seam (barcode + digital-receipt link-QR; fiscal-QR extension point) — both in `d563d0ff`, reviewer panel CLEAN, 27 web + 97 agent tests green, typecheck + i18n clean.

**Founder hands-on verify (non-blocking, hardware/visual):** browser full-height receipt in Chrome + Safari with a 20+ line receipt; the new paper QR scans to the right `/r/` page; and, at hardware go-live, thermal/dot-matrix/Arabic-canvas render + cash-drawer pulse + live SSRF reject (`8.8.8.8` host).

**Verdict:** the highest-risk surface in the POS module — the agent trust boundary — is sound and unbypassable; printing never blocks a sale; the receipt-code foundation is country-agnostic for the next decade. Signed off.

---

## ✅✅ POS MODULE — TESTING & HARDENING PROGRAM COMPLETE (2026-07-06)

All 10 submodules of the POS testing program are signed off. POS was audited as an **event-emitting front end** (never its own ledger): every sale/return/void/close ties out three ways — POS records ↔ balanced GL journal entry ↔ signed stock movement — on live Asala data (KWD 3dp), plus the offline replay, receipt/token, and print trust boundaries.

| # | Submodule | Sign-off | Headline result |
|---|-----------|----------|-----------------|
| 01 | Register & Session | ✅ | ≤1 open shift per register/cashier DB-enforced (partial unique idx) |
| 02 | Transaction Lifecycle | ✅ | server recomputes totals; balanced JE + COGS + stock per sale |
| 03 | Payment Methods | ✅ | payment completeness `Σpay − Σchange = grandTotal`; gift/store-credit not a live feature |
| 04 | Discounts & Promotions | ✅ | `grandTotal = subtotal + tax − discount` exact, no tolerance |
| 05 | Returns & Exchanges | ✅ | cumulative returned qty ≤ sold; original price; net-zero GL + stock reversal |
| 06 | Offline Mode | ✅ | exactly-once idempotent replay; `is_offline` provenance fixed + backfilled |
| 07 | Receipt Model | ✅ | token→tenant routing map, leak-free `/r/<token>`, 122-bit UUIDv4; returns now mint tokens |
| 08 | Z-Report & Shift Close | ✅ (parallel session) | cash reconciliation `expected = float + cash sales − refunds − payouts + payins` |
| 09 | Cross-Module Contracts | ✅ (parallel session) | AR derived from party-tagged 1131 GL; event contracts to accounting + inventory |
| 10 | Printing & Receipts | ✅ | agent SSRF + CSWSH guards binary-enforced & unbypassable; print never blocks a sale |

> Note: the 08 + 09 sign-offs land via their own parallel sessions; this closing verdict is written from submodule 10 (the final code submodule). If an 08/09 section merges below this line, that is expected — the program-complete verdict stands on all ten.

**Cross-cutting invariants that held module-wide:** three-way tie-out (POS ↔ GL ↔ stock) on every sale/return/void; money always in tenant functional currency at correct precision (KWD 3dp) via the shared formatter; idempotent offline replay (DB partial-unique backstop); server-recomputes-totals-never-block; immutable audit on every mutation; tenant isolation on every query; and print/sync never blocks a sale. The agent trust boundary (SSRF + CSWSH) — the module's single highest-risk surface — is enforced in the binary and 100% test-covered.

**Program methodology:** live-DB invariant recon → full backend + frontend code audit → reviewer panel (security paranoid, never lazy) → fix CRITICAL/HIGH same session behind the panel → founder dogfooding on live Asala → sign-off. Resume record: `study/pos/_hardening-log.md`; memory: `project_pos_hardening_program.md`.

---

## Submodule 08 — Z-Report & Shift Close — recon + full backend & frontend code audit (2026-07-06, PROD Asala tenant `zerupt_tenant_al_asala_auto_parts_mqy1wpk2` + admin `zerupt_admin`, both @ `br-red-term`)

> Live recon over both shifts (KWD 3dp) + full code trace of the shift-close service, the Z/X-report aggregation, the cash-movements endpoints, and the `pos.shift.closed` → GL path. **The two feared issues are both closed by construction:** the headline CRITICAL (voided cash inflating expectedCash) is FALSE — the aggregation filters positively on `status = 'completed'`, so voids/held/drafts are all excluded; the HIGH (shift.closed GL split-brain / non-idempotent listener) is mitigated — the JE payload is written to the durable outbox in the SAME transaction as the shift UPDATE, keyed by a deterministic v5 eventId (`pos.shift.closed:<shiftId>`), and posting is idempotent by `eventId` lookup. **No CRITICAL/HIGH. No code fix required this pass.**

**As-built (corrected vs handover):**
- **Status enum is `open`/`closing`/`closed`, NOT `completed`.** Closed shift #1 = `closed`. (Transactions use `completed`; shifts use `closed` — don't conflate.)
- **expectedCash** = `openingFloat + Σ(cashSales) − Σ(cashRefunds) − Σ(payOuts) + Σ(payIns)`, where cash sale/refund legs are **net of change**: `sum(pos_payments.amount − pos_payments.change_given)` scoped to `method='cash'` AND `pos_transactions.status='completed'`. Computed once in `computeCashComponents` (`pos-shifts.service.ts:602`) and reused by close + Z-report + X-report — single source, no drift. (`pos-shifts.service.ts:457` `close`, `:288` `zReport`.)
- **Void exclusion** is a *positive* `status='completed'` filter, not `status != 'voided'` — strictly safer (also drops `held`/`draft`). The checklist's §92 CRITICAL risk does not exist in this codebase.
- **cashOverShort** = `Decimal(actualCash).minus(expectedCash)` — exact, no rounding (`:481`).
- **Idempotent close:** cheap pre-check `if status==='closed' → 409` PLUS a guarded `UPDATE ... WHERE status <> 'closed'` inside the tx (`:533-540`); a lost race throws `ConflictException`, never re-closes or corrupts `actualCash`. No PATCH endpoint exists for shift fields; no `closed → open` path exists → permanently immutable.
- **Held block:** `countHeldTransactions` → 409 before close (`:470`).
- **Cash-transfer ("bank deposit") leg is OPTIONAL:** driven by `cashTransferAmount`/`cashTransferTarget` (target ∈ `bank`/`petty_cash`, DB CHECK). When >0, `buildShiftClosedJePayload` adds DR bank/petty_cash / CR cash. When 0 (the live case), no transfer leg — the checklist's "always two JEs on close" assumption is wrong; the as-built posts over/short and/or transfer, whichever is non-zero, and `null` (no JE) for a perfectly-balanced non-swept close.
- **Durable-gated GL:** outbox row inserted in the same DB tx as the shift UPDATE (`:546`); generic `@OnEvent('accounting.post', {suppressErrors:false})` handler (`journal-posting.service.ts:789`) re-throws on durable replay so the poller retries; `postFromEvent` dedupes by `eventId`. There is no POS-specific `@OnEvent('pos.shift.closed')` listener — the payload is built inline in `close()` and routed through the shared accounting-post path. This IS the outbox + deterministic-eventId equivalent of `runDurableGated`.
- **Movements validation:** `amount > 0` enforced at BOTH the service (`BadRequestException`) and the DB (`pos_cash_movements_amount_positive_check`); `reason` required (min 1); `pay_out` requires `approvedBy` (uuid) + `approvalPin` (PIN-verified before the tx); creation runs inside `SELECT ... FOR UPDATE` on `pos_shifts` with a `status='open'` re-check → serialized against a concurrent close.

**Recon — invariants (pass/fail on live data):**

| # | Invariant | Verdict | Evidence |
|---|-----------|---------|----------|
| Z1 | expectedCash formula holds for closed shift | ✅ PASS | Shift #1 (`b61dfe82`): `0 + 1996.700(cash sales, change=0) − 32.000(cash returns) = 1964.700` = stored `expected_cash` exactly. |
| Z2 | Voided cash tx excluded from expectedCash | ✅ PASS | Aggregation joins `status='completed'` only. Shift #2 probe: voided sale `B1SHUWAIKHREG1-2-3` (1.830 cash) sits in a separate `status='voided'` bucket and is not summed. Feared CRITICAL is FALSE. |
| Z3 | cashOverShort = actualCash − expectedCash exact, correct sign | ✅ PASS | Shift #1: `1960.000 − 1964.700 = −4.700` stored (negative = short). |
| Z4 | Closed shift immutable + idempotent close | ✅ PASS | Guarded `UPDATE WHERE status<>'closed'`; no PATCH/reopen endpoint; double-close → 409, `actualCash` untouched. |
| Z5 | Cash movements require approver / positive amount | ✅ PASS (code) | `amount>0` (service + DB CHECK); `reason` required; `pay_out` PIN+approver; FOR UPDATE lock. No live rows (0 movements) — verified by code + will confirm via live pay-in/out test. |
| Z6 | pos.shift.closed → balanced, durable, idempotent GL | ✅ PASS | Shift #1 close → single balanced JE `B1SHUWAIKH-JRN-200070` (`pos.shift.closed`): DR 6700 4.700 / CR 1112 4.700 (shortage). Exactly one such JE exists (idempotent). Bank-deposit leg absent because `cash_transfer_amount=0` (correct-by-design). |

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 61 | 2026-07-06 | 08 Discrepancy / unsynced gate | **MEDIUM** | **No discrepancy-threshold manager gate; close-with-unsynced is cashier self-ack (the #46 carryover, now live in 08).** `close()` records any `cashOverShort` and posts it to 6700 with no second-approval gate regardless of magnitude; likewise an over-threshold close is not blocked. The frontend red over/short indicator is the only "warn". | Close a shift with a large \|over/short\|. | Persona ruling: **warn-not-block is correct** for the shift-supervisor/owner persona (in a single-owner Kuwait auto-parts shop the supervisor IS the approver). | **DECIDED — deferred to the approvals/RBAC batch, NOT a fix this pass.** Implement as the optional **per-register manager gate** (per-register toggle, default OFF; enforce only when enabled — consistent with the POS-approval-gates-settings-optional principle). Same batch resolves #46 self-ack → manager-ack. No code change now. |
| 62 | 2026-07-06 | 08 Held block msg | LOW | Held-transaction block returns the **count** (`Shift has N held transaction(s)`), not the specific transaction numbers the checklist §48 wants. | Try to close with a held cart. | Message lists the held tx numbers. | NOTE — count is enough to act (recall/void from the register); numbers are a polish item. |
| 63 | 2026-07-06 | 08 Z-report gross | LOW | Z-report `grossSales = completed + voided` (voids shown on a separate line, then netted out); the money-correct `netSales`/`totalSales` = completed only. Checklist §29 phrasing wants voids out of "gross". | Read a Z-report with a void present. | Pure display terminology; expectedCash + net are void-free. | NOTE — no money impact; the aggregation that touches cash is correct. |
| 64 | 2026-07-06 | 08 Double-tap close | LOW | Double-tap close returns **409 Conflict**, not the already-closed record (checklist §50/§76 suggested returning the existing record). | POST :id/close twice. | No corruption either way; `actualCash` is never re-written. | NOTE — by-design; the danger (re-close corrupting actualCash) is prevented. Frontend should swallow the 409 into a benign "already closed" state. |
| 65 | 2026-07-06 | 08 Pay-in approver | LOW | `pay_in` does **not** require an approver/PIN; only `pay_out` does. Checklist §35 lists `approvedById` required for pay-in too. | Create a pay-in with no approver. | Asymmetry is defensible — cash INTO the drawer is not a theft vector; cash OUT is PIN-gated. | NOTE — by-design; revisit if audit policy demands symmetric approval. |
| 66 | 2026-07-06 | 08 Bank-deposit leg | NOTE | The "bank deposit DR Bank / CR 1112" leg posts **only when `cashTransferAmount > 0`** (optional end-of-shift sweep), not automatically on every close. On shift #1 it was 0, so only the over/short JE posted and cash stays in 1112 across shifts. | Close without entering a cash transfer. | Corrects the checklist §70/§85 "always two JEs" assumption. | NOTE — by-design; a Kuwait till legitimately keeps cash in the drawer between shifts. Bank rec relieves 1112 separately. |

### Submodule 08 — recon verdict (2026-07-06)

**Shift close and the Z-report are money-correct, immutable, and durably-posted on live data.** The load-bearing 08 invariants all hold: expectedCash is a single net-of-change `status='completed'` aggregation reused by close/Z/X (live-verified exact on shift #1 = 1964.700); voided cash is excluded by a positive completed-filter (the feared CRITICAL is FALSE — the voided probe `-2-3` never enters the sum); cashOverShort is an exact `Decimal` subtraction with correct sign (−4.700 short); the closed shift is permanently immutable (guarded UPDATE, no PATCH/reopen) and close is idempotent (double-tap → 409, actualCash safe); the `pos.shift.closed` JE is written to the durable outbox atomically with the shift UPDATE, keyed by a deterministic v5 eventId, idempotent by eventId on replay (live: exactly one balanced JRN-200070, DR 6700 / CR 1112 4.700), and the bank-deposit leg is a correct-by-design optional cash sweep. Cash-movement endpoints enforce `amount>0` (service + DB CHECK), required reason, and PIN+approver on pay-out under a `FOR UPDATE` shift lock. X-report (mid-shift interim) exists (no open-status gate), resolving the checklist's MEDIUM "no X-report" gap. **No CRITICAL/HIGH found; no code fix required.** #61 is the one product decision (warn-not-block, defer the optional per-register manager gate + #46 manager-ack to the approvals/RBAC batch); #62–#66 are LOW/NOTE, persona-acceptable.

**Founder to verify live (non-blocking):** close shift #2 with a counted `actualCash` and confirm the shown expectedCash + over/short match the recompute below, the voided `-2-3` is excluded, and a new `pos.shift.closed` JE (6700 + optional transfer) posts balanced. Run one pay-in and one pay-out (pay-out asks for PIN).

### Submodule 08 — frontend audit + fixes SHIPPED (2026-07-06, main)

Paranoid frontend audit of the shift-close flow + Z/X-report + cash-movements + back-office. The close panel itself is well-built (actualCash uses the shared `MoneyInput` 3dp-safe; server-authoritative totals with provisional-vs-final distinction; green/red over/short; debounced `AlertDialog`-gated close; i18n/RTL parity passes). Three real defects found and fixed this pass; all render money via the shared `currency-precision`/`MoneyInput` ISO-4217 path (country-agnostic by construction).

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 67 | 2026-07-06 | 08 Cash-movement input | **CRITICAL** | **Pay-in/pay-out amount field used a raw `<input type="number">`, not the shared `MoneyInput`** — reintroduced the exact KWD-3dp precision trap the team already remediated on the pay-surface and shift-open surfaces (no blur-normalize to currency precision; browser spinner/RTL/decimal-separator issues). | Enter `1.5` / `1500` as a pay-out amount for a 3dp tenant. | Field normalizes to tenant precision on blur, any currency. | **FIXED — main.** Swapped to `MoneyInput` (`currency`-bound, `min={stepValue}`, `data-testid`/`aria-*` forwarded via `{...rest}`); `onChange` now takes the raw string. 9/9 cash-movement tests green; frontend reviewer CLEAN. |
| 68 | 2026-07-06 | 08 Cash-movement list copy | **HIGH** | **Hardcoded em dash** as the type/reason separator on every cash-movement row (`cash-movement-list.tsx:92`) — violates the strict no-em-dashes-in-product-copy rule. | View any pay-in/out row. | No em dash in product copy. | **FIXED — main.** Replaced `" — "` with a language-neutral middot `" · "` (RTL-safe). |
| 69 | 2026-07-06 | 08 X-report label | **MEDIUM** | **The interim X-report was titled "Z-Report."** Clicking the X-Report button on an OPEN shift opened a document headed "Z-Report" (the final end-of-shift report) — the checklist §78 "must be clearly labelled interim, not a final Z-report" concern. Backend numbers were correct (expected cash −307.150, void 1.830 excluded — founder screenshot-confirmed). | Open shift → X-Report button. | Interim report headed "X-Report (Running Totals)". | **FIXED — main.** `ZReportDocument` title now `t(report.status === "closed" ? "zReport.title" : "xReport.title")` — a single status-driven switch that also corrects the print-view's live case; open/closing → X, closed → Z. Two tests added locking the distinction (10/10 green). i18n keys already existed en+ar. |
| 70 | 2026-07-06 | 08 Back-office shift route | LOW | **No back-office `/pos/shifts` list or shift-detail route** — only `[id]/z-report` exists; `CashMovementList` (fully built) is reachable only from the live close panel, not for a historical/closed shift. | Look for a shifts list in back-office. | A supervisor can browse closed shifts + their movement history. | NOTE — deferred build (not a bug), batched with #45 for the founder's back-office UI pass. |

**Country-agnostic foundation (founder ruling — permanent):** nothing in 08 hardcodes a country. Money = ISO-4217 `currency-precision`/`MoneyInput` (any currency/dp); GL over/short + cash + bank = per-tenant `lineType → account-mapping` (any chart of accounts, not hardcoded 6700/1112); tax = by-group aggregation from the transactions' own tax lines (Kuwait "No Tax", KSA 15%, India GST all flow through unchanged); all copy is i18n (en source + ar); X/Z is driven by shift lifecycle state; the manager/discrepancy gate (#61) is designed as an optional per-register toggle. Country requirements (ZATCA QR, GST invoice format) bolt on top via feature flags + per-tenant settings without touching the shift-close/Z-report core.

## ✅ Submodule 08 — Z-Report & Shift Close — SIGNED OFF (2026-07-06)

Live recon over both shifts (KWD 3dp) + full backend code audit (shift-close service, Z/X-report aggregation, cash-movements endpoints, `pos.shift.closed` → outbox → GL) + GL tie-out on the one live close. All CRITICAL/HIGH pass; the two feared issues (void-inflated expectedCash CRITICAL; shift.closed split-brain HIGH) are both closed by construction. One product decision logged (#61, deferred to approvals/RBAC batch); #62–#66 LOW/NOTE. No code changed this pass.

**Verdict:** the receipt model is tenant-isolated, leak-free, money- and quantity-correct, and never-block on the live dataset; the token/idempotency + public-page trust boundary are sound; all dogfooding defects fixed and (for #57) founder-verified. Signed off.

---

## Submodule 09 — Cross-Module Contracts — recon + full event-durability code audit (2026-07-06, PROD Asala tenant `zerupt_tenant_al_asala_auto_parts_mqy1wpk2` @ `br-red-term`)

> Persona: **system auditor — trust nothing until traced end to end.** POS is an event emitter, not a ledger: every completed sale/return/void/shift-close must ripple into a balanced GL journal entry (Accounting) and a signed stock-ledger movement (Inventory). 09 has no UI — verified by tracing POS record → journal_entries/journal_entry_lines → stock_ledger_entries, plus a full audit of the emit → outbox → poller → posting durability path for idempotency, atomicity, and delivery guarantees. Depends on 02/05/08 (all signed off).

### The as-built event architecture (corrected vs the checklist's assumption)

The checklist (and its §83 CRITICAL) assume the four POS events flow through a raw in-process `EventEmitter` (`pos.transaction.completed` → `PosAccountingListener`). **That is NOT the production path.** The real path, verified in code:

1. Inside the POS write transaction, the service builds a **journal-entry-ready `accounting.post` payload** (`pos-transactions-events.ts` `buildTransactionCompletedJePayload` / `buildReturnCompletedJePayload` / `buildVoidCompletedJePayload` / `buildShiftClosedJePayload`) with a **deterministic eventId** (`deterministicUuidV5(eventType + ":" + documentId)`), and inserts it into the transactional **outbox** using the **same `tx` handle** (`outboxService.insert(tenantId, ACCOUNTING_EVENTS.POST, payload, tx)`) — the outbox row commits iff the POS record commits (atomic).
2. **Post-commit** (after `db.transaction()` closes), an in-process fast-path `emitXxxCompleted()` emits `accounting.post` directly — **same eventId** as the outbox row.
3. The outbox **poller** re-emits any un-drained row on its schedule; both paths carry the same eventId.
4. `JournalPostingService.handleAccountingPost` (`@OnEvent("accounting.post")`) writes `journal_entries`/`journal_entry_lines`, **deduped on eventId** (pre-check `SELECT` + partial unique index `je_event_id_key` + concurrent-race catch). COGS + stock relief flow through a **separate** deterministic-eventId inventory event (`pos-inventory-events.ts` → `inventory-domain.listener.ts`), deduped by the **full** unique index `sle_event_id_key` on `stock_ledger_entries` (per-line eventId derived stably).

Consequence: **`pos.listener.ts`'s three `@OnEvent(POS_EVENTS.*)` handlers are DEAD CODE** — grep confirms **nothing** in `apps/api/src` ever `.emit(...)`s the POS domain events; those constants appear only as *string keys* for eventId derivation and as the `eventType` field inside the `accounting.post` payload. The dead listener cannot double-post (it never fires), so it is a maintainability finding (#67), not a data-integrity bug.

### Recon — 09 invariants (pass/fail on live data)

| # | Invariant | Verdict | Evidence |
|---|-----------|---------|----------|
| C1 | Every POS-sourced JE balanced (ΣDR=ΣCR), zero tolerance | ✅ PASS | 69 `source_document_type='pos'` journal_entries, **0 unbalanced** (per-entry ΣDR−ΣCR). Aggregate DR=CR=5305.212459. |
| C2 | eventId uniqueness / idempotency (no replay double-post) | ✅ PASS | Across the 69 POS JEs: **0 null event_id, 0 duplicate event_id**. Stock ledger: 27 `sale` + 13 `sale_return` rows, **all event_ids distinct, 0 null**. Enforced by `je_event_id_key` (partial unique) + `sle_event_id_key` (full unique) + posting-service pre-check SELECT + race catch. |
| C3 | Line-to-ledger completeness (every sale line → deduction, none dropped, no over-relief) | ✅ PASS | **24/24** completed sale lines relieved **exactly** their line qty (`mismatched_relief=0`, `lines_missing_stock=0`). The 2 lines with 2 ledger rows are legitimate **batch-tracked split relief** (distinct `batch_id`, distinct per-row eventId, totals tie: 6+5=11, 2+4=6) — NOT a double-deduction. |
| C4 | Return/void net-zero on GL AND stock | ✅ PASS | Void `-2-3`: GL nets to 0 on Cash 1112, Inventory 1141, COGS 5100, Discounts 4300; revenue reversed via 4200 Sales Returns (4110 −2.44 offset by 4200 +2.44 → P&L net 0, the documented void design blessed in 06). Stock: `sale` −1 (cost 1.59) + `sale_return` +1 (cost 1.59) → **net qty 0, net cost 0**, restored at **original** cost. |
| C5 | POS never writes JEs/stock directly (no cross-module service injection) | ✅ PASS | Grep of `apps/api/src/pos/` finds **zero** imports/injections of a JournalEntry/StockLedger service and zero direct writes to those tables. `pos.module.ts` header states it explicitly; all cross-module writes flow through `accounting.post` + the inventory domain event. |
| C6 | Emit-after-commit + durable, idempotent, at-least-once delivery (§83 CRITICAL) | ✅ PASS — **§83 MITIGATED** | Outbox insert uses the **same `tx`** as the POS write (`outbox.service.ts:74` `db = tx ?? getTenantDb()`); fast-path emit is strictly **post-commit**. Failure path: `OUTBOX_MAX_ATTEMPTS=5`, backoff `[0,30,120,600,3600]s`, then terminal `dead_letter` (poller skips; manual replay CLI). Stuck-`processing`-row re-claim on `last_attempt_at` threshold. **No silent event loss; no double-post** (dedup on eventId). The feared "EventEmitter is not transactional" CRITICAL does not apply — the real path is a durable transactional outbox. |
| C7 | shift.closed GL (over/short sign; zero → no JE; no zero-amount entry) | ✅ PASS | Shift #1 (closed, `cash_over_short=-4.70`): balanced close JE **DR 6700 Cash Over/Short 4.70 / CR 1112 Cash 4.70** (correct shortage sign). Shift #2 (open): **no JE** (correct). `buildShiftClosedJePayload` returns `null` when nothing happened → no zero-amount entry. Bank-deposit leg posts only on an explicit cash transfer (the #66 by-design note). |

Chart-of-accounts confirmed for this tenant: 1112 Cash Register, 1141 Merchandise Inventory, 4110 Product Sales, 4200 Sales Returns and Allowances, 4300 Sales Discounts, 5100 COGS, 6700 Cash Over/Short. (The checklist's generic "4110 Sales Revenue"/void-reverses-4110 differs: POS books voids/returns to the **4200** contra, not a direct 4110 negation — net P&L effect identical.)

| # | Date | Submodule | Severity | Summary | Repro | Expected vs Actual | Status |
|---|------|-----------|----------|---------|-------|--------------------|--------|
| 67 | 2026-07-06 | 09 Dead listener | **MEDIUM** | **`pos.listener.ts` (`PosAccountingListener`, 3 × `@OnEvent(POS_EVENTS.TRANSACTION_COMPLETED/RETURN_COMPLETED/VOID_COMPLETED)`) is dead code — nothing emits those domain events.** The live path builds `accounting.post` directly in `pos-transactions-events.ts`. The listener + its 32KB spec test a path that never runs in production, and it **duplicates the JE line-mapping logic** (`PAYMENT_LINE_TYPE`, `REFUND_LINE_TYPE`, return/void line construction) that also lives in the live emitter — a real drift risk (a future dev could "fix a bug" in the dead file, or edit one mapping and not the other). No data-integrity impact (it cannot double-post since it never fires). | Grep `apps/api/src` for `.emit(POS_EVENTS` → zero hits; the only refs are string-key/eventType usages. | Delete the dead listener + spec (and drop the unused `PosAccountingListener` from `AccountingEventsModule` providers), OR, if it is kept as documentation, a header comment marking it non-wired. | **FIXED — commit `c64422ac` on main** (founder chose delete). Removed `pos.listener.ts` + its spec; dropped `PosAccountingListener` from `AccountingEventsModule` + the integration harness providers; redirected the 4 "source of truth" comments (`account-mapping-defaults.ts` ×3, `account-mapping-seed.service.spec.ts`, `build-pos-transaction-post.ts`) to `pos-transactions-events.ts`. No behaviour change (handlers never fired). API typecheck clean; account-mapping + inventory-listener suites 185/185 green. |
| 68 | 2026-07-06 | 09 ZATCA listener | LOW | **`zatca.listener.ts` `@OnEvent(POS_EVENTS.TRANSACTION_COMPLETED)` is also dead** (same reason — nothing emits the POS domain event). So ZATCA e-invoice generation would **never fire from a POS sale** if it relied on this trigger. **Zero live impact for Asala** (Kuwait — no ZATCA/VAT e-invoicing). | Grep as #67. | For a future KSA tenant, ZATCA POS coverage must hang off the live `accounting.post`/inventory event or an explicitly-emitted POS event, not this unreachable handler. | NOTE / tracked to the ZATCA epic — verify ZATCA POS coverage is wired to a live event (or intentionally deferred to Phase 2). No fix this pass; flagged so the ZATCA work doesn't silently ship a dead trigger. |

### Submodule 09 — recon verdict (2026-07-06)

**Every cross-module contract holds on live data.** All 69 POS-sourced journal entries are balanced with unique, non-null eventIds; all 24 completed sale lines relieve stock exactly once (batch-split rows are legitimate, totals tie, zero over-relief); the void `-2-3` nets to zero on both GL and stock at original cost; shift #1's close posts a correctly-signed over/short JE and the open shift #2 posts nothing. The POS module writes **no** JEs or stock directly. **The headline §83 event-durability CRITICAL is definitively MITIGATED, not merely assumed:** the four POS events flow through a durable transactional outbox (same-tx insert, atomic with the POS write) + post-commit in-process fast path + idempotent eventId dedup (partial-unique JE index, full-unique stock index, pre-check SELECT, race catch) + a DLQ with bounded retries and manual replay — giving at-least-once delivery with idempotent replay and no silent loss/double-post. Two findings, both **non-blocking**: #67 dead `pos.listener.ts` (MEDIUM maintainability — **FIXED/deleted this pass**, `c64422ac`) and #68 dead `zatca.listener.ts` POS trigger (LOW, no live impact for Kuwait; tracked to the ZATCA epic). **No CRITICAL/HIGH found.**

## ✅ Submodule 09 — Cross-Module Contracts — SIGNED OFF (2026-07-06)

Full live-data recon (POS record → journal_entries/journal_entry_lines → stock_ledger_entries) over all 18 sales / 9 returns / 1 void / 2 shifts on prod Asala (KWD 3dp) + complete event-durability code audit (emit → same-tx outbox → poller → idempotent eventId-deduped posting → DLQ). All CRITICAL/HIGH pass.

**Load-bearing 09 invariants — GREEN on live data:**
- **Balanced JE, every POS entry:** 69/69 POS-sourced journal_entries balanced (ΣDR=ΣCR), 0 unbalanced.
- **eventId idempotency:** 0 null + 0 duplicate event_id across the 69 JEs and the 40 POS stock-ledger rows; enforced by `je_event_id_key` (partial unique) + `sle_event_id_key` (full unique) + posting-service pre-check SELECT + concurrent-race catch. Replay is a true no-op.
- **Line → ledger completeness:** 24/24 completed sale lines relieved exactly their qty (0 dropped, 0 over-relief); batch-split rows are legitimate (distinct `batch_id`, totals tie).
- **Return/void net-zero:** void `-2-3` nets to 0 on GL (Cash/Inv/COGS/Discount; revenue via 4200 contra) AND stock (qty 0, cost 0, original-cost basis).
- **No direct POS write:** zero JournalEntry/StockLedger injections in `pos/`; all cross-module writes flow through `accounting.post` + the inventory domain event.
- **§83 event-durability CRITICAL MITIGATED (proven, not assumed):** transactional outbox insert atomic with the POS write (same `tx`) + post-commit fast-path + idempotent eventId dedup + DLQ (5 attempts, backoff, dead_letter, manual replay) = at-least-once with idempotent replay, no silent loss, no double-post.
- **shift.closed GL:** shift #1 shortage −4.70 → DR 6700 / CR 1112 (correct sign, balanced); open shift #2 posts nothing; no zero-amount entries.

**Fix shipped this pass (erp main):** #67 dead `PosAccountingListener` removed (`c64422ac`) — deletion behind API typecheck + 185-test suite; the live JE mapping in `pos-transactions-events.ts` is now the single source of truth (drift risk eliminated). **Tracked (not this pass):** #68 dead ZATCA POS trigger → ZATCA epic (verify KSA POS e-invoice coverage hangs off a live event).

**Verdict:** every POS event correctly ripples into a balanced GL entry and a signed stock movement; the trail is idempotent, atomic, durable, and never double-posts on the live dataset. The feared partial-write/lost-event CRITICAL is closed by the transactional-outbox architecture. Signed off. **POS module testing program (01–09) complete** (10 Printing & Receipts is device/agent-layer, tracked separately).
